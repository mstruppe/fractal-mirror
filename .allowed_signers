# FRACTAL allowed-signers file (C-074, 2026-08-14) — infrastructure, C-006 exception class (like .gitignore).
# Custody-scoped signing: ONE key = Max's custody environment. A signature attests that a commit was
# created inside that environment — it never authenticates WHICH writer authored it (the author field
# attributes, C-037; per-writer keys arm at the first writer outside this custody environment).
# All three writer identities therefore share the single custody key below.
# Bind locally after clone: git config gpg.ssh.allowedSignersFile <absolute-path-to-this-file>
max.struppe@gmail.com,claude@fractal.local,codex@fractal.local ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIHOQaxo9q0mqWsubXl1y0BmzSi5gmLLBymsSchMjl/gp
