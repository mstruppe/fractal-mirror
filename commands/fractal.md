---
description: Open a FRACTAL working session — load the context spine and report where things stand
---
Open this FRACTAL working session per the standing orientation procedure (CLAUDE.md rule 1; the Local Context's opening line):

1. Read `Claude/Context Packages/Global/Fractal_Global_Context.md` (vision + current realisation).
2. Read the active Local Context named in `Claude/Context Packages/Fractal_Context_Index.md` — currently `Claude/Context Packages/Local/Fractal_Local_Context_Knowledge_Graph.md`.
3. Read the Return Package the Local Context's **Active Return Package** pointer names (its opening line) — the exact path is the selector; "newest file" ordering is retired (it lies on same-date closes, and a fresh clone erases file dates).
4. Run `python3 "Claude/Knowledge Graph Store/fieldnote.py" --depth` — the buffer's pressure reading (C-121).
5. Do not scan anything else (rule 1: orient, don't scan; rule 3: load only what the task needs).

Then report back briefly: where the project stands, what is queued or standing open (the G offers and H roadmap), anything recorded as pending on Max, and the fieldnote-buffer depth (relay the tool's YELLOW advisory if it stands — work-down is session material, C-121) — and stop there. The queued agenda is an offer, never a mandate (C-031); wait for Max's direction before starting work.

---
*Stamped-procedure projection (C-035 class, per Scan #3 S3-3.1 / Protocol v0.29): compresses the standing orientation procedure — sources: CLAUDE.md rule 1 + the Local Context's opening line; the active-Local pointer above is a convenience — the Context Index is the one authoritative place (C-012). Stamped 2026-08-15; the buffer depth line added 2026-08-18 (C-121 — the pressure plug surfaces at orientation and the close walk); the Active-Return pointer made the sole selector 2026-08-21 (the F2 cure — IF-0011's conceded "newest file" gloss retired, KNet's catch credited). Refresh triggers: a change to the orientation procedure, an active-Local-Context change in the Index, a Fieldnote Format reissue moving the gauge, or a C-059 walk finding this stamp stale.*
