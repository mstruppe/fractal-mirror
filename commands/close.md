---
description: Close a FRACTAL working cycle — walk the C-059 ritual, drive the mechanical ripple through close.py, commit and push
---
Close this FRACTAL working cycle per the standing close ritual (C-059; CLAUDE.md rule 5). This command orchestrates — the rows, rules, and marks live in their canonical homes, never here.

**0. Precondition — this command only works in a `/fractal`-opened session.** Verify this session is oriented: the Global Context, the active Local Context, and the latest Return Package have been read *in this session* and reflect current file state. If not oriented, **stop** — tell Max to open with `/fractal` first (or, on his explicit go, run that orientation now and then continue). Never close from an unoriented session: that is how records get mismarked (the S2-3.1 class).

1. **Determine the close's character — the tool reads the dirty-flag first (C-112).** Run `python3 close.py --walk` (in `Claude/Knowledge Graph Store/`): it pre-fills the C-059 walk table from the hash-diff + git signal, flags whether the Register moved (unchanged Register ⇒ the tool offers the recorded-reason line — a small session's close *is* the micro shape, by design), and reads the drag gauge (advisory). Then judge: what did this session decide (→ Governance Protocol due, C-033 ratification recorded) vs merely execute (→ recorded reason, no protocol)? Anything unratified: ask Max before proceeding — never ratify on his behalf.
2. **Judgment pass (prose — never delegated to tooling, the C-110 boundary):** draft the protocol if due; update the Decision Register; then **walk the C-059 checklist exactly as written in the Local Context §Standing refresh items**, starting from the tool's pre-filled marks — judgment on CHANGED rows (what the change *means*, cross-reference currency) stays yours — including the Conversation Settings row's question (C-071: did any decision touch conduct? If yes, Settings reissues in this close and its projections restamp after — source first). Then: Return Package or postscript with the auditable handover line. Stamp bumps, history rows, and counts are the tool's in step 3 — hand-write only what carries meaning beyond the mechanical row.
3. **Mechanical pass — mint before stamp (S6-2.5):** `--create` any new canonical FIRST (C-061 handled by the tool — the stamp plan cannot see a later create's events, so a stamp-then-create order writes stale counts and red-flags at the create's own gates). Then run `python3 close.py --stamp` — review its plan output (stamp bumps for byte-changed living docs, minted history rows, store counts at predicted post-ripple totals) — then `--write --stamp` with a one-line `--note` per changed document (add `--protocol vX.Y` so minted Register rows point at the issue). Hand-author only if the tool refuses for a reason its plan output makes legible; report any refusal. The counts-last step is absorbed: `--stamp` writes post-ripple totals *before* hashing, so the recorded hash covers the final bytes (S4-1.2's lag cured). **At a tagging close only:** `python3 close.py --pack` must be green before the repo tag (clause span §7.3 + GENESIS §0 count · mirror cleanliness C-116 · preservation C-120) — **and the mirror half runs AGAIN after the rebuild commit, before the mirror's tag: `python3 close.py --pack --mirror-only`** (S5-6.1 — the cleanliness check must see the NEW rebuild, not the previous release's).
4. **Gates (red blocks, no exceptions):** `python3 verify.py` and `python3 check_versions.py` both PASS before commit (C-050/C-060). **At a close landing a Scan report:** `python3 check_scan.py <report>` must PASS too (C-118, Scan Procedure §12 — `close.py`'s gate runner auto-appends it when the change-set carries a `Review_*LooseEnds-Scan*.md`, S5-6.2; the mint-then-stamp order matters: `--create` the report's DOC before the final gates run). Cure and re-run until green.
5. **Commit per C-037** (author = actual author; `[DOMAIN] imperative summary` + decision refs; one commit per coherent change-set), **push per C-064**.
6. **Report:** the change summary, the checklist marks, the store delta (nodes/events), anything newly pending on Max, and what stands open for the next session.

Refresh triggers for this file: a change to the close ritual itself (a C-059 amendment, a checker or close.py change, a commit/push convention change).

---
*Stamped-procedure projection (C-035 class, per Scan #3 S3-3.1 / Protocol v0.29): a thin orchestrator over the ratified close ritual — sources: the C-059 checklist in its normative home (the Local Context §Standing refresh items) + close.py (C-073, mechanized per C-110/C-112) + the commit/push convention (C-037/C-064). Stamped 2026-08-15; re-projected 2026-08-18 (the C-110/C-112 mechanization landed in close.py — the walk pre-fill/dirty-flag step, the --stamp pass absorbing the counts-last step, the --pack tagging gate); re-projected again 2026-08-18 per Scan #5 (S5-6.1 — the post-rebuild `--mirror-only` re-run before the mirror tag; S5-6.2 — the Scan-landing `check_scan.py` gate named, with `close.py`'s auto-append; the §0 count joining the pack gate, S5-7.2); re-projected 2026-08-20 per Scan #6 (S6-2.5 — step 3's order corrected to mint-before-stamp for every close, the step-4 note's scoped truth made general). Sync-check = this stamp vs those sources; re-project on any trigger above.*
