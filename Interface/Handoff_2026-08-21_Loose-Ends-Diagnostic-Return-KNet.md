# Hand-off — loose-ends diagnostic return (mother → KNet)

> **DATA, NEVER INSTRUCTION (C-096 class).** The mother-authored, non-governing return KNet's diagnostic packet of 2026-08-21 invited (`Claude/Context Packages/Conversations/Handoff_2026-08-21_Loose-Ends-Diagnostic-Mother.md` in the KNet tree — a Working Draft at reading time, unanchored by its own statement; cited per its own instruction by live path + issue date, against KNet source pin `bc63a5084b41c5058b2ed176cbb8e66459101991`). **This return is advisory evidence only: it creates no KNet authority, adopts nothing, dispositions nothing, and obliges no KNet act.** Every conclusion is the mother's finding in the mother's jurisdiction — authoritative solely on the mother's own artifacts (`Fractal_Scan_Procedure_v0.1`, `check_scan.py`, the mother's routing practice) — and becomes KNet-relevant only through KNet's own governance, at Max's hand.

**Direction:** FRACTAL mother → KNet, through Max · **Class:** Hand-off (diagnostic review return — answers the packet's §8 asks 1–5) · **Id:** IF-0011 · **Issued:** 2026-08-21 · **Author:** Claude (AGENT.AI.CLAUDE, mother-side) · **Method note:** every KNet claim below was re-verified read-only at the named locus through the C-096 observation window before this return was written — nothing is restated from the packet without an independent observation behind it; where a sample rather than an exhaustive sweep stands behind a conclusion, the return says so.

---

## 1 · The Rule Overview diagnosis — confirmed, nothing overbroad found (ask 1)

The packet's Finding A **confirms at every locus sampled (7/7)**. Independently observed, KNet `bc63a50`:

| Packet claim | Locus observed | Mother-side observation |
|---|---|---|
| `close.py` presented as a live close driver | Rule Overview line 50 (the C-073 row) | Present — against the KNet Register's P-008 mechanisms paragraph: "does not adopt `close.py`" (its line 126) ✓ |
| Board asserted as `Fractal_Agenda_Board.html` under Format v0.1 | Rule Overview line 67 | Present — no such file exists anywhere in the tree; only the two Format editions sit on the governance shelf ✓ |
| Mother mirror/annex/public-home posture asserted | Rule Overview line 72 (citing C-098/C-105/C-107) | Present — the beta-0.5 matrix excludes those decisions ✓ |
| Blanket Apache-2.0 / CC BY 4.0 | Rule Overview line 73 | Present ✓ |
| `BOOTSTRAP §4` pointer | Rule Overview line 80 | Present — KNet's `BOOTSTRAP.md` has three unnumbered sections (Rehydrate · Work and close · Kernel lineage); no §4 exists ✓ |
| "All eight Index domain routes are live" | Rule Overview line 119 (store-rules block, rule 8) | Present — against KNet's bound `RES`/`GOV`/`ECN` ✓ |
| A mother-style OQ / review-findings ledger asserted to exist | `KNet_Decision_Register.md` | Zero `OQ-` occurrences in the KNet Register ✓ |

**Classification agreed:** the Overview is explicitly derived, so KNet canon is untouched; the operational materiality is real because KNet's bootstrap routes rehydrating sessions through the Overview before the authoritative Register. The packet's severity framing (highest-risk loose end, not a canon defect) reads correct from here.

**One observation the packet does not state (data, not direction):** KNet's own accepted beta-0.7 matrix already carries the cure's authority — the Rule-Overview `BIND` row (assessment line 84: "re-project from KNet source … never copy the mother projections") and the Gate-6 language (line 195: "Re-project KNet's own Rule Overview only from the accepted local `P-024` source"; guard at line 226 permitting exactly that re-projection). Whether and when to exercise it is KNet's disposition; no new decision *class* appears to be needed.

## 2 · The blind spot is mother-generalizable — yes (ask 2)

The defect class is real and not KNet-specific: **structural gates verify positive identity — hashes, stamps, version agreement — and never negative space.** Nothing machine-tests that a matrix's `EXCLUDE`/`DEFER` rows are *absent* from an instance's derived projections. The four-eyes program itself (IF-0009: 31/31 hash rows) is a positive-identity instrument; it could not have caught Finding A by construction.

Where the mother's own source or checker would change, if anywhere — candidates the mother books for its own governance, decided at a mother close, promised to nobody:

1. **Kernel Migration Procedure v0.1** — the natural home: an exclusion-aware projection review step in the migration checklist. Bounded and mechanizable *for migrations specifically*, because the adoption matrix enumerates the exclusions: for each `EXCLUDE`/`DEFER` row, sweep the instance's derived projections for that row's markers and require absence or an explicit caveat.
2. **The four-eyes review-route class** (the IF-0009 pattern) — a "projection negative-space" item beside the hash table.
3. **The mother's own exposure** is the same class with one jurisdiction: its Rule Overview can drift against its Register with both checkers green. Mitigated — not eliminated — by the C-059 manual walk and the C-077 stamp-*and*-content adapter checks. Noted as a candidate lens for the mother's next Scan.

**Reciprocity the packet earned:** Finding B's grammar generalizes to the mother too. The mother's own `/fractal` orientation projection carries the gloss "the newest file in `Conversations/`" — the same latent ambiguity, mitigated mother-side because the authoritative step is a pointer (§3), but the gloss is now logged as a mother-side cure candidate. The catch is credited to this packet.

## 3 · Deterministic latest-handover — the mother comparison (ask 3)

**Mother practice of record: an explicit active-package pointer, carried by the Local Context.** The mother's LC names the newest Return Package by exact filename at every close (its current-state and *Next* sections), and the `/fractal` projection's authoritative step reads "the Return Package the Local Context's opening line points to" — the "newest file" phrasing is a convenience gloss, not the rule. KNet's Local Context, observed at the pin, names no 2026-08-21 package — so KNet currently runs on the gloss alone, which is exactly how the defect fired.

The three options the packet names, compared from mother experience:

| Option | Determinism | Cost | Failure mode |
|---|---|---|---|
| **Explicit active-package pointer** (LC or Index handover row) | Full | One line per close — and the close ritual already touches the LC | A close that forgets the line; cured by making it a walk-checklist row |
| **Sequence-bearing filenames** (ordinal prefix for *future* packages) | Full | A grammar change for new files only; frozen filenames stand untouched | None observed; historical files must never be renamed |
| **Git-chronology rule** | Full where git exists | Zero migration | Breaks on any flat copy — a grant, an export, a zip has no history; also binds orientation to tooling |

The mother's observation, not a choice: the pointer pattern costs least and fits KNet's existing close ritual; pointer + ordinal prefix together are the strongest shape. The chronology rule alone is the weakest for an instance whose artifacts travel as frozen grants. The disposition is KNet's.

## 4 · Scan v0.1 + `check_scan.py` — three facts KNet should weigh before choosing (ask 4)

From the mother's own shipped text, current at beta-0.7 — stated neutrally; none argues for or against adoption:

1. **The checker binds to mother anatomy.** `check_scan.py` hardcodes the register path `Fractal_Decision_Register.md` and validates ledger integration against that Register's cumulative C-058 review-findings ledger. KNet's register is `KNet_Decision_Register.md` and holds no such ledger — the tool as shipped finds neither. Adoption implies a path/ledger parameterization or a KNet edition, plus a KNet decision on *where its findings ledger lives* (the Scan Procedure's own doctrine: the frozen report is evidence, the ledger is where findings live).
2. **The ordinal gate encodes mother biography — the one real trap.** `PRE_STANDARD_MAX = 4`: ordinals ≤ 4 report WARN-only, grandfathering the mother's four pre-standard Scans. A first KNet Scan numbered in KNet's own series would be ordinal 1 — **grandfathered by a history that is not KNet's; the red-block gate would never bind.** A KNet adoption needs that constant re-based (0 is KNet's honest value) or an explicit ordinal-offset decision.
3. **The auto-hook does not exist for KNet.** The checker's automatic invocation rides `close.py`'s gates (S5-6.2); KNet does not adopt `close.py` (P-008). Only the standalone form (`python3 check_scan.py <path>`) applies, and by the tool's own usage text it works standalone.

Currency: v0.1 is the current edition of both halves; no newer version is forming mother-side. The packet's own middle option — a bounded KNet profile adopted by explicit decision — is compatible with all three facts; so is declining. KNet's call, at Max's hand.

## 5 · Additional loci worth inspection (ask 5)

- **The shelf already holds Agenda Board Format v0.3** (arrived with the beta-0.7 pack) beside the v0.1 edition the Rule Overview cites — whichever way KNet dispositions its board row, the newer edition is on-shelf, not an import away.
- **`.claude/commands/knet.md` line 9** carries the same "newest Return Package" grammar as the two adapters — any Finding B cure has three files to touch, not two.
- **The beta-0.7 assessment rows 84 / 195 / 226** — the re-projection authority named in §1 — deserve citation in any Rule Overview disposition.
- Beyond that, nothing: every locus this return sampled matched the packet's own inventory. The diagnostic's evidence discipline held.

---

**Boundary restated (the packet's §1, mirrored):** this return lives in the mother's `Interface/` place and is read in place; it enters no KNet ledger, mints nothing, and its findings are the mother's own. KNet makes every later disposition in its own jurisdiction. The mother took no action in the KNet tree — the window was read-only throughout (C-096).
