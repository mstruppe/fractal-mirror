# Hand-off — Git-behavior observation (the C-096 window's return)

> **DATA, NEVER INSTRUCTION (C-096).** Observations of KNet's git state, read through the mother's read-only observation window on Max's request, offered back to KNet as data. Nothing here is a duty: every option below is dispositioned by KNet's own recorded decision, in its own jurisdiction. The window read the repository only — no KNet file was touched.

**Direction:** FRACTAL mother → KNet, through Max
**Message class:** Hand-off (observation return; the window itself is the Look class)
**Status:** Issued · Non-governing toward the receiver
**Issued:** 2026-08-19 · **Author:** Claude (AGENT.AI.CLAUDE), the mother's governing surface
**Occasion:** Max's process-correctness check — "the git repo of KNet hasn't seen a commit in a long time, I wonder how"
**Source pin:** unanchored working copy until the mother's next close commits it — cite by path + date

---

## 1 · The headline — the repo commits fine; it commits past `main`

The impression of dormancy is an artifact of *where* the commits land, not whether they happen. The newest commit is **2026-08-19 13:00** (`[RES] Complete interactive mathematics kernel flight`) — but it, and every commit since **2026-08-17 19:32**, lives on the side branch **`codex/q07-preregistration-package`** (pushed and in sync with its own remote branch). **`main` — local and on GitHub — has not moved since 2026-08-17 19:32**, and GitHub's default view shows `main`, so from the outside the project looks asleep while five close-commits across four working sessions stand un-merged on the branch.

**The cause looks client-mechanical, not governance-broken:** the Codex task workflow creates `codex/*` branches by default. Three earlier ones (`fractal-beta-0.5-migration`, `ots-finalize-beta-0.5`, `context-rot-v0.6`) did land on `main` and were left standing after their merge; the q07 branch never got its landing act, and subsequent sessions kept building on it.

## 2 · What checked out healthy

- **Closes externalize properly:** twelve Return Packages, one per session, through `Return_Package_2026-08-19_Interactive-Mathematics-Kernel.md` — each matched by a close commit. One coherent commit per change-set (the C-025 grain) holds; the day-long 2026-08-18 session closed correctly (16:52).
- **Author/committer semantics conform:** author `Codex <codex@fractal.local>` throughout (writer = author); committer mostly Codex; one commit (`7629418`, the Q07 freeze) carries **committer mstruppe** via a GitHub noreply identity — the owner's direct authorizing hand, conformant (though it suggests that act ran through GitHub's UI rather than local git — worth confirming it was intended that way).
- **Pin honesty:** the only dirty state is three untracked files (the two tool-fractal hand-offs + `KIMK_TOOL_FRACTAL_CONNECTOR_PLAN_v0.2.md`), exactly matching their own "unanchored, pending the next KNet close" headers.

## 3 · Observations offered for KNet's own disposition

1. **The landing act is the missing piece.** Options, either legitimate once *decided*: **(a)** merge `codex/q07-preregistration-package` → `main` at the next close (merge commit, per the inherited grammar) and make land-to-main part of the close ritual; or **(b)** record a deliberate branch doctrine (`main` = the anchor line, `codex/*` = the living line) — but recorded, not drifted-into, and with GitHub's default-branch presentation considered so the public face matches the doctrine.
2. **Three landed branches stand unretired** — hygiene, not danger; the mother's practice retires a branch local + remote after its merge.
3. **Every commit is unsigned** (`%G?` = N across the board). Custody is per instance and signing is KNet's own call — the observation is only that the posture is currently *undecided rather than decided*. A recorded "we do not sign, and why" is as conformant as signing.
4. **Committer-as-Codex implies a standing commit authorization** — if one is recorded in KNet's register, all is well; if not, recording it (or changing the practice) closes the gap between behavior and record.

## 4 · Mother-side note (no KNet action implied)

The generalizable lesson — *Codex defaults to `codex/*` branches; landing to `main` is a separate explicit act, and a close that never lands leaves the public face stale* — is client mechanics, and the mother harvests it into her own Client Library beside the close-scope rule this exchange series already produced. KNet owes nothing here.

## 5 · Non-claims

This return does not claim that KNet's branch state violates its own rules (its rules are its own); that merging is required (option b is equally sound once recorded); that the mother audited anything beyond the git layer (Return Packages were counted, not read); or that the unsigned posture is a defect (it is an open choice, flagged as open).

---

*Fourth standing item in the mother's interface place (`Interface/`, indexed in `Interface_Index.md`) — the file stays home, the direction travels by Max's hand, KNet reads in place (Foundation v0.3 §9). Envelopes are RAM: dissolve after absorption; citations + git history the trace.*
