---
description: Open a FRACTAL working session — load the context spine and report where things stand
---
Open this FRACTAL working session per the standing orientation procedure (CLAUDE.md rule 1; the Local Context's opening line):

1. Read `Claude/Context Packages/Global/Fractal_Global_Context.md` (vision + current realisation).
2. Read the active Local Context named in `Claude/Context Packages/Fractal_Context_Index.md` — currently `Claude/Context Packages/Local/Fractal_Local_Context_Knowledge_Graph.md`.
3. Read the Return Package the Local Context's opening line points to (the newest file in `Claude/Context Packages/Conversations/`).
4. Do not scan anything else (rule 1: orient, don't scan; rule 3: load only what the task needs).

Then report back briefly: where the project stands, what is queued or standing open (the G offers and H roadmap), and anything recorded as pending on Max — and stop there. The queued agenda is an offer, never a mandate (C-031); wait for Max's direction before starting work.

---
*Stamped-procedure projection (C-035 class, per Scan #3 S3-3.1 / Protocol v0.29): compresses the standing orientation procedure — sources: CLAUDE.md rule 1 + the Local Context's opening line; the active-Local pointer above is a convenience — the Context Index is the one authoritative place (C-012). Stamped 2026-08-15. Refresh triggers: a change to the orientation procedure, an active-Local-Context change in the Index, or a C-059 walk finding this stamp stale.*
