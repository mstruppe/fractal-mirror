---
description: Begin a new FRACTAL-governed project — the onboarding interview births your instance and walks you into your first loop
---
Run the guided first loop per `Claude/Project Governance/Governance Documents/Fractal_Onboarding_Protocol.md` — read it now, in full; it is the script and this command only triggers it.

Standing check first (one read-only command, as in `/welcome`): in a **distribution clone / reference copy** you are the midwife this protocol expects — proceed. In the **governing FRACTAL repository itself**, births still run fine from here (the tool stages elsewhere); say where the session stands, then proceed.

Then stage one, §1–§2, exactly as scripted: six real questions, one at a time, defaults stated not asked — **and on this client, the choice-shaped stations render through the native question panel** (the §1 native-form provision, the founder's panel ruling 2026-08-23): §1.2b the place (the proposed default listed first), §1.4 the territories (multi-select over the proposed codes), §1.5 durability, §1.6's credentials question — one station per panel, never a bulk form, option descriptions carrying the protocol's inline teaching, the panel's "Other" door standing in for free text, the read-back-and-confirm after every answer unchanged; the free-text stations (§1.1 project, §1.2 name, §1.3 writers) stay conversational. The place question (§1.2b, *where on your machine should it live?*) asked, never assumed, its mechanics pre-answered inline (the tool creates the folder at `--target`; nothing to pre-create); the dry-run read together before any `--write`; the three day-one checks run (§2.2 — the two frictions, plus **the founder reads their day-one receipt**: the C-134 record genesis wrote into the newborn's Governance Documents — which parameters they set, which defaulted on silence, what the birth pinned; distinct from the frictions by design, nothing is wrong); the midwife's exit with **the care moment** (§2.3, C-137 — one breath as the project becomes theirs: a living system, its integrity carried by its own rituals, keeping them running the care it asks — the pet, beta-honest) and the suggested return path. Stop at the handoff — stages two and three live in the child's own sessions (jurisdiction, §0.6).

If the user arrived here without context (`/begin` as first contact), give the one-paragraph `/welcome` explanation first, then ask if they want the interview.

---
*Stamped-procedure projection (C-035 class): the shipped-command projection of Fractal_Onboarding_Protocol v0.8 — the first of its three declared projections. Stamped 2026-08-16 (the flip-preparation session); restamped 2026-08-21 (the-place reissue — §1.2b, the six-question count); restamped 2026-08-22 (the care-moment reissue — C-137, §2.3); restamped 2026-08-23 (the receipt reissue — C-134, §2.2's third day-one check); restamped 2026-08-23 again (the native-form reissue — the §1 client-form provision rendered concrete for this client's question panel; per-client mechanics live in the Client Library, C-097 — a Codex-surface projection renders through Codex's own affordances). Refresh triggers: any Onboarding Protocol reissue; a GENESIS reissue moving §1–§3; a C-059 walk finding this stamp stale.*
