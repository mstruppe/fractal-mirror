# Fractal Local Context — Knowledge Graph

> **DERIVED PROJECTION — dynamic working context (Local pole).** Living document: stable filename, version tracked below. This is "where we're looking right now."

**Version:** 0.81 · **Status:** Living (derived projection) · **Updated:** 2026-08-20 (the thirtieth session — the beta-0.6 tagging close: the KMR four-eye return, the section contract C-130, the advisory, both diets, the pack) · **Parent:** Fractal Global Context

**Aspect of the current realisation (opening convention):** within the *living-publicly-and-refining-through-use* phase (Global §2, v0.8), this addresses **refinement through use (C-107) on a now-public system** — FRACTAL improved by using it: the instantiation program (C-078) with arc 2 live (KNet), the mother's building lanes, the field-test loop carrying a public intake (issues as the off-machine fieldnote door, C-120). **The flip is executed** (2026-08-18, C-120 — live ≠ official): the mirror public, the site served from `gh-pages`, the gate at `knet.network`. The release seam stands standardized on both sides (C-116 supply · C-117 demand); **beta-0.6 the newest anchored release** (packed at the thirtieth session's close), **PR's birth from its tag the armed next act**. The KG/governance stack is complete and under machine guard; this context tracks its life in use, in public.

## Session record (era-compacted — history lives frozen; this table only points)

Every session's full narrative lives in its Governance Protocol (`Claude/Project Governance/Governance Protocol/Fractal_Governance_Protocol_Claude_v<X.Y>.md`) and its Return Package (`Claude/Context Packages/Conversations/Return_Package_<date>_<topic>.md`).

| Sessions | Era | Of record |
|---|---|---|
| 1st–9th | **The foundation era** — store, checkers, routes, navigation, custody; the instantiation program (arc 1: GENESIS + `genesis.py`, birth-tested); the vision whiteboard and the concept tier; the punch-list close (six tools); the legal layer; the first realisation transition; Scan #4 the pre-pack gate; **beta-0.1 packed** | Protocols v0.1–v0.36; Scans #1–#4; the dated Return Packages |
| 10th–16th | **The shipping arc** — KNet born + accepted (arc 2 opens); the phase-5 ingestion (C-091/C-092); the workspace forge; the second-client trial; the flip preparation (C-098–C-102: annex, licenses, fieldnote pipeline, onboarding, `/begin`); the capture program (C-103, 67 agents — the approved public-identity raw material); the publishing identity (C-105: mirror-as-home, `fractal@knet.network`); Stranger Test #1; C-106 the named opener; **betas 0.2–0.4 packed** | Protocols v0.37–v0.47; `Capture_2026-08-16_Vision-to-Horizon.md`; `Review_2026-08-17_Stranger-Test-1.md` |
| 17th–20th | **The refinement pivot + the release seam** — C-107 refinement-through-use (flip de-triggered to necessity); C-108 the flight tier + the registry vision; C-109 the frozen-tree grant; the diet program (C-110–C-112, DF1/CB1/DP1); the registry release (C-113–C-115, **beta-0.5 packed**); the TF1 postscript; C-116 the mirror rider; **KNet migrated to beta-0.5** (P-008) → UF1 → C-117 the Kernel Migration Procedure; C-118 `check_scan.py` | Protocols v0.48–v0.54; the flight records (Refinement · Diet pair · Migration-Test · Updatability) |
| 21st | **The public-identity build** — FRACTAL stays the name (umbrella → OQ-34); the profile groundwork; the first mark (*The Kernel*); the site's fractal layout built presentable | per v0.55; `Return_Package_2026-08-18_Public-Identity-Build.md` |
| 22nd | **The engine-sketch session** — C-119 (parts exposed verbatim + downloadable, command links, parts folders refresh at tagging closes); the mirror's conflict-artifact cure; the C-116 cleanliness amendment banked | per v0.56; `Return_Package_2026-08-18_Engine-Sketch.md` |
| 23rd | **The go-live** — C-120 (the flip, Max's hand; the gate; the serving split; the intake layer); the Value Model (C-086's third member); Network Foundation v0.4; Global §2's second transition | per v0.57; `Return_Package_2026-08-18_Go-Live.md` |
| 24th | **The mechanization close** — close.py the close's mechanical half (`--walk` · `--stamp` · `--pack`); TF1-4 cured (the TF1 slate fully closed); FN-0009; Scan #5 commissioned | per v0.58; `Return_Package_2026-08-18_Mechanization.md` |
| 25th | **Scan #5 + the full slate** — the flight (15 agents; the gas-metering lane — OQ-28's first datum); "full slate" (33/35 cured in-session); both diets executed; the gas article + KNet hand-off; **the Fractal Interface Foundation ratified** (C-086 fourth member); the two agenda points opened (grooming; the second child) | per v0.59; `Return_Package_2026-08-18_Scan-5-Full-Slate.md` |
| 26th | **The fieldnote-RAM + network-vision session** — the hygiene sweep (S5-8.3's refresh half); **C-121** (the buffer); the user-document trio; the network vision (the OQ-34 seat annotation; Foundation v0.2 — PR + the inbox transport) | per v0.60; `Return_Package_2026-08-19_Fieldnote-RAM-Network-Vision.md` |
| 27th | **The interface-birth session** — the instrument forge (OQ-35); **C-122** (the mirror doctrine); the first C-121 work-down; the three-leg KNet tool-fractal exchange (Foundations v0.3/v0.4); the C-096 git observation; **`Interface/` born** | per v0.61; `Return_Package_2026-08-19_Interface-Birth-Workdown.md` |
| 28th | **The interface-build session** — **C-123** (Interface Place Format v0.1); **C-124** (the pair series + the Update Plan pair); **C-125** (the takeover gate) | per v0.62; `Return_Package_2026-08-19_Interface-Build-Pair-Series.md` |
| 29th | **The user-documents session** — **C-126** (the `User Documents/` folder); the **Mining Tool sorted** + the **beta-0.7 intake**; **C-127** (the integration categories); **C-128** (the rolling done bar); **C-129** (the Gas pair); the **grooming pass opened** | per v0.63; `Return_Package_2026-08-19_User-Documents-Gas-Taxonomy.md` |
| 30th | **The beta-0.6 tagging close** — the **KMR/KIMK four-eye return** (IF-0005: one factual overturn — the "missing" receipt recovered from KNet's own object store, rescue time-bounded; the live-then-freeze direction its §6); the **grooming pass executed + checked** (**C-130** the section contract: dictionary · story · **the Research Labs** (de-branded at the gate, OQ-34); the `Site/concepts/` detail tier; *imply-never-announce* benched eighth); the ritual: the **§8 advisory first-instantiated** (IF-0006; UF1-7 cured) · GENESIS v0.15 + AST v0.30 the pack-gate cures · the parts refresh · **both scheduled diets** · the pack (tag + anchor + mirror rider with the first C-127 exclusion + `gh-pages` — the new site live; S5-8.3 closed whole) · the Update Plan reset to beta-0.7 | per v0.64; `Return_Package_2026-08-20_Beta-0.6-Tagging-Close.md` |

*Era-compacted per the Diet Procedure (rows 1–20 → three era rows at the twenty-fifth session; rows 26–29 slimmed at the thirtieth — the third LC diet, **no disposition changed**): every fact remains recoverable in the named Protocols and dated Return Packages (the fact-presence guarantee, Diet §6.2; behaviour test = this file still orients a fresh `/fractal` session end-to-end; diet history frozen in Protocols v0.40/v0.59/v0.64).*

## Current state

**The operating mode is refinement through use in public (C-107 + C-120); FRACTAL is live on both addresses** (`knet.network` → the gate → `mstruppe.github.io/fractal-mirror/`; live ≠ official — no announcement, OQ-34 asleep). The release seam is closed on both sides: **beta-0.6 shipped at this close** (C-090; the site's new C-130 layout riding its `gh-pages` refresh), the mirror rides every beta (C-116), **KNet runs on the beta-0.5 kernel** (P-008; beta-0.6 its next migration offer). **The serving split (C-120):** site refreshes are ordinary `gh-pages` pushes (pull-before-push; **the domain rides the gate repo** — S5-8.2); mirror `main` keeps the pure release rhythm; the rebuild preserves `.github/` + `gh-pages` (tooled — `close.py --pack`). **The public intake is live:** issues = the off-machine fieldnote door; PRs = proposals, never merges.

**Scan #5 stands executed** (the twenty-fifth session): 35 findings, 0 High, **33 cured in-session**, the ledger carrying every disposition — the flight, the gas-metering lane (OQ-28's first quantitative datum), the cure slate, and both diets live frozen in Protocol v0.59, `Review_2026-08-18_LooseEnds-Scan-5.md` (Ratified, "full slate"), and `Flight_2026-08-18_Scan-5-Gas-Metering.md`. Open residue: the spec-reissue pair (S3-2.2 + S5-2.4, the beta-0.7 intake); S5-8.3's live half closes with the beta-0.6 `gh-pages` refresh.

**The twenty-ninth session** (C-126 the `User Documents/` folder · the Mining Tool sorted + the beta-0.7 intake · C-127 the integration categories · C-128 the rolling done bar · C-129 the Gas pair · the grooming pass opened): Protocol v0.63 + `Return_Package_2026-08-19_User-Documents-Gas-Taxonomy.md`, pointer-weight per C-110. **The thirtieth session (this close): the beta-0.6 tagging close.** Three arcs before the ritual: the **KMR/KIMK independent review return** (IF-0005 — the four-eye ask executed at KNet's pin: two of its three loose-end readings confirmed and widened, the third **overturned** — the "missing" 1.0.0 receipt bytes found as an unreachable blob in KNet's own object store, verified byte-exact and semantically bound, the rescue time-bounded by gc; Max's live-then-freeze architectural direction recorded as the return's §6); the **grooming pass executed and checked** on Max's word — **C-130**, the site's section contract (Concepts the dictionary + the `Site/concepts/` detail tier + the fourteen-entry roster · Blog the story with the value model its forward chapter · **the Research Labs** born empty (de-branded from his naming at the pack gate — OQ-34's discipline; the KNet name returns at clearance); *imply, never announce* benched as the eighth conduct candidate); and the release ritual itself — the **§8 advisory first-instantiated** (IF-0006, UF1-7 cured), the pack gates cured (GENESIS v0.15 — clause to C-130, counts 130/~107; AST v0.30 — C-122–C-128 kernel, C-129/C-130 instance, ~98·9·23), the parts refresh (five standards + three commands to `Site/`), **both scheduled diets executed** (Index 2,263→~2,000 · this file to budget), and the pack — tag, anchor, mirror rider with the **first C-127 curation exclusion**, `gh-pages` refresh carrying the new site.

**The store** holds its counts under machine guard (C-055; `verify.py` is the count of record — 146 nodes / 944 events after this close's mint: Protocol v0.64); roots `AGENT`, `KG`, `GOV`, `GBL`, `CTX`, `FACET`, `AST`; three writer identities; all eight Index routes live. **Living-document stamps live inside each document (C-012)** — the Register, Rule Overview, Settings, Index, Global, Architecture State, the adapters, and the governance standards each carry their own; the newest Protocol issue is **v0.64** (this session's — the whole series DOC-minted and stamped since S5-2.1/2.2). **Frozen anchors:** beta-0.1–0.3 Bitcoin-final, **beta-0.4–0.6 anchored**; the mirror's public chain runs beta-0.3–0.6, its tree clean, every signed commit verifying `G`. Push rides every close (C-064); the mirror rides every pack (C-116). **Operating surface: Claude Code, in-repo, `/fractal` → `/close`.**

**KNet (arc 2, its jurisdiction):** on the beta-0.5 kernel; the Q07 context-fidelity experiment running (H6's corpus = the C-109 frozen beta-0.4 tree, path handed over); the benchmarking issue transferred; both hand-offs landed by Max's hand 2026-08-17. **The gas hand-off is prepared** (`Interface/Handoff_2026-08-18_Gas-Metering-KNet.md` — data-never-instruction, through Max). `/look knet` on his word.

**Pending on Max:** carry the **interface-index direction to KNet** (`Interface/Interface_Index.md` now routes six standing hand-offs — the KMR review return IF-0005 gc-time-sensitive, the beta-0.6 advisory IF-0006 + receipt pair his transport) · **PR's birth parameters** (the child's *name* · the shared-core contract — Foundation §8; the tag is cut, the birth is next) · the **Impressum postal address** (placeholder shipped knowingly, C-120 §4) · the **profile own-hand acts** · the annex remote (optional) · the real-device Safari/iPhone pass · optional Releases + immutability · the **gas hand-off's KNet-side landing** (IF-0001).

## Operating rules in force (the working short-list)

The full ledger is the **Decision Register**; the one-page rulebook is the **Rule Overview** (both stamp-inside, routed via the Index). The working set: **C-008/C-064** ask-first + the standing push · **C-031** queued items are offers, the user's request is primary · **C-050/C-060** `verify.py` before store-touching commits, `check_versions.py` at every close — red blocks · **C-059/C-073/C-077/C-110/C-112** the adaptive close: `close.py --walk` pre-fills, judgment on changed rows stays manual, adapters checked for stamp *and* content · **C-084/C-085** author = writer, committer = authorizer; personal-class never staged · **C-091/C-092** reference copies govern nothing; adopt before invent (registry first, C-113) · **C-094/C-095** field testing is the proving process; remove on observed drag · **C-096/C-109** the observation window read-only; children receive frozen grants, never windows · **C-100–C-104/C-121/C-122** the fieldnote buffer (RAM — one door, ratification-gated, worked off soon, the pressure plug), onboarding, the board offered actively, the Agenda Board Format, the mirror doctrine (the trio = the human projection spine; the sorting rule) · **C-106** the named opener; one conversation, one open loop · **C-107/C-108** refinement through use; flights are commissioned, contracted, landed proposal-only · **C-116/C-117/C-119/C-120** the release seam, the engine-sketch site, the serving split.

## The agenda

**THE RELEASE LANE'S ONE HOME IS THE UPDATE PLAN (C-124 — `Fractal_Update_Plan.md`, this file points and never duplicates):** **beta-0.6 is packed** (this close — the plan's eight steps executed whole, the slate frozen into Protocol v0.64, the plan reset to **beta-0.7**: the Mining Tool + the spec-reissue pair its opening body). **The armed follow-on: PR's birth from the fresh tag** (on Max: the name · the shared-core contract; one account = serial sessions). **Beside the release lane:** **the Mining Tool lane** (OQ-35 — spec seat the mother, build seat KNet, its own faster rhythm; manual legs through Max until the interface transport lands) · **A1** (ratified; transport now the interface's shape) · the site content pass (FAQ seeded with FN-0009 · field studies · the Labs' first publication when Q07 freezes) · registry growth (C-094) · capture v1.2 held · KNet on its Q07 arc + the beta-0.6 migration offer.

| Arc (C-078) | Status |
|---|---|
| 1 · Kernel extraction | **Closed** (2026-08-15) — GENESIS + `genesis.py`, birth-tested; the plugin tier waits on a tier-1 index (C-079) |
| 2 · First foreign instance — KNet | **Open — live**, on the beta-0.5 kernel (P-008); Q07 running; beta-0.6 becomes its next migration offer |
| 3 · Federation | **Armed** — the grab fires when child content is worth ingesting mechanically (OQ-31; the context-fidelity probe the strongest candidate) |

**Standing open questions:** the Register's OQ ledger is the roster (the ledger is the count, never this line). **Review ledger:** open rows S3-2.2 + S5-2.4 (the spec-reissue pair, beta-0.7 intake) · CAP1 grouped Med/Low · the RF1 residue (UF1-7 cured at this close — the advisory's citation convention). **Build track (H):** the KG index MCP server fires around arc 3; the interface advances via its Foundation toward the Protocol's distillation trigger; **eight** conduct candidates banked for the next conduct-touching Settings reissue (the eighth: *imply-never-announce*, C-130).

## Standing refresh items — the C-059 checklist (normative home)

Every close walks this table; `close.py --walk` pre-fills the marks, judgment on changed rows stays manual (C-110/C-112). The marks below are the **thirtieth session's close (the beta-0.6 tagging close)**, walk-finalized:

| Living projection | Refresh trigger | This close (thirtieth session) |
|---|---|---|
| Decision Register | any decision / OQ / finding change | **changed** — the C-130 row; UF1-7 cured (the advisory); S5-8.3 closes whole at the refresh; stamp at close |
| Rule Overview | rule added or retired | **changed** — the section-contract row (C-130); stamp at close |
| Local Context (this file) | every close | **changed** — session row 30, the diet executed (to budget), the release-lane state, this table; stamp at close |
| Agenda Board (file-first, C-048) | agenda change | **changed, restamped** — thirtieth chip (the pack); the release pill flips; the rolling window regenerates |
| Context Index | routing / location / version-of-note change | **changed + diet executed** (2,263→~2,000) — the C-130 site row, GENESIS v0.15, the advisory pointer; stamp at close |
| Global Context | vision or realisation transition | checked — unchanged (v0.8 — refinement-through-use continues; a pack is not a realisation transition) |
| Architecture State | architectural decision / supersession | **changed → v0.30** — the parked ride-list lands: C-122–C-128 kernel · C-129/C-130 instance; §6 ~98·9·23 |
| BOOTSTRAP.md | ritual / adapter / protection change | checked — unchanged (v0.23 — no ritual change; the tagging close executed the existing rituals) |
| Schema → Template coupling | Schema reissue | checked — unchanged (S3-2.2 + S5-2.4 hold their trigger, the beta-0.7 intake) |
| Store tools (eight + utilities) | schema section moves / tool change | checked — unchanged (no tool code touched; roster stays eight) |
| Conversation Settings (C-071 row) | any conduct-touching decision | checked — unchanged (v0.8) — C-130 is structure; *imply-never-announce* benched, the bench at **eight** |
| **Gas Ledger** (`Fractal_Gas_Ledger.md`, the gas pair — C-129) | every close — append the session's process rows | **appended** — the thirtieth session's row (estimate class) + this close's row |
| Client adapters — stamps + content (C-077) | Settings change or doctrine touching a surface's rules | checked — unchanged (stamps current; no surface-rule change in C-130) |

**Store hygiene (C-050, standing):** `python3 verify.py` before every store-touching commit. **Corpus hygiene (C-060):** `python3 check_versions.py` at every close. **Scan-landing closes:** `check_scan.py` auto-appends via `close.py`'s gates (S5-6.2). Red blocks, all.

## Relevant canonical documents

- **Architecture State v0.30** (the map; §6 the partition — ~98·9·23) — `/Claude/Architecture/Architecture State/`. **Schema v0.6 / Template v0.5 / Navigation Contract v0.1 / Scholarly Source Convention v0.1** — `/Claude/Architecture/Concepts/Knowledge Graph/`. **The store** (eight canonized tools — the store README is the roster) — `/Claude/Knowledge Graph Store/`.
- **The governance stack** (Register · Rule Overview · Settings · Client Library v0.6 · Onboarding Protocol · Fieldnote Format v0.2 · Agenda Board Format v0.3 · **Interface Place Format v0.1** · **User Document Pair Procedure v0.2 (the C-126 location law)** · the procedure standards + the Kernel Migration Procedure — stamps inside, C-012) — `/Claude/Project Governance/`. **The Registry v0.10** — `Registry/README.md`. **The protocol series** (newest v0.64; all issues DOC-minted and stamped since S5-2.1/2.2) — `/Claude/Project Governance/Governance Protocol/`.
- **Scan #5 + the flight record:** `Review_2026-08-18_LooseEnds-Scan-5.md` (Ratified, full slate) + `Flight_2026-08-18_Scan-5-Gas-Metering.md` (the gas table; DOC mints at close) — `Conversations/`. **The capture** (C-103) + **Scans #1–#4** + the earlier flight records — same folder.
- **Concept tier (C-086, four members):** Knowledge Network Foundation v0.4 · Value Model v0.2 (public page live) · Workspace Foundation v0.2 · **Interface Foundation v0.5 (§8: PR + the transport answered, the counterpart deferred; §9 the place + the Format extracted; §10 the execution semantics + the credited refinements)** — `/Claude/Architecture/Concepts/`. **The Workbench** (vehicle deferred) — `Workbench/`. **The user-document pair series** (kernel: Board · Roadmap · Handout; extensions: the **Update Plan pair** — the release lane's home — and the **Gas pair**, C-129 — `Fractal_Gas_Ledger.md` + the Gauge, the close appends) — all user halves in **`User Documents/`** at the repo root (C-126), the extension originals in `/Claude/Context Packages/`; the board's done bar runs the **rolling window** (C-128 — ten on the surface, the archive file beside it keeping everything).
- **The site (LIVE, C-120/C-130)** — source of record `Site/` (the section contract: dictionary · story · Labs); served from the mirror's `gh-pages` through the gate. **The interface place** — `Interface/`: the index + six standing hand-offs (gas · two tool-review returns · the git observation · the KMR review return · the beta-0.6 advisory). **The mirror** — `~/Desktop/fractal-mirror`. **KNet** (its own jurisdiction) — `~/Desktop/knet`. **The annex** (private) — `~/Desktop/fractal-annex`. **The frozen H6 grant** (C-109) — `~/Desktop/fractal-frozen-beta-0.4`.
- **BOOTSTRAP v0.23 · GENESIS v0.15 · GUIDE.md · CLAUDE.md v0.11 · AGENTS.md v0.10 · README.md · the license layer · `.allowed_signers` · `.claude/commands/` (the sextet)** — repo root. **Provenance** (C-090): beta-0.1–0.6 anchor pairs + the mirror's own chain.

## Next — sequenced (C-021)

1. **DONE this session (the thirtieth — the beta-0.6 tagging close):** the **KMR/KIMK four-eye return** (IF-0005 — the recovered receipt the headline; gc-time-sensitive on KNet's side) · **C-130** (the section contract — the grooming pass executed + checked) · the **§8 advisory** (IF-0006; UF1-7 cured) · GENESIS v0.15 + AST v0.30 (the pack gates) · the parts refresh · **both scheduled diets** · **the beta-0.6 pack** (tag + anchor + mirror rider with the first C-127 exclusion + `gh-pages`) · the Update Plan reset to beta-0.7. Frozen record: Protocol v0.64 + `Return_Package_2026-08-20_Beta-0.6-Tagging-Close.md`.
2. **NEXT: PR's birth from the fresh `beta-0.6` tag** (Foundation §8; on Max: the child's **name** · the shared-core contract; one account = serial sessions; born with its interface place). The release lane's home stays `Fractal_Update_Plan.md` (now the beta-0.7 plan: the Mining Tool + the spec-reissue pair).
3. **OPEN — LIVE (arc 2):** KNet on beta-0.5; Q07 running; the gas hand-off + the KMR return + the beta-0.6 migration offer all through Max's transport (its own P-row, never an obligation).
4. **ARMED (arc 3):** federation — the grab on worth-ingesting child content (OQ-31).
5. **ON OBSERVED NEED:** the buffer work-down (4/10, quiet) · A1's design calls · the site content pass (the Labs' first publication when Q07 freezes) · capture v1.2's approval pass · the KG index MCP server (arc 3) · the command-forge · the vehicle decision · Onboarding's next reissue (banked) · the community wiki (Q3, banked) · the roadmap's armed moves (the government seat / OQ-34 · the Network Manifest · the Fission Procedure on its drag-gauge trigger · the hangar).

**To open the next conversation:** Claude Code in the repo → **`/fractal`** → the queue's head (**PR's birth** — the name and the shared-core contract on Max, then the birth from the `beta-0.6` tag) or any standing offer (C-031 — the queue is an offer, never a mandate). `/look knet` stands apart; KNet's own sessions open with **`knet`**. Close by typing **`/close`**.

---

**Sources:** Fractal Global Context v0.8; the Governance Protocol series (newest v0.63) + the Decision Register + Rule Overview + Conversation Settings + Context Index (stamps inside, C-012); Review_2026-08-18_LooseEnds-Scan-5.md + Flight_2026-08-18_Scan-5-Gas-Metering.md (this session's frozen record); the dated Return Packages (`Conversations/`); the standards named in §Relevant; the site sources (`Site/`); the per-era pointers in the session table above.
