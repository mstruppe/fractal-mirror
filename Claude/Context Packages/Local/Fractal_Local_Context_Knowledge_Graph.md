# Fractal Local Context — Knowledge Graph

> **DERIVED PROJECTION — dynamic working context (Local pole).** Living document: stable filename, version tracked below. This is "where we're looking right now."

**Version:** 0.64 · **Status:** Living (derived projection) · **Updated:** 2026-08-16 · **Parent:** Fractal Global Context

**Aspect of the current realisation (opening convention):** within the *shipping-and-proving* phase (Global §2, v0.4), this addresses **proving the shipped foundation beyond itself** — the instantiation program (C-078) with arc 2 live (KNet, the first foreign instance, on its second client), **the flip program now built and awaiting its ship acts** (C-098–C-102, per v0.45), the workspace program (C-093), and the field-test loop (fieldnotes → ingestion → next anchored release) that turns real use into the next version. The KG/governance stack itself is complete and under machine guard; this context tracks its **life in use**.

## Session record (compacted — history lives frozen; this table only points)

Every session's full narrative lives in its Governance Protocol (reasoning of record) and its Return Package (handover, `Context Packages/Conversations/`, dated filenames). One line each:

| # | Session | Of record |
|---|---|---|
| 1st–3rd | 2026-08 · the consolidation era — store, checkers, routes, navigation, custody | the early Protocol issues (the series before the Scan #3 close) |
| 4th | Scan #3 under full machine guard; first riding drill | per v0.29 — C-077; S3 slate; OQ-13 → A |
| 5th | the instantiation program; arc 1 opens; partition measured; GENESIS issued | per v0.30 — C-078/C-079/C-080 |
| 6th | arc 1 complete — genesis.py gated, birth test ×2 (+ postscript: commit doctrine) | per v0.31 and its postscript issue — C-081–C-085 |
| 7th | the vision whiteboard; public gate (README + Site); concept tier born (+ postscript) | per v0.33 and its postscript issue — C-086; the KNet Foundation's first issue |
| 8th | the punch-list close; `doctor.py` landed — six store tools | per v0.35 — C-087/C-088/C-089 |
| 9th | the legal layer; first realisation transition (Global v0.4); Scan #4; **beta-0.1 packed** | per v0.36 — C-090 |
| 10th | the first shipping run — **KNet born + accepted**; arc 2 opens; fieldnotes (28 entries) | per v0.37 — no new number (C-078 executed) |
| 11th | the phase-5 ingestion; `/welcome`; the genesis document's fifth issue; **beta-0.2 packed** | per v0.38 — C-091/C-092 |
| 12th | the workspace session — WS forge + the Workbench; KNet Foundation v0.2 | per v0.39 — C-093 |
| 13th | the second-client trial — KNet boards the ChatGPT desktop (fieldnotes 30–34); the Local Context compacted; postscripts: the observation window opened (`/look` the fourth command, `/fieldnote` the fifth), the Command Index issued, **the client library issued (C-097)**, move 2 declared the mother's near work; at the tail: **the first `/fieldnote` wave** (entries 37–41 — the preface → the paper passport, the outsourcing pivot, the friends-beta pipeline, **Zotero adopted in the child: P-003**, the USP felt) and **the determinism ruling** (`Site/fieldnote.py` — no target, no capture); the library's first field refresh (entry 42 — the dissolved mode seam); the OQ-32 profile mechanism designed; **THE FLIP CALLED** — a friend with a real need, the shipment-design bundle becomes the queue's head | per v0.40 — C-094/C-095; postscripts per v0.41–v0.44 — C-096/C-097, the C-094 ruling, the flip call |
| 14th | **the flip preparation — the v0.44 bundle built and gated in one cycle:** triage answered (the friend shipment rides the public flip); C-098 the flip shape (raw repo, phased mirror, **the annex** — notebook moved, C-085 re-weighed); C-099 the license layer (Apache-2.0 + CC BY 4.0 + NOTICE, terms travel with newborns); C-100 the testing pipeline (**Fieldnote Format v0.1**, `fieldnote.py` the **seventh store tool** with the intake half, the kernel `/fieldnote` door + `FIELDNOTES.md` in every newborn); C-101 the onboarding protocol (the interview, the self-retiring rail, the fade, **`/begin`** the sixth command); C-102 the board offered actively (the speed asymmetry); GENESIS **v0.6**; **GUIDE.md** at the root; entries 43–45 + blocks FN-0001–0003 (the graduated tool's first captures — the wrong-jurisdiction audit, the competitor-concedes-the-need screenshot, the elided-suggestion friction); beta-0.2 upgraded **Bitcoin-final**; **beta-0.3 packed at this close** | per v0.45 — C-098–C-102 |

*Compacted 2026-08-16, thirteenth session (C-095's first exercise): the v0.40–v0.54 per-session narrative paragraphs — roughly ten thousand words — are replaced by this table. Every word of them remains verbatim in the named Protocols and the dated Return Packages; nothing was destroyed, duplication was removed.*

## Current state

**The flip is prepared: the whole v0.44 bundle is built, gated, and shipped into the kernel; what remains are the ship acts — the mirror, the friend, the raw flip at Max's moment.** The store holds **101 nodes / 601 events** in one log (C-055; post-ripple totals, per S4-1.2); roots `AGENT`, `KG`, `GOV`, `GBL`, `CTX`, `FACET`, `AST`; three writer identities; all eight Index routes live; every spec version carries its own DOC (C-061). **Protocol series at v0.45** · **Register v0.48** (eight OQs standing; one open finding across four Scans — S3-2.2, deliberately held) · **Rule Overview v0.36** · **Settings v0.8** · **Index v0.33** · **Architecture State v0.21** (§6 partition: kernel ~78 · parameterized 9 · instance 16) · **Client Library v0.2** (Codex entry harvest-pending on KNet's adapter; entry 43's read-side jurisdiction clause banked for its next reissue) · **Schema v0.6 / Template v0.5 / Navigation Contract v0.1** · **BOOTSTRAP v0.21** (§2: the command sextet; §4 the C-098 posture) · **GENESIS v0.6** (the fieldnote door + the guided path) · **Onboarding Protocol v0.1** + **Fieldnote Format v0.1** (the two new canonicals) · **GUIDE.md** (the user guide, derived) · **CLAUDE.md v0.9 / AGENTS.md v0.9** (C-077 content currency: the annex rule; the seven-tool enumeration) · **the Command Index** (stamp inside — the sextet + the two-command child tier). **The license layer stands (C-099):** LICENSE (Apache-2.0) + LICENSE-docs (CC BY 4.0) + NOTICE at the root; the name not licensed; terms travel with every birth. **The annex stands (C-098):** `~/Desktop/fractal-annex` (private, ungoverned, never shipped) holds the notebook; personal-class material routes there by default; remote binding at Max's option. **Concept tier (C-086), two members:** Workspace Foundation v0.2 · Knowledge Network Foundation v0.2. **The Workbench** (`Workbench/`, working tool; excluded from release archives by the curation layer) — vehicle decision still deferred, two data in hand. **KNet, for the record (not governed from here — observed through the window, C-096):** P-003 committed and pushed (the entry-40 durability worry cured); its own `AGENTS.md` **still unwritten** — the C-097 harvest waits on it (entry 43 records the day's wrong-jurisdiction audit specimen); until ratification an OpenAI-arriving friend boards via the advisory pattern. **Custody:** every signed commit verifies `G`; **beta-0.1 and beta-0.2 both Bitcoin-final**; **beta-0.3 tagged, signed, anchored at this close** (C-090's third execution — the friend ships from it). Off-site copy live; push rides every close (C-064); no `drone/*` branches; drills ride the Scans. **Operating surface: Claude Code, in-repo, `/fractal` → `/close`.** **Pending on Max: the ship acts** — the private mirror repo + friend invite (his account), the annex remote (optional), KNet's adapter ratification (child-side), and the raw flip + branch protection at his moment.

## Operating rules in force (the working short-list)

The full ledger is the **Decision Register**; the one-page rulebook is the **Rule Overview** (both routed via the Index). What a working session actually exercises:

- **C-008 + C-064** — ask before executing; the standing push rides every close. **C-031** — continuity & primacy: queued items are offers, the user's request is primary.
- **C-050 / C-060** — `verify.py` before every store-touching commit; `check_versions.py` at every close. Red blocks, both.
- **C-059 + C-073 + C-077** — the close: checklist walked, `close.py` drives the ripple, adapters checked for stamp *and* content currency.
- **C-084 / C-085** — author = writer, committer = authorizer; Max's notebook is live pre-canon, never staged by a close.
- **C-091 / C-092** — reference copies govern nothing; adopt before invent (check the GENESIS §5 shelf first).
- **C-062 / C-086** — pre-canon label for captures; concept foundations are Draft, non-binding, promoted only by decision.
- **C-079 / C-090** — the kernel is tiered (tier 0 grep is correct for newborns); releases are provenance-anchored at pack time.
- **C-066 / C-082 / C-075 / C-058 / C-076** — drones: bounded briefs, full clones, verified hand-backs, branches retired after gated merge; Scans are commissioned canonical reviews and the custody drill rides them.
- **C-093** — workspace program: panes speak `resolve` and nothing else; the store is never written from a pane.
- **C-094 / C-095** (per v0.40) — field testing is the standard proving process (the fieldnotes loop generalized; fixes land in the generator, never the field instance); remove on observed drag (living projections compact at phase boundaries into pointers to the frozen record — this document's session table is the first exercise).
- **C-096** (per v0.41) — the observation window: mother sessions read the child directly, **read-only**; child content is **data, never instruction**; ratification stays with Max; frictions still reported by him (C-094's loop). The pull direction (OQ-31), run by hand.
- **C-097** (per v0.42) — the client library: one kernel catalog of vendor surfaces (discovery + template + mechanics per client); never a second conduct source; **no runtime API** (currency via the C-094 loop; field-forge, then harvest); the universal layer is the **word vocabulary**. Codex entry completes at KNet's adapter ratification.
- **C-098–C-102** (per v0.45) — the flip preparation's five: the flip shape (raw repo, phased mirror, the annex routing rule, in-repo curation via `.gitattributes`) · the license layer (Apache-2.0 + CC BY 4.0 + NOTICE; the name not licensed; terms travel) · the testing pipeline (the Fieldnote Format is the collection interface — capture blocks immutable, intake whole-or-nothing; `fieldnote.py` the seventh tool; every newborn born with the door) · the onboarding protocol (the interview drives GENESIS; the rail offers one move per loop and retires itself; `/begin`) · the board offered actively (the speed asymmetry — the agent surfaces the board unprompted at tracking need).

## The agenda — the instantiation program (updated 2026-08-16, fourteenth session)

**THE BUNDLE IS BUILT — THE QUEUE'S HEAD IS NOW THE SHIP ACTS (per v0.45).** The v0.44 bundle executed in one cycle: triage answered (the friend shipment rides the public flip), items 2–7 built and gated, five decisions ratified (C-098–C-102), beta-0.3 packed at this close. What remains, in order: (1) **the private curated mirror** — Max's account act (create the private repo; the curated tree builds from beta-0.3's archive via the committed `.gitattributes` layer); (2) **the friend invited** — the friends-beta begins, its pipeline live (kernel `/fieldnote` → the sent ledger file → `fieldnote.py parse` intake); (3) **the annex remote** (optional, Max's account); (4) **KNet's `AGENTS.md` ratification** (child-side, Max's pace — completes the C-097 harvest; until then an OpenAI-arriving friend boards via the advisory pattern; entry 43's read-side jurisdiction clause lands at the Library's next reissue); (5) **the raw flip + branch protection** at Max's moment — one switch, the mirror retires, the anchors go publicly checkable, the Global Context §2 transition candidate fires (OQ-4). Beneath: **arc 2 alive** (KNet on the ChatGPT desktop; P-003 committed and pushed); **the observation window open** (C-096 — `/look knet`; captures through `/fieldnote <target>`, now machine-format, C-100); **move 2 — the public identity** remains the mother's declared near work (the USP series now three members strong — entries 41/42/44, the third the first *outside* datum; the guide surface landed as GUIDE.md, the site and tutorials still to draft from the fieldnotes); **the grab demo** arms on child content worth ingesting (direction pull, OQ-31).

| Arc (C-078) | Status |
|---|---|
| **1 · Kernel extraction** | **Closed** (2026-08-15) — partition measured; GENESIS (now v0.6) + `genesis.py` gated; packaging unit = adapter + commands (C-083); birth test ×2. Delivered the *skill* tier; the *plugin* tier waits on a tier-1 index (C-079) |
| **2 · First foreign instance — KNet** | **Open — live.** Born from the anchored beta-0.1 tag; §6 acceptance passed same day; two clean closes; now the second-client trial's testbed. OQ-28 collecting real data; OQ-4's comparison moment = this arc's completion |
| **3 · Federation** | **Armed** — the second store exists and carries a sourced queue; the grab fires when its content is worth ingesting mechanically (OQ-31: origin layer, `import` verb, compat check) |

**Standing offers beside the queue:** the workbench's next slice (the vehicle decision — informed by the negative first-use verdict and the outsourcing pivot: possibly no vehicle at all) · move 2's remaining surfaces (site, tutorials — GUIDE.md landed; the pitch leads with the USP series, entries 41/42/44) · **the preface standard / paper passport** (in the child as P-003; enters the Knowledge Network Foundation at its next revision on Max's tabling; prime grab material for arc 3) · **the command-forge** (entry 45, vision-class — fast in-project minting of custom commands; the fieldnote pair the worked exemplar) · **the multi-agent probe program** (per v0.44 §2 — bounded probes on more engines in KNet; each green probe a Library entry) · **two banked conduct candidates** for the next Settings reissue (suggestion hygiene — complete invocations only, entry 45; the board offer as a conduct clause, C-102's Settings-side twin). **Review ledger:** across four Scans one finding stands open — **S3-2.2** (Schema + Contract lack `Reviewed By`; lands at each spec's next reissue); all other rows Fixed, dispositions in the Register (C-058). No next Scan penciled — Scans are commissioned.

**G. Standing open questions — eight, all trigger-bound (offers, C-031):** OQ-28 (the weight dial — arc 2 data flowing; **new datum: the mother's own affordability**, the C-095 trigger) · OQ-29 (code as governed artifact — the Workbench its first live case) · OQ-31 (federation — two reverse-transfer data logged) · OQ-32 (profiles & roles — KNet still single-owner) · OQ-4 (first datum recorded; comparison moment = arc 2 completion) · OQ-9 (next promotion) · OQ-20 (heavier review variant) · OQ-27 (outside-custody scope).

**H. Build track (absorbed by the program):** the KG index MCP server — spec complete (Navigation Contract, C-070); trigger sharpened (C-068): inverted-view reads becoming routine — genuinely fires around arc 3. · The interface: forged and walking (C-093); next slices via the Foundation by ordinary decision. · Shipping tier: skill tier delivered (C-083); the plugin tier waits on the tier-1 index, making H's first bullet its own unblocker.

## Standing refresh items — the C-059 checklist (normative home)

Every close walks this table; each row marked **changed** or **checked — unchanged**. Since C-060, `check_versions.py` verifies the walk mechanically. Marks below are for the fourteenth session (the flip-preparation close, 2026-08-16, per v0.45):

| Living projection | Refresh trigger | This close (fourteenth session) |
|---|---|---|
| Decision Register | any decision / OQ / finding change | **changed → v0.48** (C-098–C-102 entered) |
| Rule Overview | rule added or retired | **changed → v0.36** (§2: onboarding, distribution/flip, licensing rows; field-testing row gains the capture layer; board row gains the active offer) |
| Local Context (this file) | every close | **changed → v0.64** (this walk; stamp above) |
| Agenda Board (file-first, C-048) | agenda change | **changed**, regenerated (the ship-acts head; the five decisions; the pack) |
| Context Index | routing / location / version-of-note change | **changed → v0.33** (GUIDE + Onboarding Protocol + Fieldnote Format + license rows; GENESIS v0.6; seven tools; the sextet; fourteenth session appended) |
| Global Context | vision or realisation transition | checked — unchanged (v0.4) — **deliberate:** the flip is *prepared*, not *executed*; §2 moves on the shipment/flip landing (the OQ-4 pattern) |
| Architecture State | architectural decision / supersession | **changed → v0.21** (§6 partition maintenance: C-099–C-102 kernel, C-098 instance — ~78·9·16; seventh tool; the sextet) |
| BOOTSTRAP.md | ritual / adapter / protection change | **changed → v0.21** (§0 the annex paragraph; §2 the sextet + grown genesis install; §4 the C-098 posture) |
| Schema → Template coupling | Schema reissue | checked — unchanged (S3-2.2 holds its trigger) |
| Store tools (seven) | schema section moves / tool change | **changed** — `fieldnote.py` graduated in as the seventh (C-100, capture + intake); `genesis.py` grew the door, the rail, and the license copying; the other five untouched |
| Conversation Settings (C-071 row) | any conduct-touching decision | checked — unchanged (v0.8) — C-098–C-102 are governance-process/artifact rules (the C-094/C-095 precedent class); the C-071 question asked and answered *no*; the two conduct-adjacent candidates (suggestion hygiene; the board offer as conduct) deliberately banked for the next conduct-touching reissue |
| Client adapters — stamps + content (C-077) | Settings change or doctrine touching a surface's rules | **changed — both → v0.9** (content re-projections: CLAUDE.md carries the annex routing rule, C-098; AGENTS.md's tool enumeration brought current to the seven + the new root artifacts; stamps stay at Settings v0.8) |

**Store hygiene (C-050, standing):** `python3 verify.py` before every store-touching commit; red blocks. **Corpus hygiene (C-060, standing):** `python3 check_versions.py` at every close; red blocks.

## Relevant canonical documents

- **Architecture State v0.21** (the map; ONT §3, WS §4, instantiation §6) — `/Claude/Architecture/Architecture State/`.
- **Schema v0.6 / Template v0.5 / Navigation Contract v0.1** — `/Claude/Architecture/Concepts/Knowledge Graph/`. **Store** (seven tools: `verify.py` · `mint.py` · `check_versions.py` · `close.py` · `genesis.py` · `doctor.py` · `fieldnote.py` + `fieldnote_roster.json`) — `/Claude/Knowledge Graph Store/`.
- **Protocol v0.45 + Register v0.48 + Rule Overview v0.36 + Settings v0.8 + the Client Library + the Onboarding Protocol + the Fieldnote Format v0.1** (stamps inside, C-012) — `/Claude/Project Governance/`.
- **Concept tier (C-086):** Knowledge Network Foundation v0.2 — `/Claude/Architecture/Concepts/Knowledge Network/` · Workspace Foundation v0.2 — `/Claude/Architecture/Concepts/Workspace/`.
- **The Workbench** (`Workbench/` — working tool; export-ignored from release archives). **Index v0.33 + Agenda Board + the Command Index** (stamp inside — the sextet + child tier at a glance) — `/Claude/Context Packages/`.
- **Fieldnotes** (pre-canon, C-062 — 45 prose entries + machine blocks FN-0001–0003, two field runs + the flip-preparation captures; disposition table inside) + **the second-client advisory** — `Site/`. **KNet** (external, its own jurisdiction) — `~/Desktop/knet` · `github.com/mstruppe/knet`. **The annex** (private, ungoverned) — `~/Desktop/fractal-annex`.
- **BOOTSTRAP v0.21 · GENESIS v0.6 · GUIDE.md · CLAUDE.md v0.9 · AGENTS.md v0.9 · README.md · LICENSE / LICENSE-docs / NOTICE · `.gitattributes` · `.allowed_signers` · `.claude/commands/` (the sextet: `/fractal` · `/close` · `/welcome` · `/begin` · `/look` · `/fieldnote`)** — repo root. **Release provenance (C-090):** the root folder Provenance — beta-0.1 + beta-0.2 (both Bitcoin-final) + beta-0.3 (anchored at this close).
- **Loose-Ends Scans #1–#4** (canonical reviews, C-058) — `Context Packages/Conversations/`.

## Next — sequenced (C-021)

1. **DONE:** the foundation, the checkers, arc 1, the punch list, beta-0.1, the ingestion + beta-0.2, the workspace slice, **the flip preparation + beta-0.3** — see the session table; reasoning in Protocols v0.29–v0.45.
2. **THE SHIP ACTS (the queue's head, per v0.45):** the private curated mirror + friend invite (Max's account; the tree builds from beta-0.3's archive) → the friends-beta runs on the live pipeline (kernel `/fieldnote` → sent file → `parse`) → the raw flip + branch protection at Max's moment (the Global §2 transition candidate fires there).
3. **OPEN — LIVE (C-078 arc 2):** KNet on its own sessions; child-side next act: ratify its `AGENTS.md` (completes the C-097 harvest); frictions → fieldnotes → next ingestion slate.
4. **ARMED (C-078 arc 3):** federation — the grab fires when KNet's graph content is worth ingesting mechanically; pull; machinery on the ingesting side (OQ-31).
5. **ON OBSERVED NEED:** the KG index MCP server (spec complete; fires around arc 3) · the command-forge (entry 45) · the vehicle decision (two data; possibly no vehicle) · Galaxy UI.

**To open the next conversation:** Claude Code in the repo → **`/fractal`** → **the ship-acts session**: the mirror creation + friend invite are Max's account acts (the session prepares the curated tree and the exact commands); or any standing offer (C-031 — everything is an offer). `/look knet` stands apart as ever. Close by typing **`/close`**; manual close stays the fallback (Return Package, checklist, both checkers green, protocol or recorded reason).

---

**Sources:** Fractal Global Context v0.4; Governance Protocol — Claude Series v0.1–v0.45; Decision Register v0.48; Conversation Settings v0.8; Rule Overview v0.36; BOOTSTRAP v0.21; GENESIS v0.6; Fractal_Onboarding_Protocol (stamp inside, C-012); Fractal_Fieldnote_Format_v0.1; Fractal_Client_Library (stamp inside, C-012); CLAUDE.md v0.9; AGENTS.md v0.9; README.md; GUIDE.md; Knowledge Network Foundation v0.2; Workspace Foundation v0.2; Node & Event Schema v0.6; Node Template v0.5; Navigation Contract v0.1; Architecture State v0.21; Context Index v0.33; Fractal_Command_Index (stamp inside, C-012); Knowledge Graph Store (live); Site/Fieldnotes_2026-08-15_First-Shipping-Run.md (pre-canon — 45 entries + blocks FN-0001–0003); Site/Upstream_Advisory_2026-08-16_KNet-Second-Client-Trial.md; Return_Package_2026-08-16_Flip-Preparation.md; the per-session record above (Protocols v0.29–v0.45 + dated Return Packages, `Context Packages/Conversations/`).
