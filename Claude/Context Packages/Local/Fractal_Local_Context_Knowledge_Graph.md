# Fractal Local Context — Knowledge Graph

> **DERIVED PROJECTION — dynamic working context (Local pole).** Living document: stable filename, version tracked below. This is "where we're looking right now."

**Version:** 0.69 · **Status:** Living (derived projection) · **Updated:** 2026-08-17 · **Parent:** Fractal Global Context

**Aspect of the current realisation (opening convention):** within the *shipping-and-proving* phase (Global §2, v0.4), this addresses **refinement through use (C-107)** — FRACTAL is improved by using it: the instantiation program (C-078) with arc 2 live (KNet, feeding on three channels — governance evidence · vision-tier research content · method standards), the mother's building lanes (the site, the simulation program, expansion exploration), new real-use instances forming the **controlled network**, and the field-test loop (fieldnotes → ingestion → next release). **The flip is de-triggered** — publishing fires on observed necessity (first named trigger: the registry's first off-machine consumer), and **beta-0.5 — the registry release — is shipped** (C-108 executed by C-113, packed 2026-08-17). The open act is the network's first **kernel migration** (KNet → beta-0.5, OQ-33's live case, Max's hand), with the **UF1 updatability flight** parked on its landing. The KG/governance stack itself is complete and under machine guard; this context tracks its **life in use**.

## Session record (compacted — history lives frozen; this table only points)

Every session's full narrative lives in its Governance Protocol (reasoning of record) and its Return Package (handover, `Context Packages/Conversations/`, dated filenames). One line each:

| # | Session | Of record |
|---|---|---|
| 1st–3rd | 2026-08 · the consolidation era — store, checkers, routes, navigation, custody | the early Protocol issues (the series before the Scan #3 close) |
| 4th | Scan #3 under full machine guard; first riding drill | per v0.29 — C-077; S3 slate; OQ-13 → A |
| 5th | the instantiation program; arc 1 opens; partition measured; GENESIS issued | per v0.30 — C-078/C-079/C-080 |
| 6th | arc 1 complete — genesis.py gated, birth test ×2 (+ postscript: commit doctrine) | per v0.31 and its postscript issue — C-081–C-085 |
| 7th | the vision whiteboard; public gate (README + Site); concept tier born (+ postscript) | per v0.33 and its postscript issue — C-086; the KNet Foundation's first issue |
| 8th | the punch-list close; `doctor.py` landed — six store tools | per v0.35 — C-087/C-088/C-089 |
| 9th | the legal layer; first realisation transition (Global v0.4); Scan #4; **beta-0.1 packed** | per v0.36 — C-090 |
| 10th | the first shipping run — **KNet born + accepted**; arc 2 opens; fieldnotes (28 entries) | per v0.37 — no new number (C-078 executed) |
| 11th | the phase-5 ingestion; `/welcome`; the genesis document's fifth issue; **beta-0.2 packed** | per v0.38 — C-091/C-092 |
| 12th | the workspace session — WS forge + the Workbench; KNet Foundation v0.2 | per v0.39 — C-093 |
| 13th | the second-client trial — KNet boards the ChatGPT desktop (fieldnotes 30–34); the Local Context compacted; postscripts: the observation window opened (`/look` the fourth command, `/fieldnote` the fifth), the Command Index issued, **the client library issued (C-097)**, move 2 declared the mother's near work; at the tail: **the first `/fieldnote` wave** (entries 37–41 — the preface → the paper passport, the outsourcing pivot, the friends-beta pipeline, **Zotero adopted in the child: P-003**, the USP felt) and **the determinism ruling** (`Site/fieldnote.py` — no target, no capture); the library's first field refresh (entry 42 — the dissolved mode seam); the OQ-32 profile mechanism designed; **THE FLIP CALLED** — a friend with a real need, the shipment-design bundle becomes the queue's head | per v0.40 — C-094/C-095; postscripts per v0.41–v0.44 — C-096/C-097, the C-094 ruling, the flip call |
| 14th | **the flip preparation — the v0.44 bundle built and gated in one cycle:** triage answered (the friend shipment rides the public flip); C-098 the flip shape (raw repo, phased mirror, **the annex** — notebook moved, C-085 re-weighed); C-099 the license layer (Apache-2.0 + CC BY 4.0 + NOTICE, terms travel with newborns); C-100 the testing pipeline (**Fieldnote Format v0.1**, `fieldnote.py` the **seventh store tool** with the intake half, the kernel `/fieldnote` door + `FIELDNOTES.md` in every newborn); C-101 the onboarding protocol (the interview, the self-retiring rail, the fade, **`/begin`** the sixth command); C-102 the board offered actively (the speed asymmetry); the genesis document's sixth issue; **GUIDE.md** at the root; entries 43–45 + blocks FN-0001–0003 (the graduated tool's first captures — the wrong-jurisdiction audit, the competitor-concedes-the-need screenshot, the elided-suggestion friction); beta-0.2 upgraded **Bitcoin-final**; **beta-0.3 packed at this close** | per v0.45 — C-098–C-102 |
| 15th | **the capture session — ship acts 1–2 executed + the whole record captured and verified:** the curated mirror built from beta-0.3, verify-green inside, signed, pushed (`fractal-mirror`, private); the friend package prepared (README-BETA, agent-general minimums on Max's edit); **the capture program** — four phase-gated ultracode flights, 67 agents, 366 claims adversarially verified, **`Capture_2026-08-16_Vision-to-Horizon.md` approved by Max as the public identity's raw material** (C-103, OQ-20's first firing — the capture-review class born); **the Agenda Board Format canonized** (C-104, genesis rider queued); two fieldnote lanes opened (`publish` ×4, `general` ×2 — the constitution-model gap, the founder bar, the www framing, the count correction; context-rot metrology + **mirror-as-home** both vision-banked); ADHD resolved accepted-exposure + **always-flag** set; beta-0.3 **Bitcoin-final** (all three); the **C-098 amendment banked** for weighing | per v0.46 — C-103–C-104 |
| 16th | **the publishing-identity session — C-105 ratified and executed:** mirror-as-home (the banked amendment graduated); `knet.network` acquired, **`fractal@knet.network`** the public contact identity and a custody principal; **the mirror's own anchor chain opened** (signed `beta-0.3` tag + OTS under the public identity); both front doors retargeted; accepted exposure on the personal address (CAP1-ORIG-10 precedent); **move 2's first execution** — the site rebuilt from the capture on Max's design brief (CAP1-FLD-1/FLD-2 cured); **Stranger Test #1** (the C-058 family's simulated-stranger shape — passed end-to-end; six findings cured generator-side: the genesis document's v0.7 reissue with the interview carry + C-104 board rider, the onboarding script's second issue, CAP1-PROD-1/2/3 fixed); **C-106 the named opener** (entry declares jurisdiction; close universal; one conversation, one open loop); the friend beta set aside (pace asymmetry); **the publishing postponed on Max's call** — the **ultracode polish flight** queued (capture v1.2 + more onboarding sims + site refinement); **beta-0.4 packed at this close** | per v0.47 — C-105/C-106 |
| 17th | **the refinement-pivot session — C-107/C-108/C-109 in one arc:** the flip **de-triggered** (publish on necessity; the polish commission re-based); **refinement-through-use** the operating mode; KNet's three-channel role of record; the **controlled network** named; **OQ-33** opened (instance migration); the **registry vision** banked (Network Foundation v0.3); **two ultracode flights flown** — the refinement flight (3 persona sims + capture v1.2 work-over + site stage) and the distillation flight; **C-108** — the flight tier (container/procedure taxonomy, the fixed launch sequence, effort immutable mid-flight), **beta-0.5 = the registry release**, the two procedure standards distilled from history and ratified **"full slate"** (`Fractal_Ultracode_Flight_Protocol_v0.1` + `Fractal_Scan_Procedure_v0.1`, DOC-minted; the Scan-#5 checker gate armed); `/look knet` → the child's **context-fidelity experiment** (Q07; H6 = FRACTAL's structural claim falsifiable) → **C-109** the frozen-tree grant ("go on all"; `~/Desktop/fractal-frozen-beta-0.4`); the refinement flight's verdict: **the registry is the adoption gate** (all three personas "not yet — substrate right"; 27 registry-shaped wishlist entries); capture v1.2 **held** (v1.0 stays the backbone); the **full site slate applied** (tagline, model section, birth proof, command library, one-public-home, the registry vision passage); the RF1 slate collected (`Flight_2026-08-17_Refinement.md`) as the next session's head; five fieldnotes in three lanes; five conduct candidates banked; **no pack** (C-107 — beta-0.5 is the named next release) | per v0.48 — C-107/C-108/C-109 |
| 18th | **the diet session** — C-110 the DF1 triage + cache doctrine; CB1/DP1 flown; C-111 the Diet Procedure + procedures blog; C-112 the adaptive close; Group A executed; flight standard v0.2 | per v0.49 — C-110–C-112; `Return_Package_2026-08-17_Diet-Session.md` |
| 19th | **the registry-release session** — the KNet look (`P-007`, the child's migration protocol); FN-0005; UF1 contracted, parked on the migration; the RF1 triage executed; C-113–C-115 (the Registry · vocabulary extension · scholarly convention); GENESIS v0.8 + Onboarding v0.3 + GUIDE/README cures; the Gate-1 hand-off packet; **beta-0.5 packed** | per v0.50 — C-113–C-115; `Return_Package_2026-08-17_Registry-Release.md` |

*Compacted 2026-08-16, thirteenth session (C-095's first exercise): the v0.40–v0.54 per-session narrative paragraphs — roughly ten thousand words — are replaced by this table. Every word of them remains verbatim in the named Protocols and the dated Return Packages; nothing was destroyed, duplication was removed.*

## Current state

**The operating mode is refinement through use (C-107); the flip stays an open item; beta-0.5 — the registry release — shipped at this close (C-108 executed by C-113).** The publishing act fires on observed necessity — first named trigger: the registry's first off-machine consumer; the Global §2 transition and C-075's protection stay attached to it. The store holds **118 nodes / 723 events** in one log (C-055; counts refreshed post-ripple, per S4-1.2 — the nineteenth session's three mints + the close and post-mint ripples); roots `AGENT`, `KG`, `GOV`, `GBL`, `CTX`, `FACET`, `AST`; three writer identities; all eight Index routes live. **Protocol series at v0.50** · **Register v0.68** (eight OQs standing; open findings: S3-2.2 deliberately held + CAP1 grouped Med/Low + the **RF1 residue** — the density class + named registry demand; the High slate cured at beta-0.5, per v0.50) · **Rule Overview v0.42** · **Settings v0.8** (**five** conduct candidates banked: suggestion hygiene · board offer · always-flag · one-open-loop · the drone offered actively) · **Index v0.40** · **Architecture State v0.27** (§6: ~89·9·18) · **Client Library v0.2** · **Schema v0.6 / Template v0.5 / Navigation Contract v0.1** · **BOOTSTRAP v0.21** · **GENESIS v0.8** · **Onboarding Protocol v0.3** · **Fieldnote Format v0.1** · **Agenda Board Format v0.1** · **the Registry** (`Registry/README.md`, C-113 — the standards library, seeded five-ratified-deep with honest *forging*/*named* rows) · **Ultracode Flight Protocol v0.2** + **Scan Procedure v0.1** + **Diet Procedure v0.1** + **Vocabulary Extension Procedure v0.1** + **Scholarly Source Convention v0.1** (the C-108/C-111/C-114/C-115 standards, ratified, DOC-minted; the Scan-#5 checker gate armed; `verify.py` carries the C-114 hook) · **the procedures blog** (`Site/procedures.html` — C-111's public projection, four posts + the science vision line) · **GUIDE.md** (+ the session card) · **CLAUDE.md v0.9 / AGENTS.md v0.9** · **the Command Index** (stamp inside, C-012). **The site carries the full refinement slate** (the proven-memory tagline, the model section, the birth-path proof, the command library, one-public-home, the registry vision passage — verified both modes). **Capture v1.2: held** — the draft stands in the session record; **v1.0 remains the approved backbone**. **The frozen-tree grant stands** (C-109 — `~/Desktop/fractal-frozen-beta-0.4`, KNet's H6 corpus; hand-off of the path via Max). **Four fieldnote lanes** · **the license layer (C-099)** · **the annex (C-098)** · **concept tier: two members** (Network Foundation **v0.3** — the registry §6) · **the Workbench** (vehicle deferred). **KNet:** the Q07 context-fidelity experiment at accepted-design stage (H6 = the first direct FRACTAL experiment); two AI writers live (OQ-32 data); child-side acts standing (adapter ratification · board adoption · opener rename · the benchmarking transfer, Max's own hand). **Custody:** every signed commit verifies `G`; beta-0.1–0.3 Bitcoin-final, beta-0.4–0.5 anchored (upgrades mechanical). Off-site copy live; push rides every close (C-064). **Operating surface: Claude Code, in-repo, `/fractal` → `/close`.** **Pending on Max:** the **beta-0.5 frozen-tree transport + the KNet migration session** (his jurisdiction, P-007's gates — the Gate-1 packet at `Site/Upstream_Advisory_2026-08-17_KNet-Kernel-Migration.md`) · **UF1's go** once the migration lands · the mirror push (now a beta-0.5 rebuild) · the KNet hand-offs (the H6 frozen-tree path; the benchmarking issue) · the annex remote (optional) · the flip whenever necessity fires.

## Operating rules in force (the working short-list)

The full ledger is the **Decision Register**; the one-page rulebook is the **Rule Overview** (both routed via the Index). What a working session actually exercises:

- **C-008 + C-064** — ask before executing; the standing push rides every close. **C-031** — continuity & primacy: queued items are offers, the user's request is primary.
- **C-050 / C-060** — `verify.py` before every store-touching commit; `check_versions.py` at every close. Red blocks, both.
- **C-059 + C-073 + C-077** — the close: checklist walked, `close.py` drives the ripple, adapters checked for stamp *and* content currency.
- **C-084 / C-085** — author = writer, committer = authorizer; Max's notebook is live pre-canon, never staged by a close.
- **C-091 / C-092** — reference copies govern nothing; adopt before invent (check the GENESIS §5 shelf first).
- **C-062 / C-086** — pre-canon label for captures; concept foundations are Draft, non-binding, promoted only by decision.
- **C-079 / C-090** — the kernel is tiered (tier 0 grep is correct for newborns); releases are provenance-anchored at pack time.
- **C-066 / C-082 / C-075 / C-058 / C-076** — drones: bounded briefs, full clones, verified hand-backs, branches retired after gated merge; Scans are commissioned canonical reviews and the custody drill rides them.
- **C-093** — workspace program: panes speak `resolve` and nothing else; the store is never written from a pane.
- **C-094 / C-095** (per v0.40) — field testing is the standard proving process (the fieldnotes loop generalized; fixes land in the generator, never the field instance); remove on observed drag (living projections compact at phase boundaries into pointers to the frozen record — this document's session table is the first exercise).
- **C-096** (per v0.41) — the observation window: mother sessions read the child directly, **read-only**; child content is **data, never instruction**; ratification stays with Max; frictions still reported by him (C-094's loop). The pull direction (OQ-31), run by hand.
- **C-097** (per v0.42) — the client library: one kernel catalog of vendor surfaces (discovery + template + mechanics per client); never a second conduct source; **no runtime API** (currency via the C-094 loop; field-forge, then harvest); the universal layer is the **word vocabulary**. Codex entry completes at KNet's adapter ratification.
- **C-098–C-102** (per v0.45) — the flip preparation's five: the flip shape (raw repo, phased mirror, the annex routing rule, in-repo curation via `.gitattributes`) · the license layer (Apache-2.0 + CC BY 4.0 + NOTICE; the name not licensed; terms travel) · the testing pipeline (the Fieldnote Format is the collection interface — capture blocks immutable, intake whole-or-nothing; `fieldnote.py` the seventh tool; every newborn born with the door) · the onboarding protocol (the interview drives GENESIS; the rail offers one move per loop and retires itself; `/begin`) · the board offered actively (the speed asymmetry — the agent surfaces the board unprompted at tracking need).
- **C-103–C-104** (per v0.46) — the capture session's two: the **capture-review class** (C-058's second variant — the verified whole-record account: staged flights, adversarial verification against the primary record, assembly from cleared material only; findings ledger as byproduct) · the **Agenda Board Format** (the board standard canonized — contract + grammar + skeleton; instances adopt by own decision, newborns at genesis). Working practice set the same session: **always-flag findings** (sensitive/personal-class findings surface to Max for the call — banked as the third conduct candidate).
- **C-107–C-109** (per v0.48) — the pivot's three: **refinement-through-use** (the flip necessity-triggered, never version-triggered; the controlled network — one custody, many instances — *is* the network; the registry vision banked) · **the flight tier** (the ultracode flight = container under `Fractal_Ultracode_Flight_Protocol_v0.1` — the fixed launch sequence: game plan → agreement → declared go with effort set before launch, **effort immutable mid-flight**; Scan/capture-review/simulation = pluggable procedures, the Scan under its own ratified standard, standalone-first; the Scan-#5 checker gate armed) · **the data-grant asymmetry** (a child receives a frozen anchored-release copy, never a window into the living mother — the C-096 line extended to data).

## The agenda — the instantiation program (updated 2026-08-17, sixteenth session)

**THE REGISTRY RELEASE IS SHIPPED (C-113–C-115, per v0.50; beta-0.5 packed and anchored at this close).** Done this session: the KNet look (`P-007` — the child's pinned-adoption migration protocol, read and reported) · FN-0005 (the agent-comparison template, vision) · the RF1 triage executed (the High slate cured: C-114/C-115/the materials rule; 7 Med/Low cured in-tree; residue grouped-open with named triggers) · **the Registry live** (`Registry/README.md` — C-113, seeded) · GENESIS v0.8 + Onboarding v0.3 + GUIDE/README cures · the **Gate-1 hand-off packet** for KNet's migration · **UF1 contracted, parked**. **The queue's head (new):** the **KNet kernel migration** — Max's own hand, in KNet, under its P-007 gates (the packet is Gate-1 input; the session stops at Gate 3 for his adoption decision) — then the **UF1 updatability flight** here: the executed migration its evidence corpus, the kernel-side migration/update standard its product (OQ-33's design act, beta-0.6 material). Beside it, **the named build items**: the Scan-#5 conformance checker (the C-108 gate — must exist before the next Scan closes) · the C-110 mechanization implementation (history rows at `--write`, the `--stamp` pass, the walk pre-fill, the drag gauge — built adaptively per C-112) · **registry growth on the C-094 loop** (the extraction protocol forging in KNet; the density-pass commission — RF1 residue) · **KNet's child-side acts** (adapter ratification · board adoption · opener rename — **all natural riders of the migration matrix**) · **capture v1.2** held for a later approval pass · the annex remote (optional). **The flip:** open, trigger-bound — necessity, not a date (the registry's first off-machine consumer its named face; Global §2 transition + C-075 fire with it whenever it lands). **Arc 2 alive** (KNet; the window open; the Q07 experiment at accepted-design, sequenced post-migration; four fieldnote lanes). **The grab demo** arms on child content worth ingesting (pull, OQ-31) — the context-fidelity probe the strongest candidate yet.

| Arc (C-078) | Status |
|---|---|
| **1 · Kernel extraction** | **Closed** (2026-08-15) — partition measured; GENESIS (v0.8 today) + `genesis.py` gated; packaging unit = adapter + commands (C-083); birth test ×2. Delivered the *skill* tier; the *plugin* tier waits on a tier-1 index (C-079) |
| **2 · First foreign instance — KNet** | **Open — live.** Born from the anchored beta-0.1 tag; §6 acceptance passed same day; two clean closes; now the second-client trial's testbed. OQ-28 collecting real data; OQ-4's comparison moment = this arc's completion |
| **3 · Federation** | **Armed** — the second store exists and carries a sourced queue; the grab fires when its content is worth ingesting mechanically (OQ-31: origin layer, `import` verb, compat check) |

**Standing offers beside the queue:** the workbench's next slice (the vehicle decision — possibly no vehicle at all) · move 2's surfaces (site, tutorials — now drafting *from the capture*; the pitch leads with the distillation set + USP series) · **context-rot metrology** (`general` FN-0001) · **the preface standard / paper passport** (in the child as P-003; prime grab material for arc 3) · **the command-forge** (entry 45, vision-class — the flag-mangling friction recurred twice this session, third and fourth specimens) · **the multi-agent probe program** (per v0.44 §2) · **three banked conduct candidates** for the next conduct-touching Settings reissue (suggestion hygiene · the board offer as conduct · **always-flag-findings**, Max's directive per v0.46). **Review ledger:** open rows — **S3-2.2** (deliberately held; each spec's next reissue) and **Capture #1's** (`CAP1-…`, per v0.46: 5 High on named triggers — CAP1-HUNT-1 the flip-gating consent call; Medium/Low grouped, evidence in the capture's Appendix A). No next Scan penciled — reviews are commissioned (two variants now: Scan and capture, C-103).

**G. Standing open questions — eight, all trigger-bound (offers, C-031):** OQ-28 (the weight dial — arc 2 data flowing; benchmarking its engine-side twin, `general` FN-0004) · OQ-29 (code as governed artifact) · OQ-31 (federation — the context-fidelity probe the strongest grab candidate) · OQ-32 (profiles & roles — **two AI writers now live in the child**, the waited-for multi-writer reality) · **OQ-33** (instance migration / kernel upgrade path — opened per v0.48; first live case KNet at the beta-0.5 registry) · OQ-4 (comparison moment = arc 2 completion) · OQ-9 (next promotion) · OQ-27 (outside-custody scope).

**H. Build track (absorbed by the program):** the KG index MCP server — spec complete (Navigation Contract, C-070); trigger sharpened (C-068): inverted-view reads becoming routine — genuinely fires around arc 3. · The interface: forged and walking (C-093); next slices via the Foundation by ordinary decision. · Shipping tier: skill tier delivered (C-083); the plugin tier waits on the tier-1 index, making H's first bullet its own unblocker.

## Standing refresh items — the C-059 checklist (normative home)

Every close walks this table; each row marked **changed** or **checked — unchanged**. Since C-060, `check_versions.py` verifies the walk mechanically. Marks below are for the nineteenth session (the registry-release close, 2026-08-17, per v0.50):

| Living projection | Refresh trigger | This close (nineteenth session) |
|---|---|---|
| Decision Register | any decision / OQ / finding change | **changed → v0.68** (C-113–C-115 rows; the RF1 slate dispositioned — High Fixed, 7 Med/Low Fixed, residue grouped-open; OQ-33's first live case + UF1; the v0.68 history row) |
| Rule Overview | rule added or retired | **changed → v0.42** (three rows added — the Registry, vocabulary extension, sources & materials; rule 10 registry-first) |
| Local Context (this file) | every close | **changed → v0.69** (this walk; stamp above; row 19) |
| Agenda Board (file-first, C-048) | agenda change | **changed**, restamped (head re-pointed at the migration + UF1; counts refreshed post-ripple) |
| Context Index | routing / location / version-of-note change | **changed → v0.40** (the Registry routed — entry point + document row; GENESIS → v0.8; the two new standards' rows) |
| Global Context | vision or realisation transition | checked — unchanged (v0.4) — the registry ships *inside* the shipping-and-proving mode; the flip transition stays armed on necessity (C-107) |
| Architecture State | architectural decision / supersession | **changed → v0.27** (§6 tally: C-113–C-115 kernel — ~89·9·18; the stale genesis-version claim cured) |
| BOOTSTRAP.md | ritual / adapter / protection change | checked — unchanged (v0.21) — no rehydrate-path change; the registry is in-tree content and the `verify.py` hook alters no gate semantics (absent file = inherited behavior exactly) |
| Schema → Template coupling | Schema reissue | checked — unchanged (S3-2.2 holds its trigger) — C-114 deliberately composes checker-side without a Schema reissue; the §3.3 complement joins the Schema's next reissue (the S3-2.2 rider) |
| Store tools (seven + utilities) | schema section moves / tool change | **changed** — `verify.py` gains the C-114 vocabulary hook (tested: the store passes unchanged; the three error classes fire correctly); the C-110 mechanization + C-112 adaptive build stay queued; the diet gauge gate armed |
| Conversation Settings (C-071 row) | any conduct-touching decision | checked — unchanged (v0.8) — the C-071 question asked and answered *no*: C-113–C-115 are governance-structure and standards decisions (the C-094/C-095 precedent class); **five** candidates stay banked (suggestion hygiene · board offer · always-flag · one-open-loop · the drone offered actively) |
| Client adapters — stamps + content (C-077) | Settings change or doctrine touching a surface's rules | checked — unchanged (both v0.9) — stamps current at Settings v0.8; content checked against C-113–C-115: no surface-conduct rule moved (rule 9's shelf check reaches the Registry through GENESIS §5 and the Rule Overview, both already in its pointer chain) |

**Store hygiene (C-050, standing):** `python3 verify.py` before every store-touching commit; red blocks. **Corpus hygiene (C-060, standing):** `python3 check_versions.py` at every close; red blocks.

## Relevant canonical documents

- **Architecture State v0.27** (the map; ONT §3, WS §4, instantiation §6) — `/Claude/Architecture/Architecture State/`.
- **Schema v0.6 / Template v0.5 / Navigation Contract v0.1 / Scholarly Source Convention v0.1** — `/Claude/Architecture/Concepts/Knowledge Graph/`. **Store** (seven tools: `verify.py` — now carrying the C-114 hook · `mint.py` · `check_versions.py` · `close.py` · `genesis.py` · `doctor.py` · `fieldnote.py` + `fieldnote_roster.json`) — `/Claude/Knowledge Graph Store/`.
- **Protocol v0.50 + Register v0.68 + Rule Overview v0.42 + Settings v0.8 + the Client Library + the Onboarding Protocol v0.3 + the Fieldnote Format v0.1 + the Agenda Board Format v0.1 + the Ultracode Flight Protocol v0.2 + the Scan Procedure v0.1 + the Diet Procedure v0.1 + the Vocabulary Extension Procedure v0.1** (stamps inside, C-012) — `/Claude/Project Governance/`. **The Registry** — `Registry/README.md` (repo root, C-113). **The diet flight records:** `Flight_2026-08-17_Diet.md` (DF1) + `Flight_2026-08-17_Compaction-Behaviour.md` (CB1) — `Context Packages/Conversations/`. **The migration hand-off packet** — `Site/Upstream_Advisory_2026-08-17_KNet-Kernel-Migration.md`.
- **The capture (C-103):** `Capture_2026-08-16_Vision-to-Horizon.md` — `Context Packages/Conversations/` (approved raw material of the public identity); verified dossiers — `Site/Capture Materials/` (pre-canon).
- **Concept tier (C-086):** Knowledge Network Foundation v0.3 (§6: the registry) — `/Claude/Architecture/Concepts/Knowledge Network/` · Workspace Foundation v0.2 — `/Claude/Architecture/Concepts/Workspace/`.
- **The Workbench** (`Workbench/` — working tool; export-ignored from release archives). **Index v0.40 + Agenda Board + the Command Index** (stamp inside — the sextet + child tier at a glance) — `/Claude/Context Packages/`. **The frozen H6 grant tree (C-109)** — `~/Desktop/fractal-frozen-beta-0.4` (outside both jurisdictions, read-only by construction).
- **Fieldnotes** (pre-canon, C-062 — the shipping-run ledger, 45 prose entries + blocks; **the publishing lane** FN-0001–0004; **the general lane** FN-0001–0002; four roster targets) + **the second-client advisory** — `Site/`. **KNet** (external, its own jurisdiction) — `~/Desktop/knet` · `github.com/mstruppe/knet`. **The annex** (private, ungoverned) — `~/Desktop/fractal-annex`. **The mirror** (C-098 phase, private) — `~/Desktop/fractal-mirror` · `github.com/mstruppe/fractal-mirror`.
- **BOOTSTRAP v0.21 · GENESIS v0.8 · GUIDE.md · the Registry (`Registry/`) · CLAUDE.md v0.9 · AGENTS.md v0.9 · README.md · LICENSE / LICENSE-docs / NOTICE · `.gitattributes` · `.allowed_signers` (four principals — `fractal@knet.network` joined per C-105) · `.claude/commands/` (the sextet: `/fractal` · `/close` · `/welcome` · `/begin` · `/look` · `/fieldnote`)** — repo root. **Release provenance (C-090):** the root folder Provenance — beta-0.1–0.3 (Bitcoin-final) + beta-0.4 + beta-0.5 (anchored at this close); **the mirror's own public chain** — beta-0.3 + beta-0.4 tags with receipts in the mirror's Provenance (beta-0.5 joins at the mirror rebuild).
- **Loose-Ends Scans #1–#4** (canonical reviews, C-058) — `Context Packages/Conversations/`.

## Next — sequenced (C-021)

1. **DONE:** the foundation, the checkers, arc 1, the punch list, four betas, the flip preparation, ship acts 1–2, the approved capture, the public identity + the stranger test, the pivot + the flight tier, the diet program, **the registry release — C-113–C-115: the Registry live, the RF1 High slate cured, GENESIS v0.8, the KNet migration prepared (P-007 read + the Gate-1 packet), UF1 contracted-parked, beta-0.5 packed** — see the session table; reasoning in Protocols v0.29–v0.50.
2. **THE QUEUE'S HEAD (new, per v0.50):** the **KNet kernel migration** — Max's own hand, in KNet, under P-007's gates (the packet is Gate-1 input; the child stops at Gate 3 for his adoption decision; the child-side acts — adapter ratification, board adoption, opener rename — ride the matrix naturally) — then the **UF1 updatability flight** here (the executed migration its evidence; the kernel-side migration/update standard its product — OQ-33's design act). **Named pre-builds (standing):** the Scan-#5 conformance checker (the C-108 gate) · the C-110/C-112 mechanization (the adaptive close built into the tooling).
3. **OPEN — LIVE (C-078 arc 2):** KNet — the Q07 context-fidelity experiment (sequenced post-migration by the child's own protocol; H6's corpus stays the C-109 beta-0.4 tree) · the benchmarking transfer (Max's own hand); frictions → fieldnotes → next ingestion slate.
4. **ARMED (C-078 arc 3):** federation — the grab fires when KNet's graph content is worth ingesting mechanically (the context-fidelity probe the strongest candidate); pull; machinery on the ingesting side (OQ-31).
5. **ON OBSERVED NEED:** capture v1.2's approval pass (held) · the KG index MCP server (fires around arc 3) · the command-forge · the vehicle decision · Galaxy UI · **the flip** (necessity-triggered, C-107).

**To open the next conversation:** the queue's head is **Max's own KNet session** (its jurisdiction — open there, hand it the packet). Here: Claude Code in the repo → **`/fractal`** → **UF1 on the migration's landing** or any standing offer (C-031). `/look knet` stands apart as ever. Close by typing **`/close`**; manual close stays the fallback (Return Package, checklist, both checkers green, protocol or recorded reason).

---

**Sources:** Fractal Global Context v0.4; Governance Protocol — Claude Series v0.1–v0.50; Decision Register v0.68; Conversation Settings v0.8; Rule Overview v0.42; BOOTSTRAP v0.21; GENESIS v0.8; Fractal_Registry (`Registry/README.md`); Fractal_Ultracode_Flight_Protocol_v0.2; Fractal_Scan_Procedure_v0.1; Fractal_Diet_Procedure_v0.1; Fractal_Vocabulary_Extension_Procedure_v0.1; Fractal_Scholarly_Source_Convention_v0.1; Site/Upstream_Advisory_2026-08-17_KNet-Kernel-Migration.md; Flight_2026-08-17_Refinement.md; Flight_2026-08-17_Diet.md; Flight_2026-08-17_Compaction-Behaviour.md; Fractal_Onboarding_Protocol (stamp inside, C-012); Fractal_Fieldnote_Format_v0.1; Fractal_Agenda_Board_Format_v0.1; Fractal_Client_Library (stamp inside, C-012); CLAUDE.md v0.9; AGENTS.md v0.9; README.md; GUIDE.md; Knowledge Network Foundation v0.3; Workspace Foundation v0.2; Node & Event Schema v0.6; Node Template v0.5; Navigation Contract v0.1; Architecture State v0.27; Context Index v0.40; Fractal_Command_Index (stamp inside, C-012); Knowledge Graph Store (live); Capture_2026-08-16_Vision-to-Horizon.md + Site/Capture Materials/ (C-103); Review_2026-08-17_Stranger-Test-1.md; Site/fractal-site.html (rebuilt); Site/Fieldnotes_2026-08-15_First-Shipping-Run.md, Site/Fieldnotes_2026-08-16_Publishing.md, Site/Fieldnotes_2026-08-16_General.md (pre-canon); Site/Upstream_Advisory_2026-08-16_KNet-Second-Client-Trial.md; Return_Package_2026-08-17_Publishing-Identity.md; Return_Package_2026-08-17_Refinement-Pivot.md; Return_Package_2026-08-17_Postscript-Diet.md; Return_Package_2026-08-17_Diet-Session.md; the per-session record above (Protocols v0.29–v0.50 + dated Return Packages, `Context Packages/Conversations/`).
