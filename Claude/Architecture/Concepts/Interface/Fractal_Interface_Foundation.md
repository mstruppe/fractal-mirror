# Fractal Interface Foundation

> **CONCEPT FOUNDATION (C-086 class — the fourth member) — Draft, non-binding vision tier.** This document *informs, never governs* (the C-086 contract): nothing here binds an instance until an ordinary decision adopts it. It is the forge of record for **the fractal interface** — the standardized way FRACTAL-governed projects communicate with each other. **Ratified into the tier 2026-08-18** (in-conversation per C-033 — Max: *"ratified and close"*; the Workspace/Value-Model precedent — the tier status stays Draft by class).

**Version:** 0.5 · **Status:** Draft — non-binding vision tier; C-086 fourth member, ratified into the tier 2026-08-18 (drafted the same session on Max's commission: *"I want to build a new concept which we will push into beta 0.6: the fractal interface; a protocol designed because of a real need and first step foreward towards a fractal social network"*; his call verbatim: *"ratified and close"*; §8 parameters answered 2026-08-19 — PR + the inbox transport, his *"ratify and close"*; **v0.3 the interface-place amendment, 2026-08-19 — his "ratified"**: the communication RAM, §9; the KNet-contributed execution semantics, §10; **v0.4 the installed-integration correction, same day — his "ratify"**: two connection modes, live in the workspace / fixed at the seam, §10; **v0.5 the interface-build reissue, 2026-08-19 — his "perfect, go" on the C-123 slate**: the credited KNet refinements land in §10, the Interface Place Format v0.1 extracted as §9's first mechanical slice, the two-children deferral + the transport candidate of record (git/HTTPS — the proven class) recorded in §8) · **Domain:** GBL · **Author:** Claude · **Parent:** Fractal Global Context · **Siblings:** Knowledge Network Foundation (v0.4) · Value Model (v0.2) · Workspace Foundation (v0.2) · **Document ID:** DOC-01M0B4E4CFRX5D8B5MSMD53Y9D (minted at the ratifying close via `close.py --create`; stamped by the paired revise)

---

## 1 · The real need (observed, not imagined)

The controlled network (C-107) stopped being hypothetical some sessions ago: **FRACTAL** (the mother) and **KNet** (the first child, on the beta-0.5 kernel) are two live governed instances — and the commission that opens this document declares a **second child**, with a deliberate revert recorded: *the social-network ambition leaves FRACTAL's interior* (where it lived as vision-tier features) *and becomes a network of instances*. Three instances working one shared project is not a metaphor — it is a **small organisation**, and it already talks: every exchange to date has been an ad-hoc artifact carried by Max's own hand — the migration advisory packet, the H6 frozen-tree path, the FN-0004 issue transfer, today's gas-metering hand-off. The need is exactly what the commission names: **one standardised interface, so the ad-hoc shapes become a protocol.**

## 2 · The principle — the interface holds directions, never context

The founding design sentence, verbatim: *"they get a navigation system in the interface so the interface doesn't have to hold the context, it just holds the directions towards the information it want to give."*

A message between instances is an **envelope of addresses, not a payload**. The receiver follows the direction into the sender's *governed* tree — at an anchored pin — and reads the content where it lives, under the custody and verification that already guard it. The channel itself stays thin, auditable, and incapable of rot: a pointer cannot go stale in the way a copy does, because it names its edition.

This is not a new law — it is FRACTAL's one law at a new scale. The Context Index routes instead of holding; the diet evicts narrative into pointers to frozen records (C-095/C-110); a child receives a frozen tree, never a window (C-109); a citation names its pinned edition (C-115). **The interface is the same recursive simplicity applied *between* instances** — which is what earns it the name *fractal* interface.

## 3 · The address grammar (sketch)

A direction resolves in four parts:

| Part | What it names | Existing machinery it reuses |
|---|---|---|
| **instance** | which governed project | the registered identity (GENESIS §2 name; the network roster is the umbrella's future ledger) |
| **pin** | which edition | an anchored release tag or commit SHA (C-090 — provenance-anchored, citable, immutable); `living-public` only for surfaces an instance deliberately serves live (the C-120 site class); `working` — the third, deliberately non-network class (v0.5, KNet-contributed): base SHA + content hashes of the *actually read* files + dirty flag — citable evidence for uncommitted inputs, never an address |
| **route** | which domain inside it | the instance's own Context Index — **every instance already ships its address book** (the route codes, C-046-class) |
| **locus** | which document, which section | file + anchor, the instance's own naming |

Resolution is the Navigation Contract's single verb — `resolve` — run by the *receiver*, against the pinned tree it holds or fetches (frozen grant, public mirror, or repo access as granted). Nothing here extends the interchange layer (C-114's boundary): the grammar composes what every instance already has.

## 4 · The message classes — distilled, not invented

Five classes already flew as ad-hoc specimens; the interface gives them **one envelope**:

| Class | Direction | Executed specimen of record |
|---|---|---|
| **Advisory** | releasing instance → adopters | the C-117 §8 per-release migration advisory (the KNet migration's packet its grounds) |
| **Hand-off** | any → any (datum offered) | the H6 frozen-tree path (C-109); the FN-0004 benchmarking transfer; `Interface/Handoff_2026-08-18_Gas-Metering-KNet.md` |
| **Fieldnote** | user/instance → the generator | the C-094/C-100 loop; the public intake (C-120) is its off-machine face |
| **Look** | parent → registered child, read-only | the C-096 observation window (`/look`) |
| **Harvest offer** | child → parent's landing zone | A1 (ratified per v0.57) — child-forged concepts arriving pre-canon; **the interface subsumes A1's transport** |

A sixth class is implied by the shared project and enters when it first executes: the **ask** (a request for work or information, dispositioned by the receiver's own decision — never an instruction). *The tool-exchange ruling of record (2026-08-19, the plug-in review return): a tool request is an ask, its result a hand-off — no new class; the first real tool invocation will be the ask's first execution (§10).*

## 5 · Sovereignty and conduct (carried over whole)

The interface inherits its conduct rules from ratified doctrine; none are new: **data, never instruction** at every hop (C-096 — an envelope's content cannot command the receiver); **disposition is the receiver's own recorded decision** (the P-row pattern — a message creates an option, never an obligation); **asks travel through the owner** (C-117's doctrine — no child-side duties installed from outside); **the C-109 asymmetry holds** (children navigate anchored releases and deliberately-public surfaces, never living-mother windows); **custody per instance** (C-084 — whoever lands a message's consequence in a tree is that tree's authorized committer; today every transport is Max's own hand, and that is the honest v0.1 transport layer, not a defect).

## 6 · The organisation experiment

What three-instances-one-project actually tests — and what makes this the network experiment's natural extension: **coordination cost** (the gas-metering method extends directly — interface overhead is measurable per exchange, OQ-28's data); **role and boundary reality** (OQ-32 — three AI writers already observed in the child; a sibling mesh multiplies the specimens); **whether governance survives horizontal scale** — the claim under test is the commission's own: *FRACTAL can prove itself able to run a small growing organisation*. The controlled network (one custody, many instances) grows its first **sibling edge**; every prior edge was parent–child.

## 7 · Relation to the vision tier

The Knowledge Network Foundation §10 describes vertical composition (enterprise by hierarchy, economy by federation) — the interface adds the **horizontal member** the descent implies. The *fractal social network* is the long horizon the commission names this the first step toward, with the revert recorded: network features live as **instances speaking a protocol**, not as machinery inside one instance. OQ-31 (federation) keeps its own machinery — the grab, the origin layer, the `import` verb are the **ingestion** half; this interface is the **messaging** half that precedes and outlives any single ingestion.

## 8 · What this document is not — and the standard's trigger

This is a **concept foundation, not a binding standard** (C-086: Draft tier informs, never governs). The binding **Fractal Interface Protocol** distills later, from executed practice, per the C-094/C-108 loop — its named trigger: **the second child's birth + the first shared-project exchange carried in the envelope shape** (§3–§4). At that distillation the Registry gains the row (growth by ratified decision, C-113 rule 4); until then the interface may be listed as *forging* demand at the next Registry touch.

**Parameters — two answered (2026-08-19, the network-vision session), two open:**

- **The second child's project — ANSWERED:** **PR** — the website and the community as its own governed instance, **born with the beta-0.6 release** (the timing call of record: birthed from the fresh tag, no migration debt; the grooming material its dowry). The child's **name** stays open.
- **The v0.1 transport — RATIFIED** (Max: *"ratify and close"*): **the inbox shape** — a direction envelope is a **file in the target instance's own repo** (its inbox), carried by git and the owner's hand, and **read by the target's opener at session start** as its queue head (the C-031 slot: an offer, never a mandate — the target's session executes in its own jurisdiction, own context, own close). Directions-never-context enforced by construction: what travels is the envelope, never the sender's working memory. The dispatcher's seat is the government fractal (OQ-34's annotated shape — the seat doctrine and the Manifest exception recorded there); **the mother plays seat pro tem** until it births. A sixth message-class candidate is named beside the five: the **editorial flow** (periodic, digest-shaped, public-bound — the releases-and-research stream to PR). **AMENDED at v0.3 (2026-08-19, ratified — the placement inverts, §9):** communication files stay in the **author's** interface place and the reader comes to them — pull, never push; nobody writes into a foreign tree. The inbox *placement* of this bullet is superseded as written; its C-031 queue-slot semantics carry forward unchanged — discovery is the owner's word today, index polling by openers tomorrow (§9's trajectory).
- **Open:** the shared project's definition (the network experiment itself the standing candidate; the shared-core contract's content — Max's sentence of record: siblings share the core and a contracted slice, PR need not know the domestic domain) · the concept's public page (a website-grooming candidate; the concepts user-documents section its nearest neighbor since 2026-08-19).
- **The counterpart parameter — deliberately deferred (2026-08-19, the interface-build direction of record):** the place is built **generic** — *"for now we need not specify who will look into it, whether it is KNet or someone else, we will develop that later, when we have 2 children. There it will become apparent how we navigate different fractals in the interface."* The Interface Place Format v0.1 (§9's first mechanical slice, extracted the same session) carries the deferral in its §5, and the transport horizon stays open the same way — *"later this can be automated data transfer, API, email tech, whatever is best"* — the manual place-and-index shape forward-compatible because it holds only the law. **The transport candidate of record** (same session, his *"perfect, go"* — his formulation: the proven route, simple to implement): the proven file-transfer class — **git-over-SSH/HTTPS first** (already installed in every instance, authenticated, integrity-checked, pull-never-push by construction, pins native), HTTPS serving for anonymous readers (the C-120 class), mail-class tech only if push-style notification is ever wanted. Formulated and banked, never built ahead of need (C-021).

## 9 · The interface place — the communication RAM (v0.3, ratified 2026-08-19)

Max's design, verbatim: *"a fractal has a place where it puts information files under a navigation index and every fractal knows that it can go there into other fractals to see the information"* — *"a second ram for communication"* — *"the interface holds temporary information, the time window is determined by the projects creation and deletion of the information in their own interface."*

**The model.** Every instance designates **one deliberately-readable location** — its interface place — holding its outbound communication files under a small **navigation index**. Communicating means *posting in your own place*; learning means *visiting the other's*. Four consequences, each doing real work:

1. **Pull, never push.** Nobody ever writes into a foreign tree — the sovereignty boundary is enforced by the transport's shape itself, not by discipline. (This is the §8 amendment: author-side placement supersedes the target-side inbox; the C-031 offer semantics carry forward unchanged.)
2. **The index is the addressing layer** — the Context Index pattern applied once more, at the communication scale (recursive simplicity, §2's own argument): one small routing file listing what currently stands — id · date · class · direction · counterpart · status (*standing / spent*). A reader checks the index, not the folder; an opener can poll known indexes at session start for free mechanical discovery.
3. **Declaring the place is declaring it readable.** The interface place joins the deliberately-served surface class (§3's `living-public` legitimacy, the C-120 site class) — which is exactly why a child may read it in the living mother without breaking the C-109 asymmetry. The mother's place is **`Interface/` at the repo root** — born 2026-08-19 on Max's call ("that's the birth of the interface"), the `Site/` era's standing hand-offs moved in as its founding stock; whether an instance keeps a mixed surface or a dedicated folder is an instance choice, not doctrine.
4. **Envelopes are RAM (C-121 generalized).** The information is temporary *by construction*: the window is the owner's own creation and deletion, in its own jurisdiction, at its own time. The reader's one duty follows: **absorb and cite by path + date/pin — never depend on the file persisting.** Citations plus git history are the full trace; a spent envelope dissolves exactly like a solved fieldnote.

**The trajectory** (named honestly, and load-bearing): manual — the owner speaks directions (today) → openers poll known interface indexes (the first mechanical slice; the index format is the thing to standardize) → automated transport (the e-mail analogy: shared spool directories preceded SMTP). The law — what lives where, who owns the window — survives every upgrade, which is what makes the manual version forward-compatible rather than throwaway.

**The first mechanical slice is executed (v0.5, the interface-build session):** the place-and-index shape is standardized as **`Fractal_Interface_Place_Format_v0.1`** (Governance Documents) — the contract compressed from this section, the index grammar distilled from this instance's executed index, the envelope minimum from the four standing specimens, plus an instance-neutral skeleton. Genesis ships the format in the kernel and **births every newborn with the place** (`Interface/Interface_Index.md` at `IF-0000`), installing the awareness at birth — *any other fractal project can go there* becomes true by construction. Counterpart navigation stays deliberately unspecified in it (the two-children deferral, §8); the binding Protocol's trigger is untouched.

**Executed grounding (C-094/C-108 — the design distills practice, not the reverse):** the 2026-08-19 two-way exchange ran this shape *before* it was stated — KNet's plug-in offer read in KNet's own tree, the mother's review return read in the mother's `Site/` — the interface's first two-way manual specimen, both legs author-side, both directions carried by the owner's hand.

## 10 · Execution semantics for tool exchanges (KNet-contributed, v0.3)

Contributed by the first child in its plug-in hand-off (2026-08-19) and accepted in the mother's review return — the reverse-inheritance channel (OQ-31) working as designed; credited to KNet (AGENT.AI.CODEX authorship, cross-model four-eye review the recorded practice. Non-binding here like everything in this tier; graduates into the binding Interface Protocol at §8's trigger).

- **The act chain — no act implies the next:** discover ≠ install ≠ connect ≠ execute ≠ accept ≠ publish; and upgrading a tool never reinterprets earlier results (mechanically delivered by C-115's cite-the-edition rule — results cite the pin that produced them).
- **The five-grant ladder:** permission to connect · to read named inputs · to spend to a declared resource ceiling · to retain/export tool-side artifacts · to import a result into the host. The first four enable work; **only the fifth touches host-held knowledge**, and it follows the host's own acceptance rules (pre-canon + provenance line at the seam).
- **Grants are host-side recorded decisions** (the P-row pattern), cited by id in envelopes, **never contained in them** — authority lives in the record, never in transit; even the request is an offer the executing side dispositions.
- **Jurisdiction executes — in one of two connection modes (v0.4, Max's correction of record: the tool *"should act as an integration, but one you can install or de-install"*):** whoever owns the sandbox runs the work. **Installed integration** — the default at every phase: the tool is installed into the host as a pinned release and runs in the host's own sessions, on **live working data**, mid-session — no jurisdiction boundary is crossed, so no frozen-input rule applies (C-109 governs children reading the living mother, never tools installed into a host). **Remote instance** — the rare, later mode: the tool executes asks from its own interface place, in its own loop, and cross-jurisdiction inputs travel pinned. Distribution is orthogonal to connection: a tool with its own repository and anchored releases is still normally installed and used live — its repo changes where releases come from, not how they run.
- **The sandbox is an authority boundary, never a freshness boundary:** identical in both modes — workspace-only writes, results as offers, the five grants, the C-087 secrets floor. **The pin belongs to the seams, not the calls:** pin the tool release always (the C-117 grammar); pin data only at cross-jurisdiction transfer and at acceptance into canon (inputs snapshotted at the import act where replay matters, recorded in the provenance line) — **live in the workspace, fixed at the seam**. Install/de-install rides the hangar's sandbox-clean grammar unchanged: pinned adoption by recorded decision; removal severs the route pointer, deletes package and workspace; accepted results survive on their provenance lines.
- **The credited refinements (v0.5 — accepted from KNet's second review return, the confirmation leg 2026-08-19; banked there, landed here):** **run receipts** — captured mechanically at every material invocation (the exact read set: base revision + per-file content hashes + dirty-state generation; release identity; parameters; mutations with before/after hashes; resource use; consistency flag), **promoted into the provenance line at the acceptance act** — capture is mechanical and always-on, fixation is a governance act citing the capture (the earlier "snapshot at import" wording stands corrected by it, and the receipt's hashes *are* the edition citation for uncommitted inputs — the §3 `working` class) · the **three integration profiles** (Observe / Collaborate / Live edit) as declaration-advertised, session-scoped grant bundles · the **seven-authority ladder** as the live-mode refinement of the five grants (the five remain the compact federated summary; the profile is the mother's oldest pattern at tool scale — ask-first with recorded standing authorizations, C-008/C-064) · the **concurrency law** — base-hash check before write-back, atomic application, recompute-or-ask on drift, **no silent last-write-wins**, visible staleness on read-only results (the C-049/C-050 spirit at tool scale) · the **expanded act chain** including activate / expose / write-back / deactivate / uninstall · the **uninstall floor** — resolvability, never the package: receipts + accepted outputs + a durable custody pointer to the producing release survive removal; the package itself is retained only where no anchored home exists.

---

**Refresh triggers:** the second child's birth; the first executed envelope exchange **in the §9 place-and-index shape**; a ratification act moving any § from vision to binding rule; a C-059 walk finding this stale.
**Sources:** the commission (this session, verbatim in the banner); C-086 (the concept tier); C-096/C-109/C-117/C-100/C-094 + A1 per v0.57 (the five executed message classes); C-090/C-115 (pins and editions); the Navigation Contract v0.1 (`resolve`); C-046/C-012 (route codes as address books); C-107 (the controlled network); Knowledge Network Foundation v0.4 §10–§11; OQ-28/OQ-31/OQ-32 (the measurement, ingestion, and role questions this touches); the executed hand-off specimens named in §4; `Interface/Handoff_2026-08-19_Live-Integration-Review-KNet.md` (the confirmation leg — the credited refinements' source, RAM-class, citable by path + date); `Fractal_Interface_Place_Format_v0.1` (Governance Documents — §9's extracted mechanical slice).
**Revision history:** v0.1 (2026-08-18) first issue — drafted in the twenty-fifth Code session on Max's in-session commission; Draft, pending his ratification act. · v0.2 (2026-08-19) **two §8 parameters answered (the network-vision session, Max's "ratify and close"):** the second child = **PR** (website + community, born with beta-0.6 — his timing call; name open) and the **v0.1 transport = the inbox shape** (direction envelopes as files in the target's repo, git + the owner's hand, read by the opener as the C-031 queue head; the government seat the dispatcher per OQ-34's annotation, the mother pro tem); the editorial flow named as the sixth message-class candidate; the shared-core contract sentence recorded under the open shared-project parameter. Plan of record: `Fractal_Roadmap.html`. · v0.3 (2026-08-19, same day — the tool-review session, Max's **"ratified"**) **the interface-place amendment:** §9 the communication RAM — every instance's own deliberately-readable place + navigation index; pull never push; the owner-controlled time window (C-121 generalized: envelopes are RAM, absorb-and-cite the reader's duty); the manual→polling→automated trajectory — **§8's inbox placement superseded in place, its C-031 semantics carried**; §10 the KNet-contributed execution semantics (the act chain + the five-grant ladder + grants-as-cited-decisions + jurisdiction-executes; reverse inheritance, credited); §4 gains the ask/hand-off tool ruling; the first two-way manual exchange (KNet's plug-in offer ↔ the mother's review return, both legs author-side) recorded as the executed specimen. Sources gained: the two 2026-08-19 hand-off files (KNet's `Handoff_2026-08-19_Optional-Tool-Fractal-Plugin-Mother.md`, its jurisdiction · the mother's `Interface/Handoff_2026-08-19_Tool-Fractal-Plugin-Review-KNet.md` (path current since the place's birth) — both RAM-class, citable by path + date, deletable by their owners once absorbed) · C-121/C-122. · v0.4 (2026-08-19, same session — Max's **"ratify"** on his own objection: *"I would still like to use the tool live … this should act as an integration, but one you can install or de-install"*) **the installed-integration correction:** §10's jurisdiction bullet becomes the **two connection modes** — installed integration (the default at every phase: pinned tool release running in the host's own sessions on live working data; no jurisdiction boundary crossed, no frozen-input rule — C-109 scoped back to its real subject, children reading the living mother) vs. remote instance (cross-jurisdiction inputs travel pinned) — plus the boundary restatement: **the sandbox is an authority boundary, never a freshness boundary; live in the workspace, fixed at the seam** (pin the tool always, pin data only at transfer and at acceptance-into-canon); distribution orthogonal to connection; install/de-install = the hangar's sandbox-clean grammar. The return hand-off's §3.4 corrected in the same act. · v0.5 (2026-08-19, the interface-build session — Max's direction: *"build it simple and solid, only what the process needs without hard forking it away from any future development"*) **the interface-build reissue:** §10 gains the **credited refinements block** — the five accepted from KNet's confirmation leg (run receipts capture-at-invocation / promote-at-acceptance · the three integration profiles · the seven-authority ladder · the concurrency law · the expanded act chain, + the uninstall floor), landed as banked; §3's pin grammar gains the **`working` class** (KNet-contributed, answer 7 of the confirmation return); §9 records the **first mechanical slice executed** — `Fractal_Interface_Place_Format_v0.1` extracted (contract + index grammar + envelope minimum + skeleton; genesis ships it and births the place); §8 records the **counterpart parameter deliberately deferred** (the two-children point — the direction of record verbatim) and the open transport horizon, with the **transport candidate of record** banked at ratification (his FTP/TCP instinct resolved to the class's modern members: git-over-SSH/HTTPS first, HTTPS serving for anonymous readers, mail-class at need — proven, already installed, never built ahead of need). The slate ratified whole on his *"perfect, go"* (C-123); the Protocol's distillation trigger unchanged.
