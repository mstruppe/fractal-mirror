# Fractal Rule Overview

> **DERIVED PROJECTION — the one-page rule-book, not a source of truth.** Every rule below is defined authoritatively in the document named in its *Source*; this overview only collects them so the whole system's rule-set can be read at once (per C-003 it cites and never overrides). Living document: stable filename, version tracked below. Canonical copy: `/Claude/Project Governance/Governance Documents/`. **Part of the context package (C-036):** routed in the Context Index as a standing guideline of work and read in-repo, exactly like the Decision Register (C-030; the mirror mechanism retired 2026-08-14, C-067).

**Version:** 0.38 · **Status:** Living (derived projection) · **Updated:** 2026-08-17 · **Domain:** GOV · **Author:** Claude · **Parent:** Fractal Global Context · **Document ID:** DOC-01KZVYPMBGEY2HV35F32631G0C

**Why this document:** FRACTAL's credo is that the most complex structures arise from the simplest set of rules. This page is that set, made visible — five layers, each small, each traced to its decision of record.

---

## 0 · The meta-rule (philosophy)

**Recursive simplicity / minimal necessary complexity.** Begin from the smallest abstraction; add only what an observed problem requires; the same pattern repeats at every scale (project → domain → component → conversation → task). *Source: Global Context §1.* Its two working corollaries: **lean is the tie-breaker** under fixed quality (Protocol v0.1) and **build on observed need, not anticipation** (C-021).

## 1 · Conversation rules (how every session behaves)

*Source of truth: Conversation Settings v0.8, §4 rules 1–10.*

| # | Rule | Anchor |
|---|---|---|
| 1 | Orient from the Global Context first; never scan the whole folder to orient. | CS §4.1 |
| 2 | Canonical documents in the folder are the sole source of truth; packages and Project docs are derived projections. | CS §4.2 / C-003 |
| 3 | Load only what the task needs — **Global → Local**; Domain, Task and Return are *scopes of the Local pole*, not package layers (C-009). Resolve through the Context Index. | CS §4.3 / C-009, C-054 |
| 4 | Stay in the declared domain; keep accepted decisions separate from proposals; externalise durable work promptly. | CS §4.4 |
| 5 | Ask before executing until the workflow is calibrated — **calibration = recorded per-action promotion** (a promotion covers exactly the action it names; first: `git push` at close, C-064). | CS §4.5 / C-008, C-064, C-071 |
| 6 | Close substantive conversations with a Return Package, change summary, and refresh list. | CS §4.6 / C-001 |
| 7 | Every exported artifact carries exactly one stable identity, `Fractal_<Name>_v<major>.<minor>`, authored in `.md`. *(Knowledge Graph Store node files are the identity system itself and are exempt — C-041.)* | CS §4.7 / C-005, C-041 |
| 8 | Continuity & primacy: the queued *Next* is genuine foreknowledge — expected when taken up, an offer (never a mandate) when not. The user's request is primary. | CS §4.8 / C-031 |
| 9 | **Reference copies govern nothing:** a session in a release copy (detached HEAD / release tag / any working copy that is not the instance's own governing repo) never closes, edits canon, writes the store, or pushes — reading, explaining, and GENESIS midwifery are its work. | CS §4.9 / C-091 |
| 10 | **Adopt before invent:** before building any new working tool or projection, check the shelf of specified components (GENESIS §5; here, this overview + the Architecture State) and offer the existing one first. | CS §4.10 / C-092 |

## 2 · Governance rules (how decisions and documents behave)

| Rule | Content | Source |
|---|---|---|
| Projection integrity | Projections are labelled, cite sources + date, never become authoritative. | C-003 |
| Update ordering | Conversation → canonical update → protocol (if needed) → context-package refresh. | C-004 |
| Living-document naming | Living projections: stable filename, internal version. Historical artifacts (protocols, return packages): dated/versioned filenames. **Canonical specifications are versioned artifacts — every substantive change is a new version, never an in-place revision.** | C-012, C-040 |
| Review act | Review = explicit in-conversation acceptance after a walkthrough; recorded in the next protocol; status flips Draft → Ratified. `Review Owner` until reviewed, dated `Reviewed By` after. | C-033 |
| Open-question ledger | The Decision Register's OQ section is cumulative; items enter with a disposition and leave only by recorded disposition. | C-034 |
| Reviews are canonical | Whole-project reviews are **canonical, sequential, dated** artifacts — DOC-minted, never revised; findings carry stable ids dispositioned in the Register's **review-findings ledger** (leave only by recorded disposition). Findings are observations; fixes are proposals until adopted by decision. **Two variants (C-103, OQ-20 resolved):** the **Loose-Ends Scan** (defect hunt, `S<n>-x.y`) and the **capture-review** (the verified whole-record account for transmission — staged multi-agent flights, adversarial verification of every claim against the primary record, assembly from cleared material only; `CAP<n>-…` ids; first member: the Vision-to-Horizon capture). | C-058, C-103 |
| Close checklist | Every close walks the **full living-projection table** — Register, Rule Overview, Local Context, Agenda Board, Context Index, Global Context, Architecture State, BOOTSTRAP, Schema→Template, store tools, **Conversation Settings (C-071)**, client adapters — **stamps *and* content (C-077):** stamps current with Settings, and any doctrine decision touching a surface's rules re-projects the affected adapter in the same close — each marked *changed* or *checked — unchanged*. Return Packages open *Next* with the auditable handover ("previous queued X; this did Y"); every close = Return Package + protocol **or recorded reason none**. | C-059 |
| Version agreement | The close ritual runs **both checkers**: `verify.py` (store fold, C-050) and `check_versions.py` (corpus: every `<Doc> vX.Y` claim vs the named document's internal stamp, plus path existence) — **red from either blocks the close**. Objective disagreements error; currency heuristics warn; frozen artifacts are exempt from currency, never from existence; the in-script SUPPRESS list stays short and Max-ratified. | C-060 |
| Stamped projections | Every client adapter carries a **governed projection** of Conversation Settings with a version stamp naming source + date; sync-check = stamp comparison (both root adapters machine-checked STRICT; **no vendor-held projection currently exists** — the pattern stands for any future vendor surface, C-089). Re-project, never hand-fork. | C-035, C-065, C-071, C-089 |
| Close mechanics | The **mechanical tail** of a close — changed-doc detection by hash, `revise` events, node sync, series ripple, gate runs — may be driven by **`close.py`**, the fourth canonized store tool (drone-built, C-066's second flight). Dry-run by default; judgment (prose, version bumps, protocols, ratification) is **never** delegated to it; both checkers remain the only gate. | C-073 |
| Settings upstream currency | The C-059 checklist carries a **Conversation Settings row**: every close asks whether any decision touched conduct (C-008 promotion, surface/writer change, close-ritual change) — if yes, Settings reissues **in the same close**, projections restamp after. **The source moves first**: projections are never brought current with reality while the source stays behind. | C-071 |
| Agenda Board | The board is a standing **stamped projection** of the Local Context's agenda — C-035 extended to an interface. Its source is the **repo file** `Context Packages/Fractal_Agenda_Board.html` (living projection, HTML by declared C-006 exception, not DOC-minted) — since the Cowork surface retired (C-089), the file is the **only** rendering; the republish step is gone. Refresh is **file-first** at any close that changes the agenda; never authoritative; nothing lives only in it. **And it is offered actively (C-102):** the agent offers the board by name, unprompted, when open threads accumulate — the speed asymmetry (AI works faster than human tracking; the board is the human's window); every instance's rail carries the same offer. **The format is a canonized standard (C-104):** `Fractal_Agenda_Board_Format_v0.1` (contract + section grammar + skeleton, instance-neutral); existing instances adopt by their own recorded decision, newborns receive it at genesis (rider queued at the next `genesis.py` touch). | C-047, C-048, C-035, C-089, C-102, C-104 |
| Concept foundations (the vision tier) | A maturing vision area lives in **one living canonical concept document** (`/Claude/Architecture/Concepts/<Area>/`, stable filename, version inside) — status **Draft, explicitly non-binding**: promotion into architecture or rules is by ordinary decision only, on a named trigger. Session captures stay frozen traces feeding it; **a public blueprint/whitepaper is a stamped C-035 projection of it**, never a second source. First member: `Fractal_Knowledge_Network_Foundation`. | C-086 |
| Field testing (the proving process) | Every new development that lands for real use — a release, a tool, a surface, a client — is proven by a **field run**: real use by its intended user, a pre-canon fieldnotes ledger (C-062) capturing frictions *and* green data at the moment they occur, appended run by run; the slate is triaged and **ingested at the next phase boundary**, fixes landing in the **generator** (kernel, next release), never as patches hand-applied to the field instance. One ledger per test subject. **The capture layer is standardized (C-100):** entries are immutable machine-format blocks (`Fractal_Fieldnote_Format_v0.1` — the expected form *is* the collection interface: a tester reports by sending the ledger file, any channel, no phone-home); `fieldnote.py` (the seventh store tool) is the deterministic capture + intake half; every newborn is born with the door (`/fieldnote` + `FIELDNOTES.md`, parameter 7 concrete). | C-094, C-100 |
| Remove on observed drag (compaction) | Minimal necessary complexity's mirror discipline: **living projections compact at phase boundaries** — accumulated narrative already frozen verbatim in Protocols / Return Packages / Scans is replaced by pointers to it. Lossless by construction (projections re-tell, never own). Trigger: **observed drag**, per-document — never a sweep for its own sake. First exercise: the Local Context (thirteenth session, 8,629 → ~2,300 words). | C-095 |
| Onboarding (the guided first loop) | A newcomer's first loop runs on **one canonical script** — `Fractal_Onboarding_Protocol` (stamp inside): the interview (five real questions, defaults stated not asked, known frictions pre-answered inline at their moment), the birth via GENESIS, the midwife's exit (the clone session never acts in the child), then the child-side **three-stage fade** — the genesis-written self-retiring rail offers **one move per loop** (C-031 as interface) and retires by loop 5. Projections (`/begin`, app first-run, website walkthrough) render from the script and never fork it. Instrumented under C-094. | C-101 |
| Distribution & the public home (release content) | **The curated mirror is the permanent public home (C-105, amending C-098's endgame):** the mother repo stays private; the mirror (`fractal-mirror`) is the standing public version — stable, standardized, versioned, the baseline every sovereign copy can return to. **Ownership resolves as proof-in-reserve:** publishing the original stays an always-available option; the priority proof is the anchor, not the publicity (C-090). **Two anchor chains:** the mother's (private, the priority proof) and the mirror's own signed tags + OTS receipts under the public identity (`fractal@knet.network` — a custody principal). Release curation lives **in-repo** as `.gitattributes` `export-ignore`; **personal-class material routes to the private annex by default** (`fractal-annex` — ungoverned, never shipped, never a birth source); what enters the mother ships publicly only through curated rebuilds. | C-098, C-105 |
| Licensing | **Free use, attribution preserved**, implemented as a split by content class: code Apache-2.0 · documents CC BY 4.0 · NOTICE carries attribution; **the name and marks are not licensed** (stated in the README beside the split). The terms **travel with every newborn** (genesis copies the license files; the Genesis Record + inheritance clause are the attribution). Personal pre-canon is not licensed for reuse. | C-099, C-090 |
| Lineage | ChatGPT-era material is pointer-only — cited, never renumbered into the C-series. | C-029 |
| Anchoring | Canonical docs in the folder; the context package (Global, Local Context, Index, Register, Rule Overview) is **read in-repo by every client** — the Project-spine mirror mechanism is retired, and since C-089 **no vendor-held artifact exists at all**; a future vendor client would carry exactly one stamped instructions-field adapter (C-035). | C-002, C-030, C-036, C-067, C-089 |
| Phasing | The Global current-realisation line changes only at genuine transitions (criterion TBD, OQ-4); the sequence is the phase history. | C-009, C-010 |
| Commit convention | Git author = the actual author of the change (Max or Claude), mirroring `AGENT.HUMAN.MAX` / `AGENT.AI.CLAUDE`; message = `[DOMAIN] imperative summary` (+ refs); one commit per coherent change-set — the commit **is** the C-025 event boundary. **The committer field is mechanical** — no governance meaning; attribution = author, custody = signature (C-074a). | C-037, C-074 |
| Bootstrap | The repo rehydrates from `BOOTSTRAP.md` at the repo root (ships in-repo, C-020), including the **client-adapter step**; acceptance = the "laptop in the ocean" test. | C-038 |
| Off-site copy | The off-site copy is a **private GitHub repository** (remote `origin`); the push **rides every close** (C-064 — executed by whichever writer holds the surface under Max's standing authorization; a mid-session commit reaches the off-site copy at its session's close push). The host is a C-018 swappable convenience — nothing hard-binds to it; second-machine clone deferred to observed need. | C-056, C-064 |
| Protection model | Custody is layered — device (FileVault) · account (2FA/passkey; tokens fine-grained/read-only/expiring) · authorship (the git author field **attributes, never authenticates**; signing **live since the first drone write** and **custody-scoped**: one key = Max's custody environment — a signature attests *where*, never *which writer*; verification bound via `.allowed_signers` at the root — **every signed commit verifies `G`**: zero bad/unknown signatures anywhere; pre-signing commits unsigned by design (unsignable without rewriting, C-037; scoped per Scan #3 S3-1.1, the drill checks the shape), and **one dated exception class in the signing era** (observed at flight 4, per v0.35) — a drone commit made in a full clone (C-082) arrives unsigned, its custody attestation being the *signed gated-merge commit*; forward cure: signing configured in the clone at launch; **per-writer keys arm at the first writer outside that custody**; GitHub `main` branch protection = dormant recommendation — rulesets unenforced on the private repo under the current plan (checked by Max, 2026-08-14); trigger: Team/org migration or a public flip) · knowledge layer (append-only + hashes + verify ritual + no-rewrite): alteration is **evident and reversible**, never hidden. Checklist: BOOTSTRAP §4. | C-057, C-074, C-075 |
| Secrets | **No canonical artifact ever holds a secret value** — store, log, governed docs, committed config; a secret value is not knowledge, it is access. The **fact** of a credential (name, purpose, scope, holder, lifecycle events) is recorded; the **value** lives in the environment layer (`.gitignore`d env / OS keychain — the ignore entries ship in the kernel). Kernel rule; the credential *inventory* is an instance value (the eighth parameter, GENESIS §2). **Deliberately non-durable:** excluded from the off-site copy by construction — rebuild = re-issue against the recorded facts. Guard: `doctor.py`, the sixth store tool (landed at flight 4, per v0.35). | C-087 |
| Provenance anchor (releases) | **Every shipped release tag is externally anchored at pack time:** an OpenTimestamps attestation of the tag's commit hash, the receipt committed into the repo — the repo carries its own proof; eIDAS / US-registration escalations optional at need. Ownership is automatic (Berne); the anchor closes the one real gap — proof of priority: after the first anchor, any copier's earliest provable date is later, forever. Kernel rule; the anchor-authority set is the instance value (**the ninth parameter**, GENESIS §2 row 9). First execution: the first shippable beta's tag. | C-090 |
| Document identity | A canonical document's persistent ID is its minted `DOC-…` node; the `Fractal_<Name>` is an alias over it (`kind: route` from 2026-08-13 — it is a machine token; corpus `label` bindings grandfathered). "Document ID: TBD" is retired; new canonical docs are minted at or shortly after first commit. Living-doc DOC titles are versionless — the version lives in the `revise` chain. **DOC identity mirrors file identity (C-061):** versioned artifacts (specs, protocols) get **one DOC per version file** with a version-labeled `route` alias; the specs' living ids remain as **series pointers** (versionless alias, hash tracks newest). A spec reissue = one series `revise` + one per-version mint. **Client adapters are canonical artifacts (C-065):** an in-repo adapter (`CLAUDE.md` = `Fractal_Claude_Code_Adapter`) carries identity, an internal version, a DOC mint, and STRICT checker scope — while its *content* stays a C-035 compression of Conversation Settings, the sole normative source (re-project, never hand-fork). Vendor-held adapters stay projections by impossibility. | C-041, C-042, C-045, C-061, C-065 |

## 3 · Knowledge-Graph format rules (how knowledge is recorded)

| Rule | Content | Source |
|---|---|---|
| Two relations, one log | Identity + Placement in one append-only Event Log; everything else is blocks added back. | C-014 |
| Identity ≠ context | A thing's identity is separate from where it sits; names and codes are aliases. The `TYPE-ULID` scheme is **confirmed** (2026-08-12). | C-015, C-039 |
| Node file | Markdown + YAML front-matter; required identity `{id, type, created, created_by}`; foldering is navigation, not identity. TOP nodes carry `code:` + `facet:` (log-wins). | C-022, C-040 |
| Event log | Append-only JSONL, one event per line, **one active file** — the 50,000-event tripwire is advisory, and only the recorded **roll ceremony** (merged state, one commit) ever opens a new partition (C-055, resolving OQ-3); verbs `{create, place, unplace, mint, alias, revise, run}`; alias kinds `{label, route, redirect}` — newest label = `title:`; route = machine token over an identity; redirect = superseded code. | C-023, C-045, C-055 |
| Log-canonical | The event log is authoritative; node `placements:` is a materialized fold of it — **and the fold resolves codes through redirect chains** (materialized state carries current canonical codes). | C-024, C-045 |
| Supersede, never edit | No edit/delete; correction = new event with `supersedes`; state = fold in `ts` order. Events at commit, not per keystroke — the git commit is the boundary. | C-025, C-037 |
| Identity format | Standard 26-char Crockford ULID; id prefix must decode into the `ts`/`created` second (coherence rule); generate with the method in the store README, never by hand. `content_hash` = sha256 of the node body (store) or the committed file (external doc); `revise.supersedes` = prior event id; `actor` = committing agent. | C-040 |
| Substrate | Plain text in git (repo *is* FRACTAL); indexes are derived, disposable caches; vendor surfaces are swappable clients. | C-018, C-019, C-020 |
| Multi-writer safety | Identity cannot collide (ULID); code collisions resolve **first-mint-wins** (`ts`, ULID tie-break) with a mandatory append-only cure; root mints carry a recorded call; **`verify.py` must pass at every store-touching commit/merge** (git merges JSONL textually — the semantic check is the ritual); `mint.py` makes local duplicates impossible. | C-049, C-050 |
| Attribution & authority (who wrote it, who authorized it) | **Author = whoever writes the bytes** (C-037). In this repo that is Claude; Max authors only what he writes himself. **Committer = whoever authorized *this* act** — semantic, not mechanical (C-084 amending C-074a): Claude when acting inside permissions already granted (a C-008 promotion such as the standing push), **Max when he actively commands or ratifies** (`/close`, *go*); a drone is author *and* committer of its own flight, since the brief is the grant. **Custody stays the signature** (C-074b, Max's key) — so *author Claude, committer Claude, signed by Max* is the expected shape for standing-authority work. Pre-C-084 committer fields are noise by design and are never retro-fitted. | C-037, C-074, C-084 |
| Live pre-canon (the notebook class) | Personal capture artifacts — Max's `Vision thought scramble.docx` — are **tracked** (versioned, backed up, pushed off-site) and **formally non-governing**: never a source, never orientation-loaded, outside both checkers, never in a close's change-set. That last follows from C-084 rather than from a mechanism: its author is Max, and a close never stages work Claude did not write. Distinct from C-062's *frozen* pre-canon, which is historical; this class is live. | C-062, C-085 |
| Instantiation (birthing an instance) | A new FRACTAL-governed project is born from the **tier-0 kernel** by `genesis.py` (`GENESIS.md` — BOOTSTRAP's forward sibling). Its Register opens with **one inheritance clause**, never copied rows, and numbers its own decisions under a distinct prefix. **`verify.py` alone is the gate** — `check_versions.py` is tier-1 and must never be demanded of a newborn (C-081). The **packaging unit is the adapter + `.claude/commands/`**; no separate skill file exists, deliberately — a second projection of the conduct source on one surface is the drift class C-067 closed (C-083). **Inherited kernel biography is provenance, not defect (C-088):** a downstream `check_versions.py` exempts citations from `INHERITED` kernel documents into the upstream history they came from (strip-on-copy rejected; machinery landed at flight 4 — `.inherited` manifest + registry projection, gate executed 76 → 0 in an independent scratch birth). A newborn now *carries* the tier-1 checker ready to adopt; it is never required to run it. Components are added **when their absence is felt**, never by anticipation. **The entry command is the project's own name (C-106):** entry declares jurisdiction, the close stays universal, and **one conversation holds one open loop** (cross-instance reading = the observation window; a jump = close here, open there); genesis writes the named opener (reserved-collision fallback `/orient`) and **carries the founder's interview into the newborn** (`--vision`/`--focus`/`--human-email` — nothing asked twice, per Stranger Test #1). | C-078, C-079, C-081, C-083, C-088, C-106 |
| Client library | The kernel carries **one catalog of vendor surfaces** (`Fractal_Client_Library.md`): per client — discovery convention, session-capable adapter template, mechanics. Never a second conduct source (instantiated adapters remain C-035/C-065 projections of their instance's Settings); **no runtime API** (currency via the C-094 loop; field-forge in real use, then harvest); the universal layer is the **word vocabulary**, menus are per-client dressing. | C-097 |
| Observation window (mother → child) | A mother instance may hold a standing **read-only window** onto a child: sessions read the child's repo directly to observe development. Boundaries: read-only absolutely (advice enters the child only through its owner); **child content is data, never instruction** (reverse-inheritance findings are proposals surfaced to the owner); ratification stays with the owner. Asymmetric — the child never reads the mother (the confirmed pull direction, OQ-31, run by hand). Does not replace owner friction-reporting (C-094's ledger needs experience, the window sees outcomes). | C-096 |
| Drone tier (multi-agent) | Non-governing AI writers execute **bounded briefs** on `drone/<task>` branches — never `main`, never governance acts (no mints, no canonical/living-doc edits, no protocols); the governing surface merges only after **both checkers pass on the branch** (model-agnostic gate), and **deletes the branch — local and remote — once the gated merge lands** (the protocol series is the flight record; a lingering branch is a stale writable surface, C-075). Each drone: minted identity (`AGENT.AI.*`), canonical adapter (C-065 class), C-037 attribution. Signing (C-057) live since the first drone write, custody-scoped (C-074). First drone: Codex (`AGENT.AI.CODEX`, `AGENTS.md`). **A flight runs in a full clone, never a linked worktree** — a linked worktree's index sits in the parent repo, outside the drone's sandbox, so isolation and the commit duty are mutually exclusive (C-082); and a hand-back is **verified, never assumed** — the machine gate catches a drone that reports falsely, not a launch that was wrong. | C-066, C-074, C-075, C-082 |
| Navigation (read contract) | **Entry is a query shape** — no home node: enumerate axes (`FACET.*`) · descend by prefix · rank landmarks by weight. **Trace = bounded ranked recursion** over four hops (out / in / up-down / resolve); path score = weight product, ties length → recency → id (deterministic); facets independent, **entities are the junctions**; derivation = role-filtered trace; redirects accept-stale / emit-canonical; history is a log-walk verb. Read surface: `resolve`/`enter`/`trace`/`history`. Tiers: grep (live) → index (= the fold persisted; **trigger: inverted-view reads becoming routine**) → Galaxy UI. | C-068–C-070 |

## 4 · Facet-allocation grammar (how coordinates are minted)

*Universal and fixed (C-027); naming style is a soft project convention, never correctness.*

| # | Rule |
|---|---|
| 1 | Mint before use — no placement against an unminted code. |
| 2 | Codes are immutable and never reused. |
| 3 | Unique under parent; **root segments are unique globally, across all facets** (C-044). |
| 4 | Label is separate from code (sign ≠ word). |
| 5 | Honest depth + weight — place where it truly belongs, weight `[0,1]` says how centrally. |
| 6 | Reparent by redirect, never delete: **re-mint on the same subject + `alias(kind: redirect)` on the old code; the fold resolves through the chain** (C-045). |
| 7 | Parentage is carried by the code prefix (one-grep subtree scans). |
| 8 | Routing codes derive from identity — **complete: all eight Index domain routes (`KG`/`GOV`/`GBL`/`CTX`/`AST`/`ONT`/`WS`/`PROT`) are live `alias(route)` bindings; facet tokens use the same mechanism** (C-046, C-053, C-043). |
| 9 | Collisions between concurrent writers resolve **first-mint-wins**; the cure is append-only (re-mint + re-point); root mints are deliberate — explicit flag + recorded call (C-049). Mint through the guard; verify at every store commit (C-050). |

Registry context: the facet registry is the **log-derived projection** of `TOP` concepts grouped by facet (C-026) — **including the facets themselves, minted under the `FACET` root in the meta-facet (token `facet`; C-043)**: `FACET.TOPIC`, `FACET.METHOD`, `FACET.SOURCE_TYPE`, `FACET.TIME`, `FACET.AGENT`. The agent facet branches by kind (`AGENT.HUMAN.*` / `AGENT.AI.*`). A new facet is a mint + token binding, never a schema edit. Placement targets are coordinates *or* entities — an entity target puts its `TYPE-ULID` in the `code` field (Schema §3.5). Topic roots minted so far: `KG`, `GOV`, `GBL`, `CTX`, `AST` (first C-049 root ceremony, C-053).

## 5 · Standing caveats (rules about the rules' limits)

- **C-038 acceptance test exercised green (2026-08-13)** — first push done (off-site copy live); cloud-session clone + §1 rehydrate + `verify.py` PASS: "laptop in the ocean" is demonstrated fact. **The drill's cadence is decided (C-076, 2026-08-14): it rides every canonical Loose-Ends Scan** plus an ad-hoc trigger on any custody/host change. The **integrity & safety remainder** has narrowed to OQ-27's outside-custody scope + OQ-20's heavier review variant (OQ-25/OQ-26 resolved at the custody session, per v0.28). **Whole-repo verification / projection drift is mechanized since 2026-08-14** — the version-agreement checker (C-060) runs at every close.
- Full open-question ledger: the Decision Register (stamp inside, C-012), OQ-1–OQ-32 (OQ-24 resolved by C-060; OQ-10 by C-063; OQ-6 by C-067; OQ-25/OQ-26 by C-076/C-074–C-075 at the custody session; **OQ-13 closed permanently — machine-blocked (C-080); no remaining trigger**; **OQ-16 resolved fully at the punch-list close** — packaging by C-083, the vendor-held half mooted by C-089; **OQ-17 calibrated by observation, OQ-18 resolved as no-auto-derive, OQ-30 resolved as C-087 — all per v0.35**; OQ-27 carrying its doctrine — C-066 tier, C-074 custody semantics, C-075 lifecycle — open for the outside-custody world; OQ-9 stands alone again, annotated per v0.35). **Standing open: OQ-4, OQ-9, OQ-27, OQ-28, OQ-29, OQ-31, OQ-32** — every one on a named trigger or an arc (OQ-23 resolved by C-093 per v0.39; **OQ-20 resolved by C-103 per v0.46** — the capture-review is the heavier variant, proven by first execution). The review-findings ledger lives there too (C-058) — **S3-2.2 held open deliberately** (per v0.31: `Reviewed By: Max` cannot be supplied on Max's behalf, and reissuing two kernel specs for a header field adds weight where OQ-28 says weight is the risk; its OQ-18 bundle dissolved per v0.35 — the trigger stays each spec's next *real* reissue), **plus Capture #1's rows** (`CAP1-…`, per v0.46: 5 High open on named triggers — the friend's-thread consent call the flip-gating one — and the grouped Medium/Low tranches; ADHD resolved accepted-exposure, ANCH-1 fixed). Pre-canon vision material **informs, never governs** — its influence is captured in protocol reasoning (OQ-22 disposition), and the ruling is visible on disk since C-062 (BOOTSTRAP §0 line).
- **WS is the declared open part of the architecture** — no Claude-era canonical yet; forge on observed need (Architecture State §4).

---

**Refresh triggers:** any change to Conversation Settings, a new C-decision that adds/retires a rule, or resolution of a standing caveat.
**Sources:** Fractal Global Context; Conversation Settings v0.8; Governance Protocols — Claude Series v0.1–v0.46; Knowledge Path Foundation v0.1; Node & Event Schema v0.6; Navigation Contract v0.1; Fractal_Architecture_State (stamp inside, C-012); Decision Register (stamp inside, C-012); BOOTSTRAP.md (stamp inside, C-012); GENESIS.md (stamp inside, C-012); Loose-Ends Scans #2 (2026-08-14), #3 & #4 (2026-08-15); Capture_2026-08-16_Vision-to-Horizon.md (C-103).
**Revision history:** v0.1 (2026-08-12) first consolidation, five layers, all rules traced. · v0.2 (2026-08-12) joined the context package by Max's decision (C-036, per Protocol v0.8); added the substrate-realization rules (C-037, C-038). · v0.3 (2026-08-12) identity & schema consolidation (per Protocol v0.9): C-015 caveat discharged (C-039); identity-format and document-identity rows added (C-040/C-041); store exemption noted on rule 7; topic roots updated; own Document ID recorded. · v0.4 (2026-08-13) facet-layer completion (per Protocol v0.10): grammar rules 3/6/8 updated (C-044/C-045/C-046); alias kinds registered on the event-log row; fold-resolves-redirects on the log-canonical row; facet minting in the registry context (C-043); unbound-routes caveat added; versionless DOC-title practice noted. · v0.5 (2026-08-13) Agenda Board row added (C-047, per Protocol v0.11) — the C-035 stamped-projection rule exercised at an interface surface. · v0.6 (2026-08-13) board row updated (C-048, per Protocol v0.12) — repo-resident file, artifact demoted to display surface, file-first refresh. · v0.7 (2026-08-13) multi-writer safety (C-049/C-050, per Protocol v0.13): multi-writer row added to §3; grammar rule 9; single-writer caveat retired from §5. · v0.8 (2026-08-13) workstream F (C-051–C-054, per Protocol v0.14): rule-3 row reworded to the Global → Local spine; grammar rule 8 complete (all eight routes bound); AST root added; unbound-routes caveat retired; WS-open caveat added. · v0.9 (2026-08-13) event-log layout (C-055, per Protocol v0.15): event-log row — one active file, advisory tripwire, roll-by-ceremony; OQ-3 resolved in the ledger caveat. · v0.10 (2026-08-13) off-site copy & protection (C-056/C-057, per Protocol v0.16): §2 rows added; C-038 caveat re-armed (runnable after first push); ledger caveat → v0.12, OQ-22 opened. · v0.11 (2026-08-13) OQ-22 resolved (Max's call: pre-canon material informs, never governs); ledger caveat → v0.13. · v0.12 (2026-08-13) C-038 drill: armed-caveat retired from §5 (test exercised green — first push + cloud-session clone-rehydrate + verify PASS); integrity & safety remainder recorded as a standing offer; ledger caveat → Register v0.14. · v0.13 (2026-08-14) loose-ends pass (C-058/C-059, per Protocol v0.17): reviews-canonical and close-checklist rows added to §2; ledger caveat → Register v0.15, OQ-27; sources cured to current versions. · v0.14 (2026-08-14) checker canonized (C-060, per Protocol v0.18): version-agreement row added to §2; integrity remainder updated (whole-repo verification mechanized); ledger caveat → Register v0.16. · v0.15 (2026-08-14) spec-version identity (C-061/C-062, per Protocol v0.19): document-identity row extended — DOC identity mirrors file identity, per-version DOCs for versioned artifacts, series pointers kept; pre-canon ruling visible on disk; ledger caveat → Register v0.17. · v0.16 (2026-08-14) first Code session (C-063/C-064, per Protocol v0.20): sources-currency cure — caught by `check_versions.py`'s first red run at this close and cured in-close; ledger caveat → Register v0.18, OQ-10 resolved; no rule added or retired. Postscript (same conversation): the BOOTSTRAP citation made version-agnostic (stamp-inside, C-012) at the `CLAUDE.md`-adapter landing. · v0.17 (2026-08-14) adapter canonization (C-065, per Protocol v0.21): document-identity row extended — client adapters are canonical artifacts; Register citation made version-agnostic (same churn-class cure); ledger caveat → Register v0.19. · v0.18 (2026-08-14) the drone doctrine (C-066, per Protocol v0.22): drone-tier row added to §3; ledger caveat → Register v0.20. · v0.19 (2026-08-14) mirrors retired (C-067, per Protocol v0.23): anchoring row reworded — read-in-repo replaces the Project-spine mirror; ledger caveat → Register v0.21, OQ-6 resolved. · v0.20 (2026-08-14) the navigation close (C-068–C-071, per Protocols v0.24–v0.25): navigation row added to §3 (entry as query shape; the trace contract); rule-5 row gains the recorded-promotion mechanism; stamped-projections row generalised to per-surface adapters; Settings-upstream-currency row added to §2; close-checklist enumeration gains the Settings row; sources cured to v0.5/v0.6/v0.25. · v0.21 (2026-08-14) Cowork write capability (C-072, per Protocol v0.26): no rule added or retired — sources currency; the C-071 tripwire's first live firing recorded. · v0.22 (2026-08-14) close.py canonized (C-073, per Protocol v0.27): close-mechanics row added to §2 — the mechanical ripple automatable, judgment never delegated; ledger caveat → Register v0.24. · v0.23 (2026-08-14) the custody session (C-074–C-076, per Protocol v0.28): commit-convention row gains the committer-is-mechanical clause; protection-model row rewritten — custody-scoped signing live with verification bound (`.allowed_signers`, history-wide `G`), per-writer keys trigger-armed, branch protection a standing recommendation; drone-tier row gains the branch lifecycle (delete after gated merge); §5 drill caveat resolved to the rides-the-Scans cadence; ledger caveat → Register v0.25, OQ-25/OQ-26 resolved. · v0.24 (2026-08-14) custody-session postscript: the branch-protection recommendation checked by Max — dormant (rulesets unenforced on the private repo under the current plan; trigger: Team/org migration or public flip); no rule added or retired. · v0.25 (2026-08-15) the Scan #3 session (C-077, per Protocol v0.29): close-checklist row widened to adapter **stamps + content** (C-077); protection-model row's custody claim scoped to the signing era (S3-1.1 cure); §5 ledger pointer made version-agnostic (S3-1.2 — the tolerant-zone specimen cured by the C-012 stamp-inside rule); OQ-13 resolution and the one open finding (S3-2.2) entered in the §5 caveat. · v0.26–v0.28 (2026-08-15, per Protocols v0.30–v0.32): instantiation rows (C-078/C-079/C-081/C-083), the drone row's C-082 amendment, the attribution & authority row (C-084 amending C-074a), and the live pre-canon row (C-085) — entries consolidated here retroactively; the versions were bumped at their closes with the rows landed, and the history line lagged (observed at the v0.29 edit, cured in the same pass). · v0.29 (2026-08-15) the postscript protocol (C-086, per v0.34): **concept-foundations row added to §2** (the vision tier — one living Draft concept document per area, non-binding, blueprint = C-035 projection); §5 ledger range cured to OQ-32. · v0.30 (2026-08-15) the punch-list close (C-087–C-089, per v0.35): **secrets row added to §2** (values never canonical, facts recorded, env-held, non-durable — C-087); instantiation row gains the **C-088 inherited-provenance clause**; stamped-projections, anchoring, and Agenda Board rows updated for the **Cowork retirement** (C-089 — no vendor-held artifact exists; the walk fully repo-verifiable); §1 source pointer → Settings v0.7; §5 ledger caveat updated (OQ-16/17/18/30 off the ledger; nine OQs standing, all on triggers or arcs). Same close, after the flight landed: secrets and instantiation rows moved to *landed* (flight 4 gated and merged — `doctor.py` the sixth tool, 76 → 0 in a scratch birth); protection row gains the full-clone drone-commit exception (custody attestation = the signed gated merge). · v0.31 (2026-08-15) Scan #4, the pre-pack gate: **provenance-anchor row added to §2** (C-090 — releases externally timestamped at pack time, receipt committed; the ninth parameter); off-site row aligned to the practiced ritual (the push rides every close, C-064 — S4-1.5); §5's frozen next-Scan parenthetical dropped (S4-1.3); sources gain Scan #4. · v0.32 (2026-08-15) the phase-5 ingestion (C-091/C-092, per Protocol v0.38): **§1 gains rules 9 and 10** — reference copies govern nothing (the shipment-doesn't-know-it's-a-shipment cure) and adopt before invent (the hidden-shelf cure); §1 source pointer → Settings v0.8, rules 1–10. Both rules are kernel conduct, born from the first shipping run's fieldnotes on Max's "full slate as recommended". · v0.33 (2026-08-16) the second-client-trial session (C-094/C-095, per Protocol v0.40): **§2 gains the field-testing row** (the fieldnotes loop generalized — field run, ledger, ingestion at phase boundary, fixes in the generator) **and the remove-on-observed-drag row** (compaction of living projections at phase boundaries, lossless via the frozen tier; first exercise the Local Context). Settings unchanged — both are governance-process rules, not conversation conduct (the C-084/C-090 precedent class). · v0.34 (2026-08-16) the postscript (C-096, per Protocol v0.41): **§3 gains the observation-window row** — mother reads child read-only, data never instruction, ratification with the owner; the pull's manual tier. Settings unchanged — the window grants no executable authority; it bounds an existing capability. · v0.35 (2026-08-16) the second postscript (C-097, per Protocol v0.42): **§3 gains the client-library row** — the vendor-surface catalog, no runtime API, field-forge-then-harvest, word-universal vocabulary. Settings unchanged — governance infrastructure, not conversation conduct. · v0.36 (2026-08-16) the flip-preparation session (C-098–C-102, per Protocol v0.45): **§2 gains three rows** — onboarding (the guided first loop with the fade, C-101), distribution & the flip (raw-repo posture, phased mirror, the annex routing rule, in-repo curation — C-098), licensing (Apache-2.0 + CC BY 4.0 + name-not-licensed, terms travel — C-099); the **field-testing row gains the standardized capture layer** (the Fieldnote Format + the seventh store tool + the kernel door, C-100); the **Agenda Board row gains the active-offer clause** (C-102 — the speed asymmetry). Settings unchanged — all five are governance-process/artifact rules (the C-094/C-095 precedent class); the two conduct-adjacent candidates of the session (suggestion hygiene; the board offer as a conduct clause) are banked for the next conduct-touching Settings reissue. · v0.37 (2026-08-16) the capture session (C-103–C-104, per Protocol v0.46): the **reviews-canonical row gains the capture variant** (the C-058 family's second member — the verified whole-record account, adversarially checked; OQ-20 resolved by first execution) and the **Agenda Board row gains the format standard** (C-104 — `Fractal_Agenda_Board_Format_v0.1`, instance-neutral, genesis rider queued); §5's standing-open list cured to the seven current rows (OQ-23's stale listing fixed in-passing — CAP1-LAW-1) and the Capture #1 findings referenced. Settings unchanged — both decisions are governance-artifact rules (the C-094/C-095 precedent class); a third conduct candidate (always-flag-findings, Max's directive of record) joins the banked pair. · v0.38 (2026-08-17) the publishing-identity session (C-105–C-106, per Protocol v0.47): the **distribution row rewritten to the public-home posture** (mirror-as-home ratified — two anchor chains, proof-in-reserve, the public identity; the annex rule survives unchanged) and the **instantiation row gains the named-opener clause** (entry declares jurisdiction, close universal, one open loop per conversation, the interview carry per Stranger Test #1). Settings unchanged — distribution posture and command-tier doctrine (the C-084/C-090/C-098 precedent class); the loop rule joins the banked candidates as the fourth.
