# Fractal Governance Protocol — Claude Series v0.29

**Topic:** The Scan #3 session — the third canonical Loose-Ends Scan issued and minted (8 findings: 5 Medium, 3 Low, **0 High** — the smallest of the series), the **first Scan-riding restore drill executed green** (C-076), **OQ-13 resolved** (Max: "OQ-13 A" — the grandfathered filenames stay; re-fire at the first public-facing boundary), the **full fix slate adopted** (7 of 8 in-pass; S3-2.2 open on a named trigger), and **C-077** minted — the C-059 client-adapter row widened from stamps to **stamps + content** (the S3-3.2 structural cure, exercised the same close: AGENTS.md → v0.4).
**Status:** Ratified (2026-08-15 — ratified in the conversation it records, per C-033) · **Date:** 2026-08-15 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.28

---

## 1. Context

Fourth Claude Code session, opened by `/fractal` on a queued-empty handover. Max pulled Scan #3 forward from its pencil ("let's do the 3rd scan" — C-031: the pencil was an offer; his request is primary) and asked the surface question — "should we send codex or fable?" The recommendation of record: **Fable, on the governing surface** — a Scan *is* the review layer (sending the reviewer to a drone inverts the C-066 gate), Scan #3 carries the custody drill (C-076 — custody work belongs in Max's custody environment), and OQ-13 rides it needing interactive calls. Max: **"go."** The session ran sweep → drill → OQ-13 → write-up; the scan was delivered checker-green before any call was asked.

## 2. Evidence of record (what the scan found, 2026-08-15, at HEAD `98e2952`)

- **First scan under full machine guard, and the smallest findings list of the series** (26 → 24 → 8). The thesis the evidence supports: **drift now lives exactly in the checkers' declared blind spots** — tolerant-class body text, filename tokens, absence-of-claims, the `.claude/` tree outside the walk, adapter content behind a stamps-only checklist row. Each guard, once built, defines the next drift surface at its own boundary.
- **The sharpest specimen (S3-1.1):** the custody session's flagship claim — *"the whole history verifies `G`"* — false as stated: `git log --format='%G?'` shows **16 `G` / 38 `N` / zero `B`·`E`·`U`**; the 38 predate signing and are unsignable without rewriting (C-037). Protocol v0.28's own §2 had the evidence correctly *scoped* ("since the C-066 arming"); the conclusions generalized it. The truth is strictly stronger than the claim.
- **The drill (first Scan-riding execution): green, every step** — fresh SSH clone, HEAD parity exact, all 11 §1 paths resolve, the §4 rebind works as documented for a stranger, signature distribution identical to source, **both checkers PASS in the clone** (the S2-3.4 content check riding a drill for the first time). The drill's signature step is what surfaced S3-1.1 — the cadence doing exactly what C-076 wanted.
- **Residue: 27 of 28 prior findings verified holding** against sources; S2-2.2's forward half ("Schema at its next reissue") regressed silently at its named trigger and re-entered as S3-2.2 — exposing the class: Fixed dispositions with pending forward-commitments had no tripwire.
- Full findings, evidence, and per-finding fixes: `Review_2026-08-15_LooseEnds-Scan-3.md` (canonical, C-058).

## 3. Decisions of record

- **C-077 — Adapter-content currency (amends the C-059 checklist; the S3-3.2 structural cure).** The client-adapter row widens from *stamps* to **stamps + content**: every close asks (a) are all adapter stamps current with Settings, **and** (b) did any decision of this close touch a surface's doctrine (drone rules, custody infrastructure, command procedures) — if yes, the affected adapter **re-projects in the same close**, source-first per C-071. Rationale: AGENTS.md sat content-frozen at v0.1 through C-074/C-075 — its forbidden-paths list predating the custody layer it operated under — because stamps only move when Settings moves; doctrine outside Settings had no tripwire. First exercise: this close. *(Working Decision.)*
- **OQ-13 resolved (Max's call, no new number — the OQ-22 pattern: a recorded call that changes no rule).** The three grandfathered `_v0.1` filenames (Global Context, Context Index, Conversation Settings) **stay**; C-012's grandfathering is affirmed. Measured basis: ~36 in-scope references bind the names, **15 in frozen artifacts** — a rename permanently orphans the frozen layer's pointers for an aesthetic gain with no current audience (every reader resolves through the Index). **The rename question re-fires as an ordinary decision at the first public-facing boundary** — public flip, curated mirror, or the OQ-16 shipping/packaging pass, whichever first (the OQ-26 resolve-with-named-triggers precedent).
- **Fix slate adopted (Max: "Full slate (Recommended)").** Seven findings fixed in-pass — S3-1.1 (custody wording scoped in Register/Rule Overview/Local Context + the machine-checkable `%G?` drill shape in BOOTSTRAP §4), S3-1.2 (Rule Overview §5 pointer version-agnostic), S3-1.3 (store README: spec pointers current, `close.py` listed, Navigation Contract heads the trace ritual), S3-2.1 (Architecture State §4 → the C-068 posture), S3-3.1 (both command files stamped; `check_versions.py` walk extended to `.claude/commands/`), S3-3.2 (AGENTS.md → v0.4 content re-projection; the structural half is C-077), S3-3.3 (BOOTSTRAP §2 records the landed command tier). **S3-2.2 deliberately open** on its named trigger (the `Reviewed By` fields land at each spec's next reissue — no out-of-cycle edits to frozen versioned artifacts); its class note — *forward commitments stay Open with a named trigger, never Fixed on a promise* — adopted as standing ledger practice.
- **Findings on the agenda (Max's directive, mid-session):** the Scan #3 findings are carried as a standing block on the Agenda Board and in the Local Context's agenda until dispositions change — the board's first findings section.

## 4. Executed this close (one `[GOV]` commit, C-037; push = Claude, C-064)

- **Fix executions** as listed in §3 across: Decision Register → **v0.27** (C-077; OQ-13 resolved; S3 ledger seeded 7 fixed / 1 open; C-074 row wording scoped; forward-commitment practice adopted) · Rule Overview → **v0.25** · BOOTSTRAP → **v0.14** · Architecture State → **v0.8** · `AGENTS.md` → **v0.4** · store README (pass) · `check_versions.py` (scope line — tested green in place) · both `.claude/commands/` files (stamps) · Context Index → **v0.21** (Scan #3 in the reviews row; PROT range → v0.29).
- **Scan #3 finalized and minted:** status → Ratified (in-conversation), DOC-minted via `close.py --create` (the hardened `MINTED:` flow's first live use); Protocol v0.29 (this document) minted the same way.
- **Local Context → v0.40** (fourth-session block; **the Scan #3 findings block on the agenda** per Max's directive); Agenda Board regenerated file-first (stamp v0.40, findings section added); Return Package issued.
- **Checked — unchanged:** Global Context v0.3 · Schema v0.6 / Template v0.4 / Navigation Contract v0.1 (S3-2.2 waits for their reissues by design) · `CLAUDE.md` v0.3 (stamp current; no Code-surface doctrine change — the command stamps live in the command files) · **Conversation Settings v0.6 — the C-071 row walked: no conduct-touching decision** (C-077 changes what the *checklist* asks, in the checklist's normative home — the Local Context; no Settings sentence covers row composition, so no reissue; recorded reasoning, not a skipped row).

## 5. Calls recorded (Max, 2026-08-15, this conversation)

1. Commissioning: **"let's do the 3rd scan. should we send codex or fable?"** → recommendation Fable → **"go."**
2. OQ-13, after the worked options (§5 of the scan): **"OQ-13 A."**
3. Fix adoption, from the presented slate: **"Full slate (Recommended)"** — all seven riding fixes + C-077; S3-2.2 deferred to its named trigger.
4. Agenda directive, mid-session: **"put the findings of scan #3 on the agenda."**

## 6. Ratification record (2026-08-15, in-conversation per C-033)

Max commissioned the scan, chose the executing surface on the recorded recommendation, received the full findings + drill record + worked OQ-13 options before any call was asked, and made all three calls explicitly (surface, OQ-13, slate — plus the agenda directive). The governing surface executed the slate, verified both checkers green at source and in the drill clone, and issued this record. Ratified in the conversation it records; no caveats.

## 11. Open Questions (TBD)

*(Per C-034 these are entered in the Register's cumulative ledger.)*

- **OQ-13 resolved** (keep + public-boundary re-fire trigger — §3). **None opened.** The review-findings ledger holds **one open row: S3-2.2** (named trigger: the specs' next reissues) — the first standing open finding since the ledger went fully green 2026-08-14; deliberate, not leaked (the S3-2.2 class note is the guard). Standing OQs otherwise unchanged: OQ-4, OQ-9, OQ-16, OQ-17, OQ-18, OQ-20, OQ-23, OQ-27 (outside-custody scope).

---

| Field | Value |
|---|---|
| Document Title | Fractal Governance Protocol — Claude Series v0.29 |
| Document Type | Governance Protocol (historical artifact, dated identity — C-012) |
| Version | v0.29 |
| Status | Ratified (2026-08-15, in-conversation per C-033) |
| Domain | GOV |
| Prepared By | Claude |
| Reviewed By | Max (2026-08-15, this conversation) |
| Date | 2026-08-15 |
| Parent Protocol | Fractal Governance Protocol — Claude Series v0.28 |
| Related Documents | `Review_2026-08-15_LooseEnds-Scan-3.md` (canonical review, C-058); Decision Register (stamp inside); Rule Overview (stamp inside); BOOTSTRAP.md (stamp inside); Architecture State (stamp inside); `AGENTS.md` (stamp inside); `.claude/commands/fractal.md` + `close.md` (stamped-procedure projections); Protocols v0.17 (C-058/C-059), v0.24 (C-068), v0.27 (C-073), v0.28 (C-074–C-076) |
| Document ID | DOC-01M0188YSWDWQE5Y2TP45E0YSE (minted 2026-08-15 via `close.py --create` — the hardened `MINTED:` flow; stamped by the post-mint revise, the v0.27/v0.28 precedent) |
