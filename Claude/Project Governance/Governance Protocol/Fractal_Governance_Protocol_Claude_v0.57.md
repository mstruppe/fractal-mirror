# Fractal Governance Protocol — Claude Series v0.57

**Topic:** The twenty-third session (2026-08-18) — **the go-live session: the value model captured, the Network Foundation's descent + serving revision, and THE FLIP EXECUTED — FRACTAL public on both addresses.** Opened `/fractal` on the post-engine-sketch queue; Max opened with the value-model capture, explored two vision arcs (the classical-transfer/server idea; the descent — FRACTAL splitting into itself), then called the go-live on observed necessity (*"I want to be able to send a link"*) and drove it end to end: the mirror flipped public by his hand, the site served, the gate built and bound to `knet.network`, the intake layer installed. One new number (**C-120**); the C-086 concept tier gains its third member; the Global Context records its **second realisation transition** (the C-105 attachment fired).
**Status:** Ratified (2026-08-18 — the captures on Max's explicit words ("go with 1", "ratified"), the go-live on his command ("let's go live with the mirror and the website") and executed through his own hands at every recorded gate; the close fired on his `/close`) · **Date:** 2026-08-18 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.56 · **Document ID:** DOC-01M0ARQ8WEQJTQVYYTZ4MX2T6M (minted at that session's close via close.py --create; the stamp landed 2026-08-18 per Scan #5 S5-2.2 — the paired revise)

---

## 1 · The session's acts (each on Max's word, in order)

**(a) The value model captured (his "go with 1"):** `Fractal_Value_Model` issued — the **C-086 concept tier's third member** (Draft, non-binding, `/Claude/Architecture/Concepts/Value Model/`): three live branches (the product · the website-as-hub · the social-media branch, declared unbuilt and trigger-armed) + the horizon held as pointer to the Network Foundation (his "shooting star") + **token economics as first-class constraint** (his exclamation mark: *value scales with token spend — FRACTAL could succeed into unaffordability*; the cost-curve question armed on existing flight data). His framings preserved verbatim; deliberately a **value promise, not a business model** — no branch carries cash flow.

**(b) The value model given its public page (his direction):** `Site/value-model.html` — the model as one article in the house style, reachable as the **first rider of the Blog subnav** ("Value model"); the page a stamped C-035 projection of the concept document, its of-record note pinned to the document version. Verified in-browser at build.

**(c) The descent + serving revision (his "ratified"):** the Network Foundation → **v0.4**, two sections appended (no renumbering — frozen citations of §§8–9 verified intact first): **§10 vertical composition** (FRACTAL splits into itself: inherited Globals, removal-of-context-per-layer as the load-bearing scaling move, the upward aggregate interface as new artifact class, custody-per-layer; the honest split — **enterprise composes by hierarchy, the economy by federation**) and **§11 the serving layer** (three tiers; the classical-transfer principle — deterministic selection outside the context window, the cost curve's strongest lever, tied to R-12's denominator; the enterprise shape with **sovereignty answered as doctrine: customer-owned deployment, anti-SaaS by constitution**; four armed security constraints, **personal-data-never-in-canon flagged as a promotion candidate** at the next conduct-touching decision). The Value Model → v0.2 same session (cross-reference pointers only). Both explorations Max's, from first principles, in-conversation; the corrected mechanism recorded: models read code and prose at the same token price — the efficiency is computation outside the context window.

**(d) THE GO-LIVE (his "let's go live with the mirror and the website … just because we're live doesn't mean we are official"):** fired as observed necessity — feedback needs a sendable link; live ≠ official held throughout (FRACTAL the public name, OQ-34 untouched). Executed across the session in the recorded hand-split — Claude prepared, **Max executed every publishing gate himself**: → **C-120** (§2). The mirror **public** (his visibility flip — the single manual act C-116 always reserved to him); the site served from the mirror's new **`gh-pages` orphan branch** (the five-page layout + value-model page + `commands/`/`standards/` downloads + index redirect; `main` keeps the pure release rhythm); the **intake layer** on `main` (issue forms in the Fieldnote Format's two classes — friction + green datum, the C-094 loop's public face; config routing conversation to Discussions; the PR template stating the **one-way flow**: proposals enter the gate, land upstream, ship credited in the next anchored release); **the gate** — his redirection of the domain plan mid-flight: *knet.network is the umbrella's home, to be built by KNet later; for now a deliberately minimal link tree* — one card per project, FRACTAL the first, presented as **an address, not a brand** (OQ-34-conform); built as its own small public repo (`mstruppe/knet-gate`, outside FRACTAL's jurisdiction, future assignment to the umbrella); DNS at Cloudflare (four apex A records + `www` CNAME, grey-cloud DNS-only), HTTPS enforced. **End state verified end to end from a cold visitor's path:** `knet.network` (200, valid cert) → card → `mstruppe.github.io/fractal-mirror/` (all 24 files 200, console clean) → repo (public, anchored chain readable). **Settings decided in the walk-through (his calls):** Issues on · Discussions on · Sponsorships off · Projects recommended off (C-092 — the board is the one tracking surface) · merge methods locked to **merge commits only** (squash/rebase off — C-084/C-025 conformance) · auto-close-issues-on-merge off (dispositions are recorded acts, never automation) · no web-UI commits ever (custody; the mechanical cure "require signed commits" rides the branch-protection ruleset). **The Impressum shipped as placeholder by his knowing call** (§5 DDG surfaced three times, decision his: the address lands when home-address-vs-service is decided). **Release immutability + Releases** explained, adoption open. **The community-wiki concept banked as Q3** (wiki as open forge floor → verification gate → registry/site shelf; the public twin of A1; Discussions the safe first version; deliberately not enabled — build on observed community).

**(e) The parked agenda resolved at the tail (his two rulings):** **Q2 (multi-project hopping) answered in-session and signed off — deliberately not carried as agenda** (his diet call: the frozen record holds the mechanic — one folder = one project = one named opener, the directory decides jurisdiction, wrong openers fail harmlessly, never hop mid-session; natural re-trigger: the website/guide refinement lane). **A1 (the harvest channel) RATIFIED as the queued agenda step:** the landing zone under the Knowledge Network concept home for concepts developed in KNet — arrivals pre-canon (C-062) and data-never-instruction (C-096), provenance lines mandatory, standardization mother-side by ordinary decision → blog (C-119) → next release; open design calls recorded (general per-child convention vs. KNet-specific; wrapper header template). Relations: OQ-31's manual dry run, Network Foundation §6 made physical, Q3's private twin.

**(f) Observed in passing (recorded, no act):** the J-Space Cognition Suite specimen (1.9k-star prompt-layer cousin — session-scoped discipline vs. FRACTAL's infrastructure-scoped memory; its README concedes the persistence seam; takeaways noted: skill-shaped registry entry as distribution move, `CITATION.cff`, prose-with-CI convergence). The favicon 404 finding (cosmetic; the Kernel mark queued for the next site push).

## 2 · C-120 — the go-live (the flip executed; the gate model; the serving split; the intake layer)

**FRACTAL is public — the flip fired on observed necessity (C-107 honored: a real need — sendable feedback links — not a date), executed 2026-08-18 with every publishing gate Max's own hand; live ≠ official is the posture of record (no announcement, no name change, OQ-34 asleep).** The standing shapes this decision ratifies:

1. **The serving split:** the public site is served by GitHub Pages from the mirror's **`gh-pages` branch**; the mirror's `main` remains the pure anchored-release rhythm (C-116 untouched). Site refreshes are ordinary `gh-pages` pushes at any pace — **pull before push** (GitHub commits `CNAME` there), and the release rebuild must preserve `.github/` and `gh-pages` (joins the banked C-116 cleanliness check on the mechanization slate).
2. **The gate model:** `knet.network` is **the umbrella's address, not FRACTAL's** — a deliberately minimal link tree (address · cards · footer), one card per project, FRACTAL the first; the gate presents an address, never a brand (OQ-34-conform); its repo (`mstruppe/knet-gate`) stands outside FRACTAL's jurisdiction, assigned to the umbrella when it forms (direction of record: built by KNet later).
3. **The intake layer:** public issues are the **off-machine fieldnote door** (forms in the Fieldnote Format's two capture classes); PRs are **proposals, never merges** (the one-way flow stated in-template; cures ship upstream, credited); merge methods merge-commit-only; no web-UI commits; dispositions recorded, never automated.
4. **The Impressum disposition:** shipped as placeholder by Max's explicit, informed call (§5 DDG stated three times on the record); completion is his pending act (home address vs. Impressum service).
5. **The C-105 attachment fired:** *the public home goes live* → the **Global Context §2 records its second realisation transition** (v0.5, this close). C-075's branch-protection trigger fired the same day — ruleset recommended in-session (`main` + `gh-pages`, block force-push/deletion, require signed commits), **enablement unconfirmed → pending on Max**.

## 3 · Ripple

Register → **v0.75** (C-120 row; OQ-34 annotated — the gate live as the umbrella's address, address-not-brand; revision rows) · Rule Overview → **v0.45** (the serving-split/intake row beside the mirror rider) · Context Index → **v0.44** (the shipping-page row: LIVE on both addresses, the serving split; the concept-tier rows: Value Model added, Network Foundation brought to v0.4) · Global Context → **v0.5** (the second realisation transition — the C-105 attachment executed) · Local Context → **v0.74** (row 23; aspect line, state, agenda, pending rewritten to the live posture; checklist walked) · Agenda Board restamped (twenty-third chip; A1 ratified-queued; Q2 disposition; pending list re-pointed) · `Return_Package_2026-08-18_Go-Live.md` issued · **New canonical:** `Fractal_Value_Model` (DOC-minted at this close via `--create`) · Network Foundation v0.4 revise event. Working-tree deltas beyond governance: `Site/value-model.html` new, `Site/blog.html` subnav rider. **Mirror-side (public, Max-pushed):** `gh-pages` opened (`21c3836`-class site set), `.github/` intake on `main` (`19c2f0a`); **gate-side:** `mstruppe/knet-gate` born (`21c3836`), Pages + DNS + HTTPS live. No pack — beta-0.6 still forms on the C-110/C-112 mechanization (its slate now: §7.3 check + C-116 cleanliness check + `.github`/`gh-pages`/`CNAME` preservation); first content C-116–C-120 + the migration standard.

---

*Author: Claude (AGENT.AI.CLAUDE) · The go-live's publishing gates all Max's own hands (visibility, pushes, Pages, DNS, HTTPS); Claude prepared, verified, and recorded. The close fired on his `/close`. Parent: v0.56. Next: `/fractal` → the mechanization, **A1 (the harvest channel)**, the site content pass, or any standing offer.*
