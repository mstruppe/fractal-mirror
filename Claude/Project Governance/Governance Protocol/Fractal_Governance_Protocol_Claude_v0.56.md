# Fractal Governance Protocol — Claude Series v0.56

**Topic:** The twenty-second session (2026-08-18) — **the engine-sketch session: the site's development model ratified (C-119), the command and standards sources exposed, the mirror tree cured.** Opened `/fractal` on the post-identity queue; Max opened with a small executed fact (the avatar uploaded), then drove the site content pass from lazy cut to doctrine: the git-setup guide resolved by deletion, the command rules made, the engine-sketch principle named, the shipped baseline's real parts made downloadable — and, mid-lane, a 285-file iCloud-conflict artifact discovered and cured in the public mirror's working tree. One new number (**C-119**); one ratified amendment banked on C-116's row.
**Status:** Ratified (2026-08-18 — C-119 and the C-116 rider amendment both on Max's explicit in-session words; the close fired on his command "let's close this session") · **Date:** 2026-08-18 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.55 · **Document ID:** DOC-01M095E5A18TTKBVVBNZ15TQQD (minted at that session's close via close.py --create; the stamp landed 2026-08-18 per Scan #5 S5-2.2 — the paired revise)

---

## 1 · The session's acts (each on Max's word, in order)

**(a) The avatar landed (his own hand):** `Site/Marks/fractal_mark_kernel_light.png` uploaded to the GitHub profile — the first of the four profile own-hand acts done; pruned from the pending lists at this close. Display name · bio · contributions toggle stand.

**(b) The git-setup guide resolved by deletion (his "here we could be lazy"):** the guide's appendix section ("Setting up your own git home") deleted rather than written; the birth section's question three now closes with a live road — *"Create your own git home — a free GitHub account is all the setup this needs"* → `github.com/signup`; question five's back-reference stays as plain prose. The home page followed: the first "git" in the setup section links the same signup page, and "Git for Windows" retargeted to the project's GitHub home (`github.com/git-for-windows/git`). The content-pass list loses its git-setup item — solved by subtraction, the C-083 pattern on a page.

**(c) Step 3 deepened one honest notch:** `/close` and `/fieldnote` in the driving section link their command-library entries, and the section closes with the invitation to the concepts and blog pages ("that's where the inspiration lives"). His framing kept: step 3 stays thin until the content pass reaches it.

**(d) The command rules made (his "let's make this rule about working on the website") + the commissioned drone flight:** **rule 1** — every command that exists in the live mirror (tracked `.claude/commands/`) has a command-library entry (the mirror's `/fractal` represented by the abstract `/<yourproject>` door — verified conformant, six of six); **rule 2** — every command mentioned on the site links to its library anchor (the recorded carve-out: prose mentions only; terminal/UI illustrations and copy-paste blocks stay plain — a link inside a mocked terminal would break the illustration). The flight he commissioned swept all six pages: **37 mentions cataloged** — 13 deliberately plain per the carve-out, 9 already linked, **14 prose mentions linked in the pass**, two recorded exceptions (the abstract `/command <target>` placeholder; the `/begin` inside the home page's birth button — a link cannot nest in a link). Infrastructure the rule forced: per-command anchors (`id="cmd-*"`) in the library, and a site-wide `scroll-margin-top` cure — no anchor on the site had cleared the fixed bars.

**(e) The command sources exposed (his md-file idea + "downloadable"):** `Site/commands/` holds the six shipped command files, verbatim from the mirror; the blog gained the **Command sources** section (wiki feed grammar, rows `id="src-*"`) — each row: the command linked to its library door, one honest line, the file downloadable (`download` attribute); each library door carries the small **door-src** reference to its blog row. The trail reads door → blog entry → the actual file the agent is told.

**(f) The engine-sketch principle (his direction, verbatim intent):** *the website is FRACTAL's engine sketch* — the machine dissected, its real parts exposed; it lives and develops in lockstep with the public mirror by system design. Applied to the bibliography in the same breath: `Site/standards/` holds the eight ratified standards that ship in beta-0.5, verbatim; the Working-standards feed rows carry **full text ↓** downloads (rows `id="std-*"`); every bibliography entry carries its **bib-src** file reference. The one honest exception proves the model: the **Kernel Migration Procedure** was ratified after beta-0.5 was packed, so it is genuinely absent from the shipped baseline — its row and entry say *"ships with the next release"* instead of faking a download. **The rule register stays concept-level (his call):** the register file is project-individual; the site explains the mechanism only; narrowed general rules a possible later point. → **C-119** (§2).

**(g) The mirror hygiene episode (found in passing, cured under his word):** checking the mirror's command roster surfaced ` 2`-suffixed duplicates — the sweep found **285 iCloud/Finder conflict copies across the whole mirror working tree** (and only there: mother, KNet, the frozen H6 tree, and the annex all clean; the evidence pattern — 20 of the copies carrying beta-0.4-era content — dates the event to the beta-0.5 rebuild). Cure, each step proven: 265 deleted on per-file byte-identity proof against the tracked originals; the 20 divergent ones proven identical to the `beta-0.4` tag's content (specimens verified) and removed by **Max's own hand**; 5 extensionless stragglers (`LICENSE 2`, `.gitignore 2`, …) caught by a wider pattern and removed after the same identity proof. End state: **zero duplicates, `git status` empty — the mirror working tree matches the public beta-0.5 state exactly.** Zero unique bytes existed in the artifact set. The Codex `* 2.md` command duplicates he ordered deleted at the session's start were this artifact's first six specimens, recognized as such only at the sweep.

**(h) The C-116 rider amendment ratified (his "is ratified"):** the mirror rider's routine gains a **post-rebuild cleanliness check** — one line, a `git status` / duplicate-pattern glance after the curated sync — folded in **when the C-110/C-112 mechanization work touches the rider**, trigger-bound, not before. Banked on C-116's Register row; the mechanization slate carries it.

## 2 · C-119 — the engine-sketch model of the public site

**The site is FRACTAL's engine sketch: the machine dissected, its real parts exposed verbatim; it lives and develops in lockstep with the public mirror by system design.** The model's rules, ratified as one working decision: **(1)** every command in the live mirror has a command-library entry; **(2)** every command mentioned on the site links its library anchor — prose only, illustrations and copy-blocks stay plain; **(3)** the blog holds the parts themselves, verbatim from the shipped baseline and downloadable — `Site/commands/` (the command files) and `Site/standards/` (the ratified standards' full texts), each part's site presence back-referenced (door-src / bib-src), a part ratified after the newest anchored release carrying an honest *ships-with-the-next-release* note instead of a download; **both folders refresh from the fresh mirror at each tagging close — a C-116 sub-step**; **(4)** the rule register stays concept-level — the mechanism explained, the file not exposed (project-individual; narrowed general rules a later option). Compressed projection lives in the site's source-of-record comment (`Site/fractal-site.html`); this protocol is the source of truth.

## 3 · Ripple

Register → **v0.74** (C-119 row; C-116 row gains the banked cleanliness-check amendment; revision row) · Rule Overview → **v0.44** (the engine-sketch row beside the mirror rider) · Context Index → **v0.43** (the shipping-page row gains the engine-sketch model + the parts folders) · Local Context → **v0.73** (row 22; state, agenda, pending re-pointed; the mechanization slate gains the rider check; checklist walked below) · Agenda Board restamped (twenty-second chip; pending list: avatar pruned, three own-hand acts + flip riders + annex remote + the flip) · `Return_Package_2026-08-18_Engine-Sketch.md` issued. Working-tree deltas beyond governance: `Site/commands/` (6) + `Site/standards/` (8) new; `guide/fractal-site/concepts/blog/site.css` edited; the mirror's working tree cured (no mirror commit — nothing tracked changed there). No pack — beta-0.6 still forms on the C-110/C-112 mechanization, unchanged at the queue's head, its slate now carrying the §7.3 pack-time check **and the C-116 cleanliness check**.

---

*Author: Claude (AGENT.AI.CLAUDE) · The close fired on Max's command ("let's close this session"), the rider amendment ratified in the same breath. Parent: v0.55. Next: `/fractal` → the mechanization, the site content pass (steps 3–5 · teach-a-command · FAQ · paper pages · field studies), or any standing offer.*
