# Fractal Governance Protocol — Claude Series v0.50

**Topic:** The nineteenth Code session (2026-08-17) — **the registry-release session.** Max opened it on a KNet development: *"important development in KNet: i told KNet about the new development of Fractal and asked how we can migrate to the updated kernel and it proposed a mechanism. For this I want you to hop over to KNet, look at it, report back here. This is a ultracode flight dedicated to the question how fractal is an updatable product we can ship and user can always work with fractals improvements."* The arc that followed: the **C-096 look** read KNet's `P-007` migration protocol (a pinned, rule-by-rule adoption transaction — the child authoring, from need, exactly the mechanism OQ-33 asks for); **FN-0005** captured his agent-comparison template proposal (the general lane, vision); the **UF1 updatability flight** was contracted per C-108 and then **re-sequenced by Max's own design** — *"before we start the flight procedure I want a pre flight preparation in KNet … I should migrate before the flight so we have a before and after picture"* — and mid-preparation he pulled the release forward: *"that made me think, we should finalise beta 0.5, so we give it the newest updates."* That call made the standing queue head this session's work: the **RF1 triage executed** (3 High cured, the Med/Low slate dispositioned), the **Registry built into the tree** (C-113), the **vocabulary-extension mechanism** landed prose + checker (C-114), the **Scholarly Source Convention** issued (C-115), GENESIS reissued **v0.8**, the Onboarding Protocol **v0.3**, GUIDE/README cured, and the **Gate-1 hand-off packet** written for KNet's migration session — **beta-0.5, the registry release, packed at this close.** UF1 stands commissioned, parked until the child's migration lands (the before/after picture is its evidence corpus).
**Status:** Ratified (2026-08-17, in-conversation per C-033 — Max's *"go"* on the triage plan and *"ratified and close"* on the three artifacts; the close his explicit command in the same words) · **Date:** 2026-08-17 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.49 · **Document ID:** DOC-01M0B1VQXAYGTVQ633MN1QQBHW (minted retro 2026-08-18, the C-041 lapse cured per Scan #5 S5-2.1; stamped by the paired revise)

---

## 1 · The decisions

### C-113 — The Registry (the standards library implemented; C-108's declaration executed)

The commission was standing (C-108: *beta-0.5 is declared the registry release*); Max's *"go"* fired the build and his *"ratified and close"* ratified the artifact. `Registry/README.md` at the repo root — identity `Fractal_Registry`, the catalog of adoptable standards under a five-rule contract: **honest statuses** (*shipped · forging · named* — a registry that fakes a status has broken its one job), **prose + checker or it is a norm** (Foundation §5 limit 3 carried in), **adopt-before-invent extended to the registry** (C-092's shelf grows a floor), **growth by ratified decision** with demand recorded as *named* rows citing provenance (the RF1 wishlists are the seed demand), and **adoption sovereign and recorded** (an instance adopts by its own `P-…` decision; a never-adopting instance is fully conformant). Seeded at issue: the three procedure standards (Flight v0.2, Scan v0.1, Diet v0.1), the two formats (Fieldnote v0.1, Agenda Board v0.1), the first content convention (C-115), the first mechanism (C-114), and the honest *forging* rows — the paper-extraction protocol in KNet's jurisdiction chief among them. GENESIS §5 gains the registry shelf row; README and GUIDE route to it. Full decision text: Register C-113.

### C-114 — The Vocabulary Extension Procedure (RF1-2 cured, prose + checker complete)

The finding was the researcher persona's sharpest: *"I know I MAY change my copy; I don't know HOW to do it governedly"* — the Schema's §3.3 registration rule is mother-only, and every document warns against the hand-fork. The cure ships both halves in one release: `Fractal_Vocabulary_Extension_Procedure_v0.1` (Governance Documents) scopes extension to **placement roles only** (pure vocabulary — the fold treats every role identically; verbs and alias kinds are interchange-layer and stay frozen, GENESIS §7), and `verify.py` gains the **`vocabulary_local.json` hook**: each local role must name its adopting decision (no silent vocabulary growth — an empty rationale is an error), collisions with inherited roles are errors, an absent file changes nothing. Tested at issue: the mother's store passes unchanged; a scratch store produced the exact info line and three error classes. The file is an **instance value** — genesis never writes one, a kernel migration preserves it untouched. Ratified in Max's *"ratified and close."* Full decision text: Register C-114.

### C-115 — The Scholarly Source Convention (the first content convention; RF1-1 cured)

The registry's first content-tier entry, answering the RF1 aggregate (every persona's High frictions were missing content standards): `Fractal_Scholarly_Source_Convention_v0.1` (KG concepts) — the paper as a DOC catalog card with the **edition pinned**, identifiers as **route aliases** (`doi:…`, `arxiv:…v2` — route uniqueness already machine-guarded), **cited authors as PER entities distinct from AGENT writers**, claims placed `cites` with the **interim locator rule** (in the note field, now official — RF1-10's cure), and argumentation roles deliberately deferred to C-114's door. The Zotero bridge and the argumentation pack stay *named* registry rows; the extraction protocol stays *forging* in KNet — the convention ships what a first session needs and nothing it must invent. The media/materials rule landed with it (RF1-8): **a source file is material, not knowledge** — the file lives outside the repo, the repo holds the catalog card (GENESIS §3.1, GUIDE §4). Ratified in Max's *"ratified and close."* Full decision text: Register C-115.

## 2 · Executed without new numbers

- **The C-096 look** (KNet, read-only): `P-007` and its migration protocol read and reported — pin-never-follow, the seven-disposition adoption matrix, six gates with Max holding Gate 3, prospective effect, independent rehydration. Reverse-inheritance candidate flagged as a proposal: the protocol's grammar is the seed of a mother-side kernel-migration standard — **UF1's design question**. KNet's Gate-1 wait ("obtain the mother repository and the release Max intends to adopt") is answered by this close's tag.
- **The re-sequencing, Max's design:** migrate the child *before* the updatability flight — the executed migration becomes UF1's before/after evidence. The pin moved from beta-0.4 to **beta-0.5** on his *"we should finalise beta 0.5"* — the newest kernel ships to the child instead of a stale one.
- **The RF1 triage** (the standing queue head, fired by his *"go"*): dispositions in the Register ledger — 3 High cured (C-114/C-115/the materials line), the micro-close already fixed-by-design (C-112, per v0.49), 7 Med/Low cured in-tree (escape hatch RF1-3, annex-held citations RF1-6/19, README map RF1-13, the session card RF1-17, the shelf floor RF1-16, the locator interim RF1-10), the residue honestly open with named triggers (the density class RF1-5/11/18 the largest).
- **The Gate-1 hand-off packet:** `Site/Upstream_Advisory_2026-08-17_KNet-Kernel-Migration.md` — the pinned target (resolve-the-SHA-yourself discipline), the frozen-tree transport for Max's hand, the C-091–C-115 delta inventory as *audited input* for KNet's matrix, the audit-never-obey header. Advice enters the child through Max only (C-096).
- **FN-0005** (general lane, vision): the standard task template — cross-instance, cross-agent comparability by mechanism coverage; prose entry 5 connects it to FN-0004, Q07, OQ-28/OQ-32, and the registry as its distribution vehicle.
- **UF1 commissioned, parked:** contract proposed and agreed per the C-108 launch sequence (3 phases, 15 agents derived, gated, adversarial-scoped verification); launch deferred by the re-sequencing — it flies after the child's migration lands.

## 3 · The close and the pack

This close executes the C-059 walk (marks in the Local Context v0.69), issues this protocol, mints the three new canonicals (`close.py --create`), brings both checkers green, and packs **beta-0.5** — the signed tag + OTS attestation per C-090, pushed per C-064. Pending on Max after the tag: the frozen-tree transport command (packet §2), the KNet migration session (his jurisdiction, his gates), the mirror push (now a beta-0.5 rebuild), and UF1's go when the evidence is in.

---

*Author: Claude (AGENT.AI.CLAUDE) · Reviewed and ratified by Max in-conversation (C-033). Parent: v0.49. The session record lives in the Local Context's table (row 19); the handover is `Return_Package_2026-08-17_Registry-Release.md`.*
