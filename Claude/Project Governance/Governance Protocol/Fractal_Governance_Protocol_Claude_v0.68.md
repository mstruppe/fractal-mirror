# Fractal Governance Protocol — Claude Series v0.68

**Topic:** The thirty-fourth Code session (2026-08-21) — **no new decision number: the concept-tier reissue class (the C-086 pattern — the ratification lives in the document, this issue is the narrative of record). The Interface Foundation reissued v0.6 (the live-seam amendment + the transport-security clause); the working estate moved under the `Knowledge Network/` roof with the path-currency pass; the annex remote born; the blog menu served.** Opened `/fractal` onto the queue (buffer 4/10, quiet). **(1) The blog menu served on his "go":** the thirty-third session's held `gh-pages` refresh executed as the ordinary serving-split act (pull-before-push; `blog.html` +137/−9, byte-identical to HEAD; commit `8b6ada8`, author Claude, committer Max under the public identity, signed `G`) — and his catch honored: the verification watcher had polled a nonexistent path (`knet.network/blog.html` 404s — the gate is a *door* to the mirror site, not a path-host); the stale monitor killed, the menu verified live at the mirror address. **(2) The two-repo review on his ask, FN-0003 on his "general note will suffice":** Modular's monorepo read as a governance specimen (the explicit contribution boundary; the open-core license seam — kin to our intake rule and license layer), NEAR's org as alien machinery around the genuinely shared arc-3 question (trustless verification between peers without a common authority; their NEPs process kin to our ratification seam); both banked watch-class in one general fieldnote (buffer 5/10), mint-on-reuse. **(3) PR's birth prepared:** the concept recalled of record (website + community as own governed instance; born from the fresh `beta-0.7` tag, no migration debt; the site-estate charter — operations transfer, the mother keeps what-and-why + the address; the two-children point unlocking the counterpart parameter; the two open parameters on Max — the name, the shared-core slice); his UI assumption verified against the app docs (second project = second folder; sessions grouped per project, side-by-side switching; per-directory adapters and memory — jurisdictions separate by construction). **(4) The live-seam direction → the v0.6 reissue on his "ratified":** his vision (*"ideally I want the PR have an open session parallel to fractal, such that fractal communicates live anything concerning pr … we update a concept and you communicate it to pr because it knows how to update everything pr related to that context"*) and his constraint (*"this is not a claude internal communication system … it needs to be a fractal system so I can connect any fractal with any agent"*) folded as **§9's live seam** — (i) parallel operation permitted, the serial posture named an operational call and relaxed, every seam rule holding; (ii) the mid-session doorbell — counterpart indexes polled during work, **the index state the normative signal**, envelopes author-side, pull-never-push and C-031 untouched; (iii) **the vendor-neutral floor** — interop floor = polling over files/git/HTTPS, any agent a full participant, push-style wake-ups (file watchers, harness messaging, webhooks) client-adapter accelerators that may only say "poll now"; (iv) routing competence as the receiving instance's domestic law via the shared-core contract's slice, the editorial flow the first consumer — plus **§8's transport-security clause** on his second-desktop question: authenticity/integrity by construction (signed commits vs `.allowed_signers`, content-addressing, hash-pinned seams — the four-eyes program the executed proof; a compromised relay withholds, never fabricates; an outsider's place is on nobody's reading list), confidentiality a per-channel choice by content class (public repo / private remote, the C-098 class / direct git-over-SSH between the owner's machines), the C-087 floor on every channel, cross-**owner** trust deliberately deferred to arc 3 (FN-0003 cited). **(5) The Desktop grooming — his commission, the frank verdict "change it now, before PR's birth":** the inventory delivered (the ghpages folder identified as the mirror's `gh-pages` *worktree* — apparatus, keep; the annex identified as the one remote-less, unrecoverable folder — never delete; `fractal-beta-0.1` deletable, `fractal-frozen-beta-0.5` spent since P-024, `fractal-frozen-beta-0.4` keep — the Q07 corpus); **KNet closed first in its own jurisdiction** on his direction (its relocation-readiness inspection observed via C-096 on his ask — the "5-step plan" read as its P-020 close transaction running at normal ritual weight, conformant not runaway; its findings aligned with the runbook: close → move → reopen → orient); then on his "let's move": **the six standing folders moved in one sweep** under `~/Desktop/Knowledge Network/`, the mirror worktree link repaired both directions, the session memory carried to the new project home, every repo verified healthy; the mid-move disconnect resolved by his redirect (the app **forked** the conversation to the new path — verified local-only, all sessions `local_*`, no cloud; the C-091 checks passed: the real governing tree, `main`, no copy); **the bin found to have taken the Q07 grant too** — flagged, and on his "go" **regenerated byte-identical from the anchored `beta-0.4` tag** (`ff7214f`, the same commit the binned tree stood at); **the annex remote born on his repo-creation** (`mstruppe/fractal-annex`, private — pushed `c2d3455`, privacy 404-verified; the one-annex-for-all shape agreed: personal-class is owner-scoped, per-project folders inside, the split trigger = a second person). **(6) The path-currency pass (C-080 class):** seven living documents cured to the roof paths (the `/look` command, CLAUDE.md → **v0.12** — path row, stamp holding at Settings v0.8 —, the Index — restamped v0.53, also carrying the Foundation v0.6 row —, the Local Context, the Register's three standing-reference rows, the Interface index, the Workbench README); the frozen records keep their paths as recorded (advisories, capture dossiers, the Site parts copy to its C-119 rhythm); two stale LC facts found at the walk and cured (the "newest Protocol v0.64" mention → v0.68; the anchor spans → beta-0.4–0.7 anchored, the mirror chain 0.3–0.7). **(7) This close on his "close":** the walk run (Register unmoved as to numbers; LC/Index/Board/adapters/Gas the changed rows), this issue + the Return Package written, the board's thirty-fourth chip, the Gas rows, the ripple, the gates, the push.
**Status:** Ratified (2026-08-21, in-conversation per C-033 — the refresh on his "go"; the fieldnote on his "general note will suffice"; the v0.6 reissue on his "ratified"; the move on his "let's move" after KNet's own close; the grant regeneration and annex wiring on his enumerated "1. go / 2. <url> / 3. close") · **Date:** 2026-08-21 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.67 · **Document ID:** DOC-01M0J1DDJ6Y470ZJ119K7GS2BD (minted at this close via `close.py --create`)

---

## 1 · The session's acts (each on Max's word, in order)

1. **The blog menu served** — the held `gh-pages` refresh executed (`8b6ada8`), the stale verification path corrected, the menu verified live.
2. **FN-0003 captured** — Modular + NEAR as watch-class references (his general-note weight; buffer 5/10).
3. **PR's birth prepared** — the concept recalled of record; the serial-UI path verified; the two parameters restated as the open gate.
4. **The Interface Foundation reissued v0.6** on his "ratified" — the live-seam amendment (parallel · doorbell · vendor-neutral floor · domestic routing law) + the transport-security clause.
5. **The estate moved** — KNet closed first (its jurisdiction, C-096-observed), the sweep, the worktree repair, the memory carry, the fork seam ridden.
6. **The Q07 grant regenerated** (`ff7214f` at the tag) and **the annex remote born** (private, pushed, verified) — the ocean gap closed.
7. **The path-currency pass** — seven living docs; CLAUDE.md v0.12; the two stale LC facts cured.
8. **The close on his "close"** — this issue, the Return Package, the board chip, the Gas rows, the ripple, the gates, the push.

*The evidence homes: `Claude/Architecture/Concepts/Interface/Fractal_Interface_Foundation.md` (v0.6 — the ratified amendment) · `Site/Fieldnotes.md` (FN-0003) · mirror `gh-pages` `8b6ada8` (the served menu) · `~/Desktop/Knowledge Network/` (the roof; the regenerated grant at `ff7214f`) · `github.com/mstruppe/fractal-annex` (private; `c2d3455` pushed) · KNet `bc63a50` (its relocation-inspection close, its jurisdiction) · CLAUDE.md v0.12 · `Return_Package_2026-08-21_Relocation-Live-Seam-Annex.md`.*
