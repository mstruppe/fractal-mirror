# Fractal Governance Protocol — Claude Series v0.58

**Topic:** The twenty-fourth session (2026-08-18) — **the mechanization close: the C-110/C-112 tool work executed in full, TF1-4 cured on its named trigger, the C-116 amendment discharged into tooling, and the Scan #5 flight commissioned for the next session.** Opened `/fractal` on the post-go-live queue; Max opened with the vendor-convergence observation (FN-0009), then commanded the queue's head — *"yes let's implement this"* — and the mechanization landed end to end with a full scratch-proof; at the tail his flight call (*"let's do the scan as an ultracode flight … extra attention … on diet and dynamic closing"*) and the close-first sequencing. **No new C-number** — every act executed standing doctrine (the v0.37/v0.51 precedent); the Register moves on two pre-ratified, trigger-bound rows, so this issue records the execution.
**Status:** Ratified (2026-08-18 — the implementation on Max's "yes let's implement this", the fieldnote on his "do it", the flight plan on his commissioning message; the close fired on his `/close`) · **Date:** 2026-08-18 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.57 · **Document ID:** DOC-01M0AVRJD2QPMY92BESMEW0AJS (minted at that session's close via close.py --create; the stamp landed 2026-08-18 per Scan #5 S5-2.2 — the paired revise)

---

## 1 · The session's acts (each on Max's word, in order)

**(a) FN-0009 — the vendor converges on durable session state (his "do it"):** Max observed Claude Code's cloud-sessions/Remote-Control update and asked whether Anthropic had copied FRACTAL's approach; the assessment of record, checked against the shipped changelog: **convergence, not copying** — the timeline rules copying out (this repo was private until the go-live afternoon), and the feature is a different layer (durable *sessions* vs. FRACTAL's session-disposable, repo-held memory, C-031). Captured to the publishing lane as **FN-0009** with the prose entry framing it the **USP series' strongest specimen** (the vendor of FRACTAL's own operating surface shipping product against the tagline's need); the block's imprecise FN-0002 sibling pointer corrected in the judgment layer (the true siblings: shipping-run entries 41/42/44). A third pitch beat (hook → twist → proof) and an FAQ seed banked for the site content pass.

**(b) The C-110/C-112 mechanization executed (his "yes let's implement this" — the queue's head since the diet session):** `close.py` now carries the close's mechanical half; `check_versions.py` gained a main-guard so close.py **imports its LIVING/SERIES registry instead of forking it** (one registry home, C-092). The landed pieces, mapped to their mandates: **(1) the walk pre-fill** (C-110 proposal 8 / C-112a) — the C-059 table's rows read from their normative home in the Local Context, marks pre-filled from the store hash-diff ∪ git working-tree signal; **(2) the judgment dirty-flag** (C-112b) — Register unchanged ⇒ the tool offers the recorded-reason line mechanically, so a small session's close *emits* the micro shape; **(3) the drag gauge** (proposals 9–10) — per-document word counts vs. the ratified advisory budgets, deltas vs. HEAD, session-table row count; advisory, never blocking; **(4) `--stamp`** (proposals 6–7, applied at `--write`) — version bumps for byte-changed living documents whose stamp still equals HEAD's, revision-history rows minted from each document's `--note` (Register table shape and prose shape both; diet-compacted documents skip), and **store counts written at predicted post-ripple totals before hashing** — S4-1.2's counts-last lag cured: the recorded hash covers the final bytes; **(5) `--pack`** — the tagging-close gate: the §7.3 clause-span check (C-117's next-tagging-close trigger, discharged early), the mirror post-rebuild cleanliness + iCloud-duplicate-pattern scan (**the banked C-116 amendment, executed**), and the C-120 preservation checks (`.github/` · `gh-pages` ref · `CNAME` informational — the gate repo carries the domain); **(6) TF1-4 cured on its named trigger** — the `--create` alias prefix parameterized from the instance's own `.version-registry` (derived from the Global Context override; explicit `alias_prefix` key wins; mother default `Fractal_` unchanged; tested all three ways). The judgment half stays manual by declared boundary (proposal 14) — the tool marks, the human means.

**(c) The proof (scratch-copy pipeline + live signals):** the full write path exercised in an isolated repo copy — stamp bumps (LC 0.74→0.75, Register 0.75→0.76), a minted Register history row carrying note + protocol pointer, counts at correctly-predicted totals (779+1=780), the revise event hashing the **post-edit** bytes, `verify.py` green; the union-signal fix landed mid-test (the Local Context is deliberately un-DOC'd — derived projection — so bump eligibility takes hash-diff ∪ git, revise events stay hash-diff-only). Against the live repo the new modes surfaced three true signals: **`--pack` correctly red** (GENESIS §3.4 at C-117 vs. Register max C-120 — exactly the staleness §7.3 exists to block; cures at the tagging close), **two drag-gauge YELLOWs** (Local Context 5,987/3,500 · Index 2,160/2,000 — measured C-095 candidates), and **all five mirror checks green**. The store README and the `/close` command re-projected in the same change-set (the command's counts-last step absorbed; the `--walk` dirty-flag step and the tagging-only `--pack` step added; its stamp carries the re-projection).

**(d) Beta-0.6 readiness assessed (his question) and the flight commissioned (his call):** the release trigger — *beta-0.6 forms on the mechanization* — is satisfied; the delta since the beta-0.5 tag measured at 12 commits / 82 files / +3,537−440 across five sessions (C-116–C-120 + the migration standard + the site + this close's content). The duty list to the tag recorded: this close · the GENESIS §3.4 clause reissue · the first §8 advisory instantiation (fires UF1-7) · the C-116 rider with the C-119 parts refresh and `--pack` green. **Max commissioned Scan #5 as an ultracode flight** — the Scan Procedure as the main flown procedure (its first `check_scan.py`-gated member), full slate over the sixteen sessions since Scan #4, release-readiness weighted, **with two commissioned attention lanes: the diet** (compaction pointer integrity, the fact-presence gate, pointer-stopping, the two YELLOW seeds) **and the adaptive close** (C-112 conformance of the built tooling, ritual documentation currency, the C-110 boundary honestly kept) — his grounds: *these features make FRACTAL a system that knows how to counter obstacles*. Gating mode, agent counts, and effort are set at launch per the flight standard (starting proposal banked: ~7–8 agents, refuter-gated, effort high).

**(e) The sequencing ruling (his question, the recommendation accepted by his `/close`):** close first, fly next session — the scan wants a committed baseline; this close is the mechanization's first live exercise (C-094 green datum before a flight leans on the tooling); the fresh working set is the diet doctrine applied to ourselves; and the next session's close can be the **tagging close** (pack beta-0.6 in one coherent event).

## 2 · The Register's two moves (both pre-ratified, trigger-bound — no new number)

1. **TF1-4 → Fixed** (the Med/Low group's last Open row): the named trigger — "the C-110/C-112 close-tool work" — fired and the spec'd cure landed as §1(b)(6). The TF1 slate is now fully closed.
2. **C-116's banked amendment → executed**: the post-rebuild cleanliness check (ratified per v0.56, trigger-bound to this work) is tooled in `close.py --pack`, alongside the C-120 preservation duties the go-live added to the same slate.

## 3 · This close (the walk's judgment summary)

The close itself ran through the new tooling — `--walk` pre-filled the table (store-tools row CHANGED; Register flagged by judgment, not hash: its row moves are this close's edits), `--stamp --write` minted the Register's v0.76 history row and wrote post-ripple counts, the gates green. Changed: Register (two row moves + minted history row), Local Context (row 24, state, agenda, this walk), Agenda Board (restamp), the store tools (close.py + check_versions.py + README), the `/close` command projection, the publishing fieldnote ledger (FN-0009 + entry 9), this protocol (minted), the Return Package. Unchanged and checked: Rule Overview, Index, Global, Architecture State, BOOTSTRAP, Schema→Template, Settings (the C-071 question asked and answered *no* — tool work is repo ritual, the C-084/C-090/C-116 precedent; five candidates stay banked), both adapters (Settings did not move; the close-ritual detail lives in the command projection, which re-projected).

---

**Sources:** the session transcript (2026-08-18, the twenty-fourth Code session); Decision Register v0.76 (this close); close.py / check_versions.py / the store README / `.claude/commands/close.md` (the change-set); `Site/Fieldnotes_2026-08-16_Publishing.md` (FN-0009 + entry 9); `Flight_2026-08-17_Diet.md` + `Flight_2026-08-17_Compaction-Behaviour.md` (the mechanization's mandate record); `Fractal_Kernel_Migration_Procedure_v0.1.md` §7.3 (the discharged trigger); Return_Package_2026-08-18_Mechanization.md (the handover).
