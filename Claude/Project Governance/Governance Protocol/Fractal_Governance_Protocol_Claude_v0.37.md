# Fractal Governance Protocol — Claude Series v0.37

**Topic:** The first shipping run (tenth Code session). Max opened by laying three moves on the table — the knowledge-network research with a plug-in demonstration, the public identity, and protective measures — and asked for an order of conduct. The session set it (**"engine first, then exterior"**: move 1 → move 2, move 3 dissolved into both), and then **arc 2 of C-078 opened by execution rather than announcement**: Max shipped FRACTAL to himself as its first user — clone of the anchored `beta-0.1` tag, the stranger protocol, an information diet, a friction ledger — and **birthed the first real foreign instance**: **KNet** (`~/Desktop/knet`, `github.com/mstruppe/knet`), a research project on the information economics of knowledge networks, with its own AI writer (`AGENT.AI.ALFRED`). **The GENESIS §6 acceptance test passed the same day** — a session worked and closed inside the child, P-001 above an inherited line never re-argued — including a defective-close discovery that the child repaired with doctrine of its own. The whole approach path is captured in a 28-entry pre-canon fieldnotes file. **No new C-number:** everything decided here executes C-078 or sequences recorded moves.
**Status:** Ratified (2026-08-15 — ratified in the conversation it records, per C-033) · **Date:** 2026-08-15 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.36

---

## 1. Context

The session opened by `/fractal` onto the packed beta — nothing pending anywhere. Max named three candidate moves and asked for an order. The assessment ran each against the corpus: move 1 *is* the recorded program (arcs 2 → 3, with the research specimen already the named candidate per v0.33, and the information-economics lens doubly self-supplying — its process tests the kernel, its findings feed the Knowledge Network Foundation's economic layer); move 3 is mostly executed already (the `beta-0.1` anchor) with its remainder being the flip-time bundle, which only binds at exposure; move 2 wants substance to publish, and the recorded posture (*private until the beta proves the kernel*) already points at arc 2 as that proof. One correction from Max sharpened the end-game: **federation is pull** — the mother grabs the published child; the child does nothing — which matches C-078 arc 3's own wording ("ingest") and keeps all OQ-31 machinery on the ingesting side. A follow-up design exchange settled the merge-naming question in principle: uniform origin-mount plus explicit `same-as` bridges, never per-collision path concatenation (the meaning layer stays human; Architecture State §6's verdict held as written).

## 2. The order of conduct (Max: "we build the engine first then the exterior")

**1 → 2, with 3 dissolved into both:** arc 2 now (the research instance, releases anchoring as they ship) → the grab demonstration (arc 3's opening, forging OQ-31 against a real second store) → the flip (identity build: website, public GitHub, name resolved), with the protective bundle firing as the flip's gate. The name debate stands as the one cheap early parallel thread. Sequencing of recorded moves — no new decision number; this section is the record.

## 3. Arc 2 opens — the shipping run and its method

Max's word arrived as execution: *"starting with move one, walk me through."* The **specimen** is his call of record: a **research project** — the knowledge network through the lens of information economics — not the tracker (which remains a candidate later instance); **placement** per the recommendation of record, *birthed beside the repo*. The method, adopted as the game rules of the run:

- **Ship = the tag.** The stranger receives `beta-0.1` — the signed, anchored artifact — via `git clone --branch beta-0.1`, treating GitHub as the distribution channel.
- **Two worlds, hard wall.** Upstream (this repo) observes and records; the stranger works elsewhere with fresh sessions. Claude Code's per-directory adapters and memory make the wall mechanical.
- **Information diet + friction ledger.** The stranger acts only on shipped material; every forced reach-back to insider knowledge is logged as a finding, not fixed in place.
- **Upstream never pre-fixes.** Findings accumulate for a batched ingestion (phase 5) that produces the next release — releases serve *newcomers*; existing instances upgrade only by their own recorded decisions.
- **The research must be real** — toy content would hollow both OQ-28's verdict and the eventual grab demonstration.

## 4. The run — KNet born, accepted, and already governing

Captured entry-by-entry in `Site/Fieldnotes_2026-08-15_First-Shipping-Run.md` (**pre-canon, C-062** — informs the future setup guide, governs nothing; 28 entries at this close). The acts of record:

- **Birth:** `genesis.py` dry-run first, exactly as GENESIS §0 prescribes; one failed `--write` (the tool constructed an HTTPS remote; the user authenticates over SSH) **cleanly rolled back by the tool's safety mechanism** — atomic birth held in a stranger's hands; second `--write` green, SSH remote bound manually after. KNet: 36 files, 5 roots (`FACET`, `AGENT`, `RES`, `GOV`, `ECN`), routes bound, `verify.py` green, commits authored `Alfred <alfred@knet.local>` from the first act.
- **Acceptance (GENESIS §6) passed:** a session worked and closed inside the child — vision written, `P-001` recorded *with a scope note Alfred added unprompted* (the outline's open research queue explicitly unaccepted — C-031's offers-never-mandates doctrine rediscovered natively), handover frozen at issue, checklist walked, verifier green.
- **The defective close and its repair:** the close's "no off-site remote is bound" claim was false — inherited from the Register's *at-genesis* parameter binding, never re-checked against `git remote`. Root cause: the remote was bound by the *clone session* (the midwife) outside the child's governance loop. Alfred surfaced three repair options; on upstream advice the child took the **marked correction block** (original text preserved verbatim, correction appended, scoped to the false fact) — and in justifying it articulated a distinction **FRACTAL's own corpus has never written down**: records *overtaken* by events (corrected by the next package — the frozen-at-issue default) vs. records **invalid at issue** (certifying a close that did not happen — corrected in place, marked). **First candidate for reverse inheritance**, noted at OQ-31.
- **The findings, compressed** (full text in the fieldnotes): the README serves the evaluator, not the user standing in the folder (the fix shape: quickstart-first — clone → `claude` → one sentence); **the shipment doesn't know it's a shipment** (the clone session, correct by its constitution, offered to run the mother's close ritual — and the owner's key *can* push to the real remote; fix shapes: a release marker or a detached-HEAD adapter clause); **the hidden shelf** (the user reinvented the Agenda Board within a day — tiering without discoverability at the moment of need silently converts saved carrying cost into reinvention cost; first fix shape: an adapter reflex, *adopt before invent*); the notebook shipped (a clone carries all history — first concrete datum for flip-vs-mirror); first contact is unscripted (the adapter's orientation rule doesn't fire on a greeting); genesis's remote-URL form; plus the guide items (terminal fluency, the missing settings surface, shipment-vs-inheritance, the minting metaphor, the two permission layers, the version-relationship rule, the eternal-updates answer).

## 5. Vision material recorded (offers, not decisions)

- **Two-route distribution:** developer route (clone + `claude`) and a **FRACTAL-branded desktop app** — a thin shell over the Claude Agent SDK, plugging in via the existing adapter class (C-065); the first app slice would fire OQ-23, and "a working interactive interface" is already Global §2's named example of a next transition.
- **The second-client trial:** the kernel is client-neutral by architecture, Claude-only by biography — one governing-scope adapter projection for a Codex-class client would demonstrate vendor independence (answers the move-3 worst case). Bundled with Max's recorded naming preference: the general "Agent" name should arrive via the ecosystem's `AGENTS.md` convergence, not by breaking client discovery conventions.
- **Onboarding as the guided first loop:** a questionnaire whose answers *are* the genesis parameters + vision + P-001 + first close — onboarding ends at the first close, not at birth; one canonical script projected into `/welcome`, the app's first run, and the website. The fieldnotes are its first draft.

## 6. What this close deliberately does not do

**GENESIS.md is not reissued**, although its named trigger — *"the first real instantiation, whose experience supersedes this document's predictions"* — has now fired: the experience's fixes are deliberately batched into the phase-5 ingestion that produces the next release, so the document moves once, with the whole slate, not twice. **No phase-5 fix is applied** — the upstream-never-pre-fixes rule holds until the ingestion session. **OQ-4 is not moved:** the first real instantiation exists, but Global v0.4's §2 already names governing a first foreign instance as the *current* mode; the comparison-forcing moment is arc 2's completion, and no fiat was declared. **No new C-number is minted** — the session executed C-078 and sequenced recorded moves. **Settings untouched** (no conduct rule changed); **adapters unchanged** (stamps current; no doctrine touched a surface's rules). **The fieldnotes stay pre-canon** — they inform the guide; they govern nothing. **KNet is not governed from here:** upstream advised when asked; every decision in the child was Max's or Alfred's, under KNet's own constitution.

## 11. Open Questions (TBD)

- **OQ-28 annotated:** arc 2 is live and producing user-side data — the hidden-shelf finding (discoverability is part of the weight answer), the spontaneous minimality question ("why does it need to know?"), and a tier-0 hand-close performed successfully on day one.
- **OQ-31 annotated:** two additions from the run — the **format-stability obligation** (upstream may never break readability of old instances at import; write as a standing rule when the import tool is built) and the **reverse-inheritance datum** (KNet's overtaken vs. invalid-at-issue handover doctrine, citable when the mother next touches close doctrine).
- **OQ-4 annotated:** the arc-2 candidate's named occurrence (first real instantiation) happened; declaration deliberately not made — see §6.
- Nothing new opened. Nine OQs stand.

## Ratification record (2026-08-15, in-conversation per C-033)

Max's calls of record, in sequence: the three moves laid out and the order requested; the federation direction corrected (*"the fractal project grabs the new KG"*); the order adopted — *"yes I agree, we build the engine first then the exterior"*; the run begun — *"starting with move one, walk me through"*; the protocol directive — *"Protocol all these set up steps and trip overs … this will be gold when designing the set-up guide"* (→ the fieldnotes file); the specimen fixed by execution — **KNet**, research instance, birthed beside the repo; the in-run calls (real email for the human writer; "keep the scope note"; the register projection moved in-repo; **push** and **option 3** on the defective close); and **`/close`**. Ripple: Register → **v0.40** (C-078 arc-2 annotation; OQ-4/OQ-28/OQ-31 annotations) · Local Context → **v0.52** · Architecture State → **v0.15** (§6: the first real instantiation recorded) · Context Index → **v0.27** (Provenance row at its queued trigger; sources current) · board regenerated (stamp v0.52) · fieldnotes committed (pre-canon, `Site/`) · this protocol minted. Unchanged, deliberately: Global Context v0.4 · Rule Overview v0.31 · Settings v0.7 · `CLAUDE.md` v0.7 / `AGENTS.md` v0.7 · Schema/Template/Contract · BOOTSTRAP v0.17 · GENESIS v0.4 (trigger fired, reissue queued with the ingestion) · all six store tools.

## Document identity

| Field | Value |
|---|---|
| Document Title | Fractal Governance Protocol — Claude Series v0.37 |
| Document Type | Governance Protocol (historical artifact, dated identity — C-012) |
| Version | v0.37 |
| Status | Ratified (2026-08-15, in-conversation per C-033) |
| Domain | GOV |
| Prepared By | Claude |
| Reviewed By | Max (2026-08-15, this conversation) |
| Date | 2026-08-15 |
| Parent Protocol | Fractal Governance Protocol — Claude Series v0.36 |
| Related Documents | Decision Register (stamp inside); Local Context (stamp inside); Architecture State (stamp inside); Context Index (stamp inside); GENESIS.md (stamp inside — trigger fired, reissue queued); Site/Fieldnotes_2026-08-15_First-Shipping-Run.md (pre-canon, C-062); KNet (external instance — `github.com/mstruppe/knet`, not governed from here) |
| Document ID | DOC-01M03GSSFFKS4TXJMQPAEGMQGR (minted 2026-08-15 via `close.py --create`; stamped by the post-mint revise) |
