# Fractal Governance Protocol — Claude Series v0.61

**Topic:** The twenty-seventh Code session (2026-08-19) — **the instrument forge, C-122 (the mirror doctrine), the first C-121 work-down, the three-leg KNet exchange, Interface Foundation v0.3/v0.4, and the birth of the interface place.** Opened `/fractal` onto the standing offers; Max drove six arcs. **(1) The instrument forge:** his commission verbatim — *"a data mining tool that can be sandbox type deployed into fractal mirror releases… this should hopefully be cheap, the mining tool sends the data to an offsite repo where it fills a data base, this data base will later feed into KNet research, we can then decide with each update which data we want to track across fractal projects"* — refined by his normalization directive (*"I don't simply want to measure token use, that alone tells nothing, we need a scaling proxy take from the system itself… preferably we have many different proxies, so we have something to measure against"*) and his consent ruling (*"yes this must be the usual questionare, 'are you willing to ship data to us?' mechanism"*); the design forged in-session (kernel-skeleton proxies · per-release manifest · close-time JSONL capture · inbox-shaped returns · dormant-by-default) and **placed by his sorting call** (*"this is not the road map… this is agenda board material"*) — banked as the OQ-35 agenda point, drafting on its turn. **(2) C-122 ratified on his "go":** his identification verbatim — *"could it be that the roadmap is global context and agenda board is local context, the AI agents core navigators for the human, both having the shared fieldnote document, I think this structure should be resembled on the website"* — executed whole as the scheduled user-document refinement pass. **(3) The first C-121 work-down on his "full slate":** buffer 11/10 → **4/10**, seven dissolved into durable homes, four held on named triggers. **(4) The three-leg KNet tool-fractal exchange** (his four-eye commission: *"make refinement proposals because you know better how the fractal network will look like than KNet. This way we also get a 4 eye principle of two different LLM models"*): KNet's plug-in offer reviewed → the mother's return → Max's live-use objection (*"when working in a fractal project, I would still like to use the tool 'live'. So in this case this should act as an integration, but one you can install or de-install"*) → KNet's independent revision converging on the same law → the mother's second return (confirmation). **(5) Interface Foundation v0.3 on his "ratified"** (his design verbatim: *"a fractal has a place where it puts information files under a navigation index and every fractal knows that it can go there into other fractals to see the information… a second ram for communication… the time window is determined by the projects creation and deletion of the information in their own interface"*) **and v0.4 on his "ratify"** (the installed-integration correction). **(6) The C-096 git observation on KNet** (his ask: *"the git repo of KNet hasn't seen a commit in a long time, I wonder how"* — resolved: commits fine, stranded on a `codex/*` branch) **and the birth of the interface place** on his word: *"we can already create the interface folder here and place the handoffs you already created plus the new one in there. that's the birth of the interface we will work on in the next session."*
**Status:** Ratified (2026-08-19, in-conversation per C-033 — C-122 on "go"; the work-down on "full slate"; Foundation v0.3 on "ratified"; v0.4 on "ratify"; the interface birth and this close on his commands of record) · **Date:** 2026-08-19 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.60 · **Document ID:** DOC-01M0D2CC2VFYRG149M7RJ1EDCQ (minted at this close via `close.py --create`; stamped by the paired revise)

---

## 1 · The session's acts (each on Max's word, in order)

1. **The instrument forge (no build):** the measurement program designed whole — scaling proxies drawn from the kernel's guaranteed skeleton (spine mass · corpus mass · store cardinality · governance cadence · churn · gauge readings; deterministic, no model calls; raw gas the optional vendor column, FN-0009's layer), the per-release manifest decided at tagging closes (consent binds to the manifest version — grown re-asks, shrunk doesn't), capture at the close (one JSONL row per session, instance + kernel pinned), the return path the interface's inbox shape (git as transport, repo as database, no server), **dormant by default** (genesis questionnaire; §8 advisory for existing instances; data leaves only by the owner's push). Recorded as **OQ-35**; build slot after the beta-0.6 pack.
2. **The refinement pass executed (C-122, §2):** role/parent declarations in all three user documents · the trio glance-path bar (identical element, current page marked) · Global Context **v0.6** (§3 gains the human spine + sorting rule; trio pointer; §3-class refresh trigger) · the site's user-documents section restructured to the mirror (*"The human gets the same spine the AI does"*) · one mid-session red cured (the LC's stale Global claims).
3. **The first C-121 work-down ("full slate"; depth 11 → 4):** general FN-0001 + FN-0006 + buffer FN-0001 → **OQ-35** (the program row; OQ-28 cross-annotated, keeping the weight-dial question) · publishing FN-0001 (open half) → **OQ-36** (the paved adoption path; trigger: first real demand) · publishing FN-0007 → the site's live bibliography section (citation in the page's provenance comment) · shipping-run FN-0005 → **Agenda Board Format v0.2** (the user-document bar entering the grammar after its same-day field trial, C-094; the coequal-branch NEXT provision — an agenda shape, never a session shape, C-106 untouched; v0.1 frozen history) · shipping-run FN-0006 → **Client Library v0.5** (the Codex close-scope rule; the missing v0.4 history row added retroactively). Held on named triggers: general FN-0005 (Q07/registry moment) · publishing FN-0003 + FN-0008 (the grooming/content passes' dowry) · FN-0009 (rides the FAQ + capture v1.2). Every home cites its ids; deletions whole; the high-water line holds.
4. **The three-leg exchange (all transports Max's hand; files author-side throughout — the practice that §9 then distilled):** *leg 1* KNet's `Handoff_2026-08-19_Optional-Tool-Fractal-Plugin-Mother.md` read in place; *leg 2* the mother's review return issued (verdict: sound; corrections — protocol-never-hub, authority-never-travels, C-087 floor, anchored pins at the adoption seam, C-117/C-115 reuse; the nine questions answered; the ask/hand-off ruling: **no new message class — the first tool invocation is the ask class's first execution**); *leg 3* after Max's live-use objection and KNet's independent revision (its law *"live at the interaction boundary; fixed at the evidence boundary"* converging on the mother's *"live in the workspace, fixed at the seam"* — the cross-model convergence datum), the second return: **confirmation**, KNet's refinements accepted and banked (run receipts — capture-at-invocation, promote-at-acceptance, *sharper than the mother's own wording and credited as such* · the three integration profiles · the seven-authority ladder — the standing-authorization pattern at tool scale · the concurrency law · the expanded act chain), with the mother-side constraints stated (live edit legitimate on working surfaces because working trees are pre-canon, C-062; **the tool never commits** — C-025/C-084 untouched; tool authorship carried by receipts, the C-114 writer-identity path optional; the working-generation label `working`, never `anchored`; the uninstall floor = resolvability; a future live bridge = the served-live surface class, C-021-deferred).
5. **The C-096 git observation (read-only):** KNet commits correctly per close (twelve Return Packages ↔ close commits, C-025-conformant; author/committer conformant; honest pin notes on its untracked trio) but **`main` stands at 2026-08-17 19:32 while five close-commits stack on `codex/q07-preregistration-package`** — GitHub's default view shows the stale branch; three landed branches unretired; commits unsigned (posture undecided-not-recorded); the standing-commit-authorization record unverified. Findings returned as data (`Interface/Handoff_2026-08-19_Git-Observation-KNet.md` — merge-at-close vs. recorded branch doctrine, KNet's own call); the generalizable client lesson harvested mother-side same-session: **Client Library v0.6** (the branch-landing rule beside the close-scope rule — the twice-missed-trigger lesson applied deliberately).
6. **The birth of the interface place:** `Interface/` at the repo root — the index (`Interface_Index.md`: place declaration, status grammar standing/spent, IF-0001–0004, the known-counterpart line; the pre-place `Site/Upstream_Advisory_*` packets stay at their frozen-cited paths) + the four standing hand-offs moved/created in (gas · both returns · the git observation). One direction now routes to everything standing — the index doing its §9 job on day one. **Registry v0.6** in the same act: the **Fractal Interface Protocol** forging row (the §8-permitted listing, landed at the birth with its distillation trigger named).

## 2 · C-122 — the mirror doctrine (the human projection spine)

**The user-document trio is the human's projection spine of the context system, mirroring the agent's pole for pole:** the **Roadmap** is the Global Context's human projection (the stable pole — vision + trajectory at phase grain) as the **Agenda Board** is the Local Context's (the dynamic pole — the state of work at session grain; its projection stamp was already literal doctrine, C-047/C-048 — C-122 supplies the other pole): **two reader-matched projection spines over one canonical substrate**, sharing the **fieldnote buffer** (C-121) as the common write-surface; the **Fieldnote Handout** is that organ's manual. **The sorting rule** (Max's, from the instrument placement that forced the question): a change to the *path* lands on the stable pole, *work* lands on the dynamic pole, *what surfaces along the way* lands in the buffer — for both readers. Recursive simplicity applied across **audiences** (the Interface Foundation §2 move applied at the reader scale). Executed: the role declarations + trio bar in all three documents, Global v0.6 §3, the site section, this row. The trio bar's grammar entry rode the C-094 field trial into Agenda Board Format v0.2 the same day.

## 3 · The concept-tier acts (C-086 class — ratified in-conversation, no new numbers by the v0.2 precedent)

**Interface Foundation v0.3** — the interface-place amendment: §9 the communication RAM (every instance's own deliberately-readable place + navigation index; **pull, never push** — nobody writes into foreign trees; declaring the place = joining the served-surface class, which is what keeps a child's read of the living mother C-109-clean; envelopes are RAM — owner-controlled window, absorb-and-cite the reader's duty; the manual → index-polling → automated trajectory, the e-mail analogy); **§8's inbox placement superseded in place, its C-031 semantics carried**; §10 the KNet-contributed execution semantics (credited — the reverse-inheritance channel's richest specimen yet). **Interface Foundation v0.4** — the installed-integration correction on Max's objection: the two connection modes (installed = the default at every phase, live working data, no jurisdiction boundary crossed — C-109 scoped back to children-reading-the-living-mother; remote = cross-jurisdiction inputs travel pinned); **the sandbox is an authority boundary, never a freshness boundary; live in the workspace, fixed at the seam**; distribution orthogonal to connection; install/de-install = the hangar's sandbox-clean grammar; the first return's §3.4 corrected in the same act (the overreach was mother-authored, owned as such in both returns). **The instance choice recorded:** the mother's place is `Interface/` (repo root), born on Max's word.

## 4 · Notes, banked, and open

- **Banked for the Foundation's next touch (credited to KNet):** the run-receipt formulation (capture-at-invocation) · the integration profiles (Observe/Collaborate/Live edit) · the seven-authority ladder · the concurrency law · the expanded act chain. **Banked as an open custody sub-question:** attribution of tool-produced bytes (recommended default: receipts carry authorship, the committer stays the host's authorized session; a C-114-minted tool writer identity an instance option).
- **The transport-model lesson, owned:** the mother's first framing over-carried (copy-the-file); Max's process question straightened it to the §9 shape — and KNet had independently proposed the same shape plus the optional notification envelope (the third convergence of the day). The cross-model four-eye practice (Anthropic mother-side, OpenAI child-side, Max above both) caught one real overreach in each direction this session — recorded as a working practice worth keeping (C-094 class).
- **Drag gauge:** Context Index (+44 over budget) and Local Context (+53) YELLOW at this close — compaction due at the next phase boundary (the beta-0.6 tagging close the natural moment); advisory, not blocking.
- **Conversation Settings (C-071 walk question):** no conduct-touching decision — C-122 is projection-structure doctrine, the interface acts are protocol/repo structure. Settings hold at v0.8; the banked conduct-candidate bench stands at six.
- **Pending on Max (delta):** carry the interface-index direction to KNet (one address now routes IF-0003 + IF-0004) · the rest of the standing list unchanged (grooming session · Impressum · profile acts · annex remote · Safari pass · Releases/immutability · the gas hand-off's KNet-side landing).

## 5 · The ripple (this close)

Register → **v0.79** (the C-122 row; OQ-35/OQ-36 opened; OQ-28/OQ-34 annotated; this history row tool-minted) · Global Context → **v0.6** (§3 the mirror; in-session) · Local Context → **v0.78** (session row 27; queue re-headed: the interface build joins the head) · Context Index → **v0.47** (Interface Foundation v0.4 row · Board Format v0.2 row · the interface-place row) · Rule Overview → **v0.48** (the board row to Format v0.2 + C-122 ref) · Agenda Board restamped (twenty-seventh chip; the trio bar riding since the pass) · **New canonicals:** `Fractal_Agenda_Board_Format_v0.2` + this protocol (both DOC-minted at this close) · **Reissues:** Interface Foundation v0.3+v0.4 · Client Library v0.5+v0.6 · Registry README v0.5+v0.6 · GENESIS §5 pointer currency (v0.12 at stamp) · `genesis.py` ships Board Format v0.2 · **Born:** `Interface/` (index + four hand-offs; two new returns + the git observation authored this session) · `Return_Package_2026-08-19_Interface-Birth-Workdown.md` issued. Gates: `verify.py` + `check_versions.py` green pre-commit; buffer 4/10; no pack — **the beta-0.6 tagging close remains next**, now also carrying Board Format v0.2, Fieldnote Format v0.2, and the interface foundation into the release.

---

*Author: Claude (AGENT.AI.CLAUDE) · Twenty-seventh Code session, opened `/fractal`, closed on Max's `/close`. Next: `/fractal` → **the interface build** (KNet's place + the index format standardization — the queue's head beside the grooming pass) → the beta-0.6 tagging close → PR's birth. Parent: v0.60.*
