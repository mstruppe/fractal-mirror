# Fractal Governance Protocol — Claude Series v0.47

**Topic:** The sixteenth Code session (2026-08-16 → 17) — **the publishing-identity session.** Max opened it by deciding the banked weighing: *"we're going with the option that a mirror will be the public version of fractal"* (**C-105** — mirror-as-home ratified, with the dedicated email, proof-in-reserve as the ownership answer, and the friend beta set aside on the pace asymmetry). The decision executed the same session: the domain **`knet.network`** acquired, **`fractal@knet.network`** recorded as the public contact identity and joined to the custody principals, **the public chain's first anchor minted** (the mirror's own signed `beta-0.3` tag + OTS receipt under the public identity), both front doors retargeted, and the accepted-exposure call recorded on the personal address. Then **move 2's first execution**: the site rebuilt from the approved capture on Max's design brief (CAP1-FLD-1/FLD-2 cured). Before the flip, Max commissioned the missing test — *"we lack the stranger test. let's simulate an onboarding"* — **Stranger Test #1** ran against the public beta-0.3 tree, passed end-to-end, and its six findings were cured **in the generator the same session** (GENESIS → v0.7 with the C-104 board rider, CAP1-PROD-1/2/3 cured; Onboarding Protocol → v0.2; `genesis.py` gains the interview carry). On the findings' heels Max designed **C-106 — the named opener** (entry declares jurisdiction, the close stays universal; one conversation, one open loop). The flip and hosting are **deliberately postponed** on his call; **beta-0.4 packed at this close** so the cures ship; the ultracode polish flight (capture v1.2 + more onboarding simulations + site refinement) queued as the next session's head.
**Status:** Ratified (2026-08-16/17, in-conversation per C-033 — the verdicts quoted per decision below; the close Max's explicit command: *"go, let's close this session, the publishing postponed once more"*) · **Date:** 2026-08-17 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.46

---

## 1 · The decisions

### C-105 — The mirror is the permanent public home (the banked C-098 amendment ratified)

Max's verdict, verbatim: *"we're going with the option that a mirror will be the public version of fractal. with this mirror I will use a custom email address dedicated to fractal. the concern about identity and ownership resolves as making the original public is always an option if needed. with the mirror we deliver a stable standardised and versioned form of fractal, as anyone can take it and make it their own, there will always be one address for a base line, equivalent to a back to default settings act."* And the friend beta: *"the beta test with the friend might take some time, we work so fast that waiting for someone to have time takes too long to wait. we proceed with the publishing, so we can put the friend package aside."*

**Executed in-session:** the Register recorded (v0.50–v0.52); CAP1-HUNT-1 **dissolved structurally** (the raw flip never fires; `Site/` never ships); CAP1-PROD-4 **fixed** (both front doors retargeted to the mirror — the mother's README and the mirror's live copy); the domain `knet.network` acquired by Max (`knet.com` unobtainable — MarkMonitor-held since 1999; `knet.network` the standout: the TLD reads as the acronym's expansion); **`fractal@knet.network`** the public contact identity (receiving via routing stands; send-as deferred — the repo never sends mail); the address joined `.allowed_signers` as the custody key's fourth principal (both repos); **the mirror's own anchor chain opened** — signed `beta-0.3` tag, tagger the public identity, cross-referencing the mother's anchored tag, OTS receipt committed (`Provenance/beta-0.3-public.txt` + `.ots`); the **"fractal by KNet" umbrella contingency** recorded (the Claude-from-Anthropic shape, correct under both naming futures); the **accepted-exposure call** on `max.struppe@gmail.com` in the public record (Max: *"I take the eccepted exposure"* — the CAP1-ORIG-10 precedent class, flagged per always-flag; no scrub, the record ships whole). The KNet repo's accidental public window closed by Max (flip to private verified 404). *Class: instance (FRACTAL's own distribution posture; the pattern — a curated public mirror as standing home with its own anchor chain — recorded as reusable practice).*

### C-106 — The named opener: entry declares jurisdiction; the close stays universal

Max's design, verbatim: *"in your case I could determine with /fractal if I work here or /knet if I use claude in KNet, of course this has to somehow translate to any vendors specification but the wording stays the same. therefore the opening command becomes load bearing I guess? it determines the entry to the project while close simply closes any loop that was opened."* Ratified: *"go."*

**The rule:** the entry command is the **project's own name**, lowercased (`/fractal` here; each newborn its own) — entry is a jurisdiction declaration, so it carries the name; closing is jurisdiction-free, so one universal close suffices. The word is the command; each vendor's trigger is adapter-tier (C-097). `genesis.py` writes the named opener (reserved-collision fallback `/orient`); existing children adopt by their own recorded decision (KNet's line waits child-side). **With it the loop rule: one conversation, one open loop** — cross-instance *reading* mid-session is the observation window (C-096); a true jump is *close here, open there* (the C-091 instruction-bleed hazard made structural). The mother's `/fractal` was the field-proven original; the generator had shipped a generic `/orient` — the field split cured. Gated by scratch birth (`/gatethree` shipped, clause spans C-106, both checkers green). *Class: kernel (the clause spans it). The loop rule is additionally banked as the fourth conduct candidate for the next Settings reissue.*

## 2 · Also of this session (recorded, no numbers)

- **Move 2's first execution — the site rebuilt from the capture:** Max's design brief collected via an interactive draft form (black-and-white luxury-minimal, sans, both modes, content floating in space, mostly-empty arrival, Apple-shape navigation); `Site/fractal-site.html` rebuilt with **capture-verified wording only** (hero quotes verbatim; the four strongest claims in the dossier's cold-open order; the entry-44 honest-limit caveat riding the memory claims per the dossier's travel rule; volatile numbers structurally avoided; no Workbench material) — **CAP1-FLD-1 and CAP1-FLD-2 fixed**. Verified in both modes by headless render; a `noscript` fallback added (a real bug the verification surfaced). Setup made **agent-neutral** on Max's call (the README-BETA minimums posture; Claude Code the tested reference; the plain-words fallback). The in-house-first build order held (Max's orchestrator question answered: third-party design tools sit outside the drone doctrine and their strength — writing copy — is the C-103 hazard; parallel design-candidate fan-out stands as the layer-two option).
- **Stranger Test #1 (the C-058 family's simulated-stranger shape; review at `Review_2026-08-17_Stranger-Test-1.md`):** commissioned by Max before the flip; a sandboxed persona session ran quickstart → `/welcome` → `/begin` → real `genesis.py` birth → newborn first-open, against the mirror at `beta-0.3`. **Passed end-to-end**; the green datum: no outside knowledge needed at any step (the GENESIS §0 acceptance test under stranger conditions; C-088 at 0 errors / 104 exempted in the field). Six findings (1 High: the interview's vision text thrown away by the tool) — **all cured generator-side the same session** (C-094): `genesis.py` gains `--vision`/`--focus`/`--human-email`; GENESIS → **v0.7** (clause current to C-105/C-106, the C-104 board rider executed, name grammar and identity mechanics inline — CAP1-PROD-1/PROD-2 cured); Onboarding → **v0.2** (findings pre-answered inline); the rail gains the handover skeleton. Re-gated by three scratch births.
- **The store README currency:** `fieldnote.py` (the seventh tool) and `stats.py` (convenience utility, its recorded status since the first drone flight) both named — CAP1-PROD-3 and MACH-1's listing cured.
- **The publishing postponed, twice, on Max's calls** (*"hold off the last step, no flip and host"* · *"the publishing postponed once more"*): the flip-and-host act stands fully staged (one visibility switch + Pages + DNS; the zero-cost path requires the repo public — recorded, not executed). **Queued as the next session's head, Max's commission:** the **ultracode polish flight** — updated-knowledge capture **v1.2** forged by a new multi-agent program, further onboarding simulations, the capture tested against the site's content, refinement, and suggestions for the site's open sections — "one final polish before going live."
- **beta-0.4 packed at this close** (the C-090 ritual): the stranger-test cures, C-105's identity layer, C-106, and the rebuilt site ship as the new baseline; the mirror rebuilds from it (its second own-chain anchor, under the public identity).
- **Banked:** the **fourth conduct candidate** (one conversation, one open loop) beside the standing three · the curation-layer fixes (unchanged) · KNet's adapter ratification + board adoption + opener rename (child-side, Max's pace).

## 3 · Ripple

Register → **v0.56** (C-105 v0.50–v0.52 · site v0.53 · ST1 v0.54 · C-106 v0.55 · ratification pass v0.56) · Rule Overview → **v0.38** (the opener rule; the public-home posture) · Local Context → **v0.66** (session row 16; agenda re-headed to the polish flight; checklist marks) · Architecture State → **v0.23** (§6: C-105 instance, C-106 kernel — ~81·9·17) · Context Index → **v0.35** (GENESIS v0.7 + Onboarding v0.2 + Command Index v0.4 rows current; sixteenth session appended) · Agenda Board regenerated · Return Package issued · GENESIS's series DOC revised to v0.7 (living root document — revise chain, C-061's series-pointer class) · the Stranger Test review DOC-minted + this protocol DOC-minted · both checkers green · commits per change-set through the session, the close commit, the pack commit · push (C-064). **Unchanged with reasons:** Settings v0.8 (C-071 asked, answered **no** — C-105 is distribution posture, C-106 command-tier doctrine, the C-084/C-090/C-098 precedent class; **four** candidates now banked) · Global Context v0.4 (**deliberate** — the transition candidate fires at "the public home goes live," postponed with the flip) · BOOTSTRAP v0.21 (no mother-ritual change) · Schema/Template/Contract (S3-2.2 holds) · both client adapters v0.9 (no surface-conduct change; stamps current at Settings v0.8; the annex routing rule survives C-105 untouched — the amendment changed the flip's endgame, not the routing).

## Document identity

| Field | Value |
|---|---|
| Document Title | Fractal Governance Protocol — Claude Series v0.47 |
| Document Type | Governance Protocol (historical artifact, dated identity — C-012) |
| Version | v0.47 |
| Status | Ratified (2026-08-16/17, in-conversation per C-033) |
| Domain | GOV |
| Prepared By | Claude |
| Reviewed By | Max (2026-08-16/17, this conversation — the mirror-as-home verdict, the domain acquisition, the accepted-exposure call, the design brief, the stranger-test commission, "go on all", the flip hold, the opener design, and "go, let's close this session", all his, quoted above) |
| Date | 2026-08-17 |
| Parent Protocol | Fractal Governance Protocol — Claude Series v0.46 |
| Related Documents | Review_2026-08-17_Stranger-Test-1.md; GENESIS.md v0.7; Fractal_Onboarding_Protocol.md v0.2; Fractal_Command_Index.md v0.4; Site/fractal-site.html (rebuilt); Provenance/beta-0.4.txt(.ots) (this close); fractal-mirror: Provenance/beta-0.3-public.txt(.ots) + .allowed_signers (the public identity); Return_Package_2026-08-17_Publishing-Identity.md |
| Document ID | DOC-01M06E62J5M3H1BAH12T18P7VH (minted at this close via `close.py --create`; stamped by the post-mint revise) |
