# Fractal Governance Protocol — Claude Series v0.27

**Topic:** `close.py` canonized (C-073) — the second drone flight lands: Codex built the close-ripple generator on a bounded brief, the governing surface gated and merged it, and the tool's **first supervised `--write` generated this very close's store events**. The close ritual's mechanical tail (the observed cost driver: ~20% of a 5-hour usage window) is now one command; judgment — prose, versions, protocols, ratification — stays with the governing surface, exactly the C-066 boundary.
**Status:** Ratified (2026-08-14 — ratified in the conversation it records, per C-033) · **Date:** 2026-08-14 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.26

---

## 1. Context

Same conversation as v0.24–v0.26. After the double close, Max observed its cost ("closing the loop took 20% out of my 5h usage limit — should we outsource this to codex?"). The assessment split the close into judgment (irreducible, governing-surface work) and the mechanical ripple (deterministic, the bulk of the cost) — and the C-066-conformant answer: **the drone builds the automation; it never performs governance.** Max: "yes go ahead" (the existing Codex drone). The brief specified the tool against the Schema §2.1/§3.7/§4 grammar with `verify.py` as the oracle, scratch-copy-only testing (the live store read-only to the drone), and the standard gate.

## 2. The flight (execution record, C-066's second exercise)

- **Brief flown on `drone/close-tool`:** one commit `99c4aa3`, author `Codex <codex@fractal.local>`, **signed** (repo-local key); one file, `Claude/Knowledge Graph Store/close.py`, 486 lines, stdlib-only.
- **Gate:** both checkers **PASS on the branch** (zero warnings).
- **Independent acceptance (governing surface, scratch copy):** plan-mode clean baseline · single-edit detection with correct hash pair and `supersedes` · `--write` appends one revise + syncs the node, both gates PASS · idempotence (re-plan: nothing) · `--create` mints create + route alias + two places + node stub, gates PASS. All green.
- **Code review:** dry-run default; append-only (never edits a log line); monotonic ULID clock (coherence rule §2.1); mandatory one-line notes per changed doc; C-061 series-pointer ripple on `--create` (per-version alias must match the filename stem); repo-escape path guards; gates auto-run after every write.
- **Fast-forward merged** to `main`; push per C-064.
- **Flight-2 data (→ OQ-27):** committer = **Codex** this flight (flight 1: committer Max) — commit still signed by the repo-local key; the committer-identity convention is unpinned and recorded in the ledger. A frozen-file guard is a noted future hardening (the tool would propose a revise if a frozen artifact were ever edited; the reviewed plan output is the current guard).
- **First-run finding, cured in-close (governing surface):** `--create` refused the **pointer-less series** class — the protocol series is per-version only (C-061 kept series pointers for the specs), and the drone's guard treated zero pointers as an error rather than "nothing to revise." One-guard cure applied on the governing surface (zero → proceed without a series revise; more than one still refuses), the tool-evolution path every canonized tool follows. The mint then ran clean — this protocol's own DOC is the tool's first minted identity.

## 3. Decision of record

- **C-073 — `close.py` canonized: the fourth store tool.** Joins `verify.py`/`mint.py`/`check_versions.py` (C-050/C-060 family; C-006-exception code artifact, **not** DOC-minted — tool class, C-042 mint-on-reuse). Scope: the close's **mechanical tail only** — changed-doc detection by hash against the node bindings, `revise` generation with coherent ids and correct supersedes chains, node `content_hash` sync, the C-061 series ripple and DOC minting on `--create`, and both gates after every write. **Judgment is never delegated:** version bumps, prose, protocols, and ratifications remain governing-surface acts; the tool is run by the governing surface, dry-run first, plan output reviewed before `--write`. Both checkers remain the only gate (C-060). *(Working Decision.)*

## 4. Executed this close (one `[KG]` commit, C-037; push = Claude, C-064)

- Merge as §2. **Store events generated by `close.py` itself — its first supervised live run:** the protocol DOC minted via `--create`; the ledger/context revises via the changed-doc pass (Register → v0.24 with C-073 + the OQ-27 datum; Rule Overview → v0.22, close-mechanics row; Context Index → v0.19, tool list + series range; Architecture State → v0.7, §2 tools line; this protocol's own post-mint Document-ID stamp picked up as an ordinary revise).
- Local Context → v0.35; board restamped; Return Package postscript; both checkers green before commit. BOOTSTRAP, Global, Settings, adapters, Schema/Template/Contract: checked — unchanged.

## 5. Calls recorded (Max, 2026-08-14, this conversation)

1. **The cost observation and the outsourcing question were Max's**; the split (automate mechanics, never governance) was accepted with "yes go ahead," Codex named explicitly as the existing drone.
2. **Merge + canonization pre-approved** as the stated endpoint of the brief; plan approved before execution.

## 6. Ratification record (2026-08-14, in-conversation per C-033)

Max raised cost, approved the brief, flew it in Codex, and approved the merge-and-canonize plan. The governing surface gated, acceptance-tested independently, merged, and exercised the first supervised write. Ratified in the conversation it records; no caveats.

## 11. Open Questions (TBD)

*(Per C-034 these are entered in the Register's cumulative ledger.)*

- None opened; none resolved. **OQ-27 annotated** (flight-2 committer datum). **OQ-9 note:** the close's mechanical tail is now tool-assisted — still governing-surface-executed, so no C-008 promotion occurred; the next promotion question stays open. Standing items carry. The review ledger stays fully green.

---

| Field | Value |
|---|---|
| Document Title | Fractal Governance Protocol — Claude Series v0.27 |
| Document Type | Governance Protocol (historical artifact, dated identity — C-012) |
| Version | v0.27 |
| Status | Ratified (2026-08-14, in-conversation per C-033) |
| Domain | KG (recorded in the GOV history layer) |
| Prepared By | Claude |
| Reviewed By | Max (2026-08-14, this conversation) |
| Date | 2026-08-14 |
| Parent Protocol | Fractal Governance Protocol — Claude Series v0.26 |
| Related Documents | close.py (the artifact; tool class, not minted); AGENTS.md (stamp inside); Decision Register (stamp inside); Rule Overview (stamp inside); Architecture State (stamp inside); Local Context v0.35; Protocols v0.24–v0.26 (this conversation's series) |
| Document ID | DOC-01M011AXX9R92AG73J4Z5XRARG (minted 2026-08-14 via `close.py --create` — the tool's first minted identity; stamped in by the post-mint revise, per this protocol §4) |
