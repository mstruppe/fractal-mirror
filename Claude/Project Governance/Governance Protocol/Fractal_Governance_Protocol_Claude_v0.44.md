# Fractal Governance Protocol — Claude Series v0.44

**Topic:** Fourth postscript to the thirteenth Code session (2026-08-16, same conversation as v0.40–v0.43) — **the flip is called.** Max, verbatim: *"I have a pressing issue: we need to flip it. I talked with a friend and it solves a real need. I want to ship it asap to him, to test how well it keeps the high promises and expectations. Therefore we need to implement all open items we collected for the shipment design."* The ratified order of conduct (*engine first, then exterior*, per v0.37) is **re-ordered by the owner's primacy (C-031 — no new number; the order was his to set and is his to change)**: the flip preparation becomes the queue's head, pulled forward by a real external need — the first real stranger has a name. Beside the directive, the conversation's last stretch produced the **profile-mechanism design** (OQ-32's sharpest annotation yet) and the **multi-agent probe program** (recorded as a standing offer).
**Status:** Ratified (2026-08-16, in-conversation per C-033) · **Date:** 2026-08-16 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.43

---

## 1. The re-order (C-031 exercised — the flip becomes the head)

The v0.33 precedent applies (arc 2 was once pushed later the same way): a priority call by the owner is recorded, not numbered. **The near work is now the shipment-design bundle**, collected across the fieldnotes and postures, enumerated for the flip session:

1. **The friend shipment vs. the public flip — the triage that opens the next session.** Shipping to one named, trusted friend is the **friends-beta** (entry 39), not yet the public flip — it needs the testing chapter (items 2–5) but possibly not the public bundle (items 6–8). Max triages at the session's open; "asap" argues for the smallest honest shipment first.
2. **The onboarding interview** (entries 23/38) — the designed first loop with the three-stage guidance decay (interview → suggestive moves → nothing). The named project; the biggest single build item.
3. **The testing pipeline** (entry 39, refined): kernel `/fieldnote` in every newborn (GENESIS parameter 7 gets its door — genesis §3.6 grows), the **machine-parsable entry format** (the load-bearing piece — "the expected form is the whole interface"), collection by sent file; `fieldnote.py` grows the intake/parse half and graduates beside the store tools by ordinary decision.
4. **The C-097 harvest dependency:** KNet's `AGENTS.md` ratification completes the Codex/ChatGPT library entry — the friend may arrive on either vendor; the library should be ready for both.
5. **The guide surface for a real stranger:** README + `/welcome` + the per-surface pages (the fieldnotes' guide-implication backlog, 42 entries of material) — enough of it to keep the high promises the friend was given.
6. **The distribution unit** (entries 13/35): what ships — full repo vs. curated release (`export-ignore` the lean shape); the notebook and `Site/` decided together with the Workbench's shelf status.
7. **The legal bundle at need** (C-090 postures): license text finalized (free use, attribution preserved — the recorded posture), the compound name / trademark posture, branch protection (dormant → its named trigger is exactly this flip).
8. **The public posture itself** (flip-vs-mirror, with entry 13/35's two data) — only if Max flips publicly rather than shipping privately first.

## 2. Also of this conversation-tail (recorded, no numbers)

- **The profile mechanism (OQ-32 annotated):** the agent profile grows two dimensions beyond the role — **model** (engine, with caveats) and **account** (whose access — the custody-adjacent dimension). Recognition = **declaration at orientation** (identity · model · account; the handover records the triple; an unknown model/account is the recognition event and triggers the mint conversation). Verified fact of record: `verify.py` already **enforces mint-before-use for agents** (an event's actor must be a minted AGENT code — the "profile must preliminarily exist" behavior exists at the store layer today); what's missing is only the declaration ritual and the two profile dimensions. Attribution stance unchanged: declared engine is attestation, custody stays the signature (C-074).
- **The multi-agent probe program (standing offer):** run bounded probes with more engines in KNet — each probe = orientation + small task + `/fieldnote`; each green probe = a Client Library entry at harvest; multi-writer reality = OQ-32's data supply. The `AGENTS.md` convergence (entry 17) makes marginal probe cost near zero. Research stays primary; probes are side-sessions.
- **Entry 42 + Library v0.2** stand committed from earlier in this tail (the dissolved mode seam — the C-094 currency loop's first library exercise).

## 3. Ripple

Register → **v0.47** (OQ-32 annotated — the mechanism; the probe program noted as its data supply) · Local Context → **v0.63** (the agenda re-headed: the flip bundle is the near work) · board regenerated (the Next card re-headed) · this protocol DOC-minted at the close · push carries all (C-064). **Unchanged with reasons:** Settings v0.8 + adapters (a priority re-order and design discussions touch no conduct — C-071 asked, answered *no*) · Rule Overview v0.35 · Architecture State v0.20 · Index v0.32 · BOOTSTRAP v0.20 · Schema/Template/Contract (S3-2.2 note: the model-property event field is a *candidate* for the next real Schema reissue — the finding's named trigger may finally be approaching) · store tools · Global Context v0.4 (**deliberate**: the flip *called* is not the flip *executed* — §2 moves on completed fact, the OQ-4 pattern; the flip session may move it). **No separate Return Package** — the postscript pattern; this protocol + the Local Context §Next carry the handover.

## Document identity

| Field | Value |
|---|---|
| Document Title | Fractal Governance Protocol — Claude Series v0.44 |
| Document Type | Governance Protocol (historical artifact, dated identity — C-012) |
| Version | v0.44 |
| Status | Ratified (2026-08-16, in-conversation per C-033) |
| Domain | GOV |
| Prepared By | Claude |
| Reviewed By | Max (2026-08-16, this conversation — the flip call his, verbatim) |
| Date | 2026-08-16 |
| Parent Protocol | Fractal Governance Protocol — Claude Series v0.43 |
| Related Documents | Site/Fieldnotes_2026-08-15_First-Shipping-Run.md (the shipment-design backlog, 42 entries); Fractal_Client_Library (stamp inside — v0.2); Decision Register (stamp inside — OQ-32); GENESIS.md (stamp inside — §3.6/parameter 7, the pipeline's build site); KNet (external — the harvest source, its own jurisdiction) |
| Document ID | DOC-01M05BAE2ZNWQTNHJ4BJDRQ8YH (minted 2026-08-16 via `close.py --create`; stamped by the post-mint revise) |
