# Fractal Governance Protocol — Claude Series v0.39

**Topic:** The workspace session (twelfth Code session, opened 2026-08-15, closed 2026-08-16). Max opened on an observed need — *"the terminal is not a good work environment … I would like a splittable interface where I can read documents, interact with AI agents, or even other advanced tools (last part I would leave conceptually open)"* — and the corpus answered that the need was already anticipated three ways (the archive's split-screen doc-viewer coupling, the v0.37 §5 branded-app route, OQ-23's forge trigger). On **"let's go with the WS forge"** the WS domain's first canonical document was issued — **`Fractal_Workspace_Foundation`** (C-086 class, second member) — and on **"yes build the workbench as webapp, try to replicate your UI capabilities"** the forge turned into **the first WS build slice in the same session**: the **Workbench** (`Workbench/` — folder system, reading surface with first-class PDF and **tracked markings**, session surface with the agent at its core, a browser as the open slot's first occupant), nested behind the FRACTAL homepage on Max's call. Adopted as **C-093**; **OQ-23 resolved**. A second act rode the session: Max reported **KNet's session-two close**, and the Knowledge Network Foundation took its **first external-evidence revision** (→ v0.2) — the network's first child naming dragons against the mother's own vision.
**Status:** Ratified (2026-08-16 — ratified in the conversation it records, per C-033) · **Date:** 2026-08-16 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.38

---

## 1. Context

The session opened by `/fractal` onto an event-driven queue (per v0.38: KNet's sessions, the grab demo on real content, the flip behind the engine; nothing pending). Max raised the working-environment concern for KNet research, explicitly leaning on move 2. The C-092 shelf check ran first and found the need *already specified*: Architecture State §4 carried the archive's spatial-workspace vision pointer-only (split-screen doc-viewer coupling named verbatim in Max's own 2026-spring UI note), the two-route distribution (v0.37 §5) had recorded the branded desktop app as OQ-23's likeliest igniter, and OQ-23 itself waited on the first WS build. What the concern added was the *driver*: the researcher's own working environment is an **engine** need (KNet's actual work), not merely move-2 exterior — the first real WS friction produced by arc 2.

## 2. The decision (C-093)

**C-093 — the workspace program: the WS forge executed, and the workbench is the first build slice.** Three parts, ratified in sequence:

- **The forge (OQ-23's firing):** `Fractal_Workspace_Foundation` issued at `Claude/Architecture/Concepts/Workspace/` — canonical concept document (C-086 class, second member; Draft, explicitly non-binding), the WS route's first canonical document, forged clean from Master Index Parts VI–VIII + the UI-vision note by the C-013 method, exactly as Architecture State §4 said it would be. It records: the terminal-fails-the-reader problem from arc 2's observed need; the accepted ground restated (C-068 entry, client-thin, C-017 ontology, C-079 tiering); the archive reservoir summarized with the archive as detail source; the **identity-coupling mechanism** as the load-bearing idea — panes couple through `resolve` and nothing else, so the reversible doc-viewer coupling of Max's old note is two renderings of one resolve, its "integrated ID system" condition now satisfied fact.
- **The slice (the build, ratified mid-flight):** the **Workbench** — a thin local web app in `Workbench/`. Pane roster fixed by Max's directive of record (*replicate the proven surface*): **folder system** (roots switchable — the repo and `~/Desktop/knet`), **reading surface** (Markdown typography; **PDF via PDF.js with a real text layer**), **session surface** (a real shell; the agent at the core — the Agent SDK chat-native pane is the recorded next tier), **browser** as the open slot's first occupant, the slot itself conceptually open per Max's instruction. **The markings USP** (Max: *"a USP would be if you could mark a section and then ask the agent about the marked section — the agent can track markings"*): select → **Mark** records to `Workbench/markings/markings.md` (gitignored working data the session's agent can read) → **Ask agent** hands the quoted passage into the session prompt unexecuted. **Homepage nesting** (Max: *"the workbench access should be nested in the fractal homepage"*): the served landing at `/` is the FRACTAL site (Site/ the source of record, which gains a Workbench section), the workbench mounts at `/work` — the root-galaxy entry idea in its smallest honest form.
- **The postures and boundaries:** technology **web-thin** (Node + browser page; Electron and the branded-app identity stay deferred to their own decision — the ChatGPT-era preferences held provisional, as the Foundation records); the slice is a **client** — panes speak the Navigation Contract's verbs, the store is never written from a pane, `resolve` is a per-request tier-0 fold of the log (no persisted index — C-079 honored); markings and `node_modules` are working data and environment, never knowledge (gitignored). Knowingly **OQ-29's first live test case** (code as a governed artifact, in-repo — the `Site/` precedent extended).

**Acceptance:** verified in-session on the governing surface (a UI slice has a human gate, not a machine gate — the recorded reason this was not a drone flight): identity-link resolution opened Settings from a rendered document via the store fold; PDF text-layer selection → marking recorded → passage handed to the session prompt, end-to-end; both roots served; homepage nesting live. Two defects found and cured in the pass: npm strips the execute bit from `node-pty`'s `spawn-helper` (cured + `postinstall` guard) and a pane failure could kill the server (spawn wrapped; a pane failure must never kill the workbench).

## 3. The second act — the child reports back (executed, no new number)

Max reported KNet's **session-two close** (the sourced research queue `R-01`–`R-12`; the child's `P-002` — distributional inversion as a load-bearing risk; a clean close that checked the remote directly, the session-one defect's cure now practiced habit; the child *refusing unverified citations because its own constitution demanded verified conduct*). Three findings cross the jurisdiction boundary and landed in the **Knowledge Network Foundation → v0.2** (its own refresh trigger — *"any session that materially advances the network vision"* — the ordinary loop): §7 gains the **child-named dragons** (distributional inversion, KNet P-002/R-08, empirically grounded and aimed at the vision's own distributional claim; the ledger-as-strategic-history attack, R-06); §2's increment thesis gains the **separability caveat** (R-02, armed); §5 names the **open verification-cost quantity** (R-12 — the scaling law's denominator, currently without a literature). Nothing resolves in the mother: the threads live in the child's queue and their answers transfer when they close. **OQ-31 gains the second child→mother transfer datum** — and a different kind than the first: v0.37's was governance doctrine flowing back; this is *research content* flowing back, precisely what arc 3's grab will mechanize. **No fieldnotes entry** — the report contained no onboarding frictions; both child sessions ran native-clean.

## 4. What this close deliberately does not do

**No Electron, no branded app, no Agent SDK embedding** — the vehicle decision stays deferred with its provisional preferences recorded (Foundation §6). **The Galaxy is not designed** — the workbench is the dense-interface half (archive ch. 28) shipped first, building toward the spatial environment, not past it. **The open slot's roster is not extended** beyond the browser — occupants arrive on observed need. **Global §2 is not moved:** the workbench is the first slice of "a working interactive interface" — Global §2's own named example of a next transition — but declaration is Max's (the OQ-4 pattern), and the walking skeleton is not the working interface proven; the comparison moment remains arc 2's completion. **KNet is untouched** — the report was received, not governed; the Foundation annotation cites the child as an external source in its own jurisdiction. **The beta-0.2 OTS receipt is not upgraded** — checked as the standing mechanical carry; it upgrades whenever Bitcoin finality arrives, any later session.

## 11. Open Questions (TBD)

- **OQ-23 resolved** — the forge executed on its named trigger (observed need; the first WS build). The row leaves the standing list; WS work henceforth flows through the Foundation and ordinary decisions into Architecture State §4.
- **OQ-29 annotated** — first live test case: the Workbench is code in the governed repo (committed source, gitignored working data, a close now walks it). The question itself (DOC identities for source files; modules as minted concepts) stays open — forged by use.
- **OQ-31 annotated** — the second child→mother transfer datum (research content: KNet P-002/R-08, R-02, R-06, R-12 informing the mother's Foundation v0.2).
- **Eight OQs stand** (OQ-4, 9, 20, 27, 28, 29, 31, 32); one review finding open (S3-2.2, deliberately held).

## Ratification record (2026-08-15/16, in-conversation per C-033)

Max's calls of record, in sequence: the concern stated (the terminal fails the reading researcher; the splittable interface; *"last part I would leave conceptually open"*) · **"let's go with the WS forge"** · **"yes build the workbench as webapp, try to replicate your UI capabilities"** · the pane roster named (*"agent UI, browser, terminal access, folder system, maybe a doc reader … with the agent at it's core"*) · the PDF + tracked-markings USP (*"the agent can track markings"*) · **"the workbench access should be nested in the fractal homepage"** · the KNet session-two report tabled for reception · **`/close`**. Ripple: `Fractal_Workspace_Foundation` **v0.1 issued** (DOC minted at this close) · `Workbench/` landed (+ `.gitignore` working-data lines, `.claude/launch.json`) · `Site/fractal-site.html` gains the Workbench section (derived projection) · Knowledge Network Foundation → **v0.2** · Register → **v0.42** (C-093; OQ-23 resolved; OQ-29/OQ-31 annotated) · Local Context → **v0.54** · Architecture State → **v0.17** (§4: the forge recorded, the canonical pointer added) · Context Index → **v0.29** (WS row active; Workspace Foundation entered) · board regenerated · this protocol minted. Push carries all of it (C-064).

## Document identity

| Field | Value |
|---|---|
| Document Title | Fractal Governance Protocol — Claude Series v0.39 |
| Document Type | Governance Protocol (historical artifact, dated identity — C-012) |
| Version | v0.39 |
| Status | Ratified (2026-08-16, in-conversation per C-033) |
| Domain | GOV |
| Prepared By | Claude |
| Reviewed By | Max (2026-08-15/16, this conversation) |
| Date | 2026-08-16 |
| Parent Protocol | Fractal Governance Protocol — Claude Series v0.38 |
| Related Documents | Fractal_Workspace_Foundation (stamp inside — v0.1, first issue); Fractal_Knowledge_Network_Foundation (stamp inside — v0.2, first external-evidence revision); Architecture State (stamp inside); Decision Register (stamp inside); `Workbench/` (working tool — first WS build slice); Site/fractal-site.html (derived projection — the gate, workbench nested); KNet (external — `github.com/mstruppe/knet`, its own jurisdiction) |
| Document ID | DOC-01M03QYTF9VZ4KR9HTCE7WHZSC (minted 2026-08-16 via `close.py --create`; stamped by the post-mint revise) |
