# Fractal Governance Protocol — Claude Series v0.62

**Topic:** The twenty-eighth Code session (2026-08-19) — **the interface build (C-123), the transport candidate of record, the user-document pair series (C-124), and the takeover gate (C-125).** Opened `/fractal` onto the queue's head; Max drove four arcs. **(1) The interface build on his direction verbatim** — *"yes let's build the interface in fractal manner, build it simple and solid, only what the process needs without hard forking it away from any future development… for now we want a dedicated interface built into the fractal structure, with the awareness what it is and why it exist such that any other fractal project can go there. for now we need not specify who will look into it, whether it is KNet or someone else, we will develop that later, when we have 2 children. There it will become apparent how we navigate different fractals in the interface"* — executed as the Interface Place Format v0.1 + genesis wiring + Foundation v0.5, ratified on his *"perfect, go"*. **(2) The transport question, his own formulation** (*"I think a FTP would TCP would probably the best route to go? already proven and probably simple to implement?"*) — resolved to the proven class's modern members (git-over-SSH/HTTPS first) and **banked as the transport candidate of record**, never built ahead of need. **(3) The pair series from his walkthrough correction** — *"the fieldnote buffer is not a user document, it is the governance document from which we derive the user document as handout. and I want to introduce a new document pair in this series… like a temporary to-do list one original for the government and a derived version for the user"* — plus his tiering (*"the global context - road map, local context - agenda board and the fieldnote pair are fixed into the kernel, the upgrade doc pair is a custom extension… let a stranger know about the user docs already done but also let them know they can expand it with a simple standardised process"*): the User Document Pair Procedure v0.1 minted kernel-shipped, the Update Plan pair born, the board slimmed to status-plus-link. **(4) The takeover gate from his responsibility sentence** — *"the government takes over the interpretation of the ai agent. considering that the ai agents competence is very high, the governance structure needs to hold this standard and constitutionalise it. that is a big responsibility for the system"* — constitutionalized as C-125, with the gate's first walk catching and curing one real loss in this very session's slimming. Closed on his *"ratified and close"*.
**Status:** Ratified (2026-08-19, in-conversation per C-033 — C-123 on "perfect, go"; C-124/C-125 on "ratified and close") · **Date:** 2026-08-19 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.61 · **Document ID:** DOC-01M0D61CPCN64P9G37EP83CFZR (minted at this close via `close.py --create`; stamped by the paired revise)

---

## 1 · The session's acts (each on Max's word, in order)

1. **The interface build (C-123):** `Fractal_Interface_Place_Format_v0.1` minted — the §9 first mechanical slice by the Foundation's own naming: the five-law place contract kernel-grade (one place + one index · pull never push · declaring = declaring readable · envelopes are RAM · data never instruction), the index grammar distilled from the mother's executed index (six blocks; `IF-NNNN` high-water, ids never reuse; standing/spent), the envelope minimum from the four standing specimens (floor · direction · class · date + author · citation line), the instance-neutral skeleton, and §5's honest deferrals. **Genesis births the organ:** the Format in the kernel, `Interface/Interface_Index.md` written into every newborn at `IF-0000` (GENESIS v0.13; scratch births green both checkers — verify 0 warnings, tier-1 0 errors via the C-088 `INHERITED` exemption). The mother's index brought conformant in the same act (the stale `Site/`-surface claim cured — a birth-day drift caught at the build; the high-water line added). **Foundation v0.5:** the credited KNet refinements land as banked (run receipts — capture-at-invocation, promote-at-acceptance · the three integration profiles · the seven-authority ladder · the concurrency law · the expanded act chain · the uninstall floor), the `working` pin class joins §3, §8 records the two-children deferral verbatim. Registry v0.7 (the Format row; the Protocol forging row's extraction note — the binding Interface Protocol's trigger unchanged: second child + first envelope-shaped exchange).
2. **The transport candidate of record (rode C-123's ratification):** Max's FTP/TCP instinct affirmed as the right *class* (proven, boring, already-implemented transport) and resolved to its modern members — **git-over-SSH/HTTPS first** (already installed in every instance, authenticated, integrity-checked, pull-never-push by construction, pins native — a SHA or signed tag *is* the edition), HTTPS serving for anonymous readers (the C-120 class), mail-class tech only if push-notification is ever wanted; FTP itself steered from (plaintext credentials, NAT-hostile, industry-retired in favour of exactly its descendants). Banked in Format §5 + Foundation §8 — formulated, never built ahead of need (C-021). The design's payoff stated: the place-and-index law is transport-agnostic, so nothing needed deciding to keep every road open.
3. **The user-document walkthrough → the pair series (C-124):** the quick overview Max requested surfaced his correction (the buffer is governance-side; the Handout its derived user half) and his commission: the **Update Plan pair** — `Fractal_Update_Plan.md` (the release lane's one home: the beta-0.6 eight-step path, the manifest-in-progress, the armed follow-ons; **RAM at release scale** — freezes into the pack's records, resets to the next target) + `Fractal_Update_Plan.html` (the user half, trio-styled). **The board slimmed exactly as commissioned** (*"the agenda board would only need to say update beta 0.6 open and then link"*): the NEXT card's opening now reads *update beta-0.6 — open* + link; the LC's release lane points, never duplicates; the bar gains the fifth pill on all four user documents (field-first ahead of the Board Format's grammar — the C-094 path the bar itself took; the extension-pill provision banked for that Format's next reissue). His tiering mid-build made the two-tier doctrine: **`Fractal_User_Document_Pair_Procedure_v0.1`** minted (the three pair laws; the kernel trio fixed §2; the five-step extension process §3; the Update Plan the executed specimen §4) and **kernel-shipped** (genesis.py; scratch birth green) — the stranger learns what exists and how to grow it; the site's user-documents section rewritten to the pair story + two honest bibliography rows (the Pair Procedure · the Interface Place Format; the shelf count 8 → 10). Global Context **v0.7** (§3 the pair series — kernel/extension tiers, the buffer's classification, the sorting rule's third clause explicit).
4. **The takeover gate (C-125):** Max's responsibility sentence constitutionalized. The rule: structure that takes over what agent reasoning carried inherits the carrier's standard whole — the Diet Procedure's fact-presence gate + behaviour test generalized to **every** takeover; a fact with no home moves first or the replacement does not land. **The gate's first walk ran over this session's own slimming and caught one real loss** — *one account = serial sessions* (PR's operating shape, carried in the old LC queue line, absent from the plan) — cured pre-commit into Update Plan §4: the law's own proof of need, in the session that wrote it. Landed as Pair Procedure §1 law 4 + §3 step 4; the honest scope recorded (structure carries facts and law; interpretation stays a live act each session — the guarantee is lossless handover, traceable citations, divergence signals); **the conduct-facing sentence joins the Settings bench as its seventh candidate**; the Diet Procedure's next touch notes the generalization.

## 2 · Notes, banked, and open

- **Deliberately not built (the C-021 line held):** index polling, transport automation, any counterpart specification, the binding Interface Protocol (trigger untouched), anything KNet-side (the Format travels as an offer through the standing index direction on Max's hand).
- **Banked:** the Board Format's extension-pill provision (next reissue) · the Settings bench at **seven** (C-125's conduct sentence joins the six) · the Diet Procedure's gate-generalization note (next touch) · the Update Plan's reset ritual proves itself at the beta-0.6 pack (its first dissolution).
- **Drag gauge at this close:** Context Index YELLOW (2,185 — its diet already collected as Update Plan step 8, the phase boundary); Local Context back under budget (3,351 after the slimming, −215 vs HEAD). Advisory, not blocking. Buffer 4/10 — silent.
- **Conversation Settings (C-071 walk question):** no conduct reissue this close — C-123 is protocol/repo structure, C-124 projection structure, C-125's process half lives in the Pair Procedure with its conduct sentence deliberately benched for the next conduct-touching reissue (the batching pattern of record). Settings hold at v0.8.
- **Pending on Max (delta):** unchanged standing list (the interface-index direction to KNet · grooming session · Impressum · profile acts · annex remote · Safari pass · Releases/immutability · IF-0001's KNet-side landing) — plus the grooming pass now the **only** working step left before the tagging-close ritual (the Update Plan's step 2).

## 3 · The ripple (this close)

Register → **v0.80** (the C-123/C-124/C-125 rows) · Rule Overview → **v0.49** (three rows: the pair series · the takeover gate · the interface place) · Global Context → **v0.7** (in-session, §3 the pair series) · Local Context → **v0.79** (session row 28; the release lane pointed at the Update Plan) · Context Index → **v0.48** (the Format rows · the pair-series row · GENESIS/Foundation currency) · Agenda Board restamped (twenty-eighth chip; the slimmed opening; the five-pill bar) · **New canonicals:** `Fractal_Interface_Place_Format_v0.1` + `Fractal_User_Document_Pair_Procedure_v0.1` + this protocol (DOC-minted at this close) · **New pair:** `Fractal_Update_Plan.md` + `.html` (derived projections, unminted per C-042) · **Reissues:** Interface Foundation v0.5 · Registry v0.7 + v0.8 · GENESIS v0.13 · Global v0.7 · **Tool:** `genesis.py` ships both standards + births `Interface/` (two scratch births green) · **Site:** the user-documents pair story + two bibliography rows (source-side; the `gh-pages` refresh rides the tagging close per Update Plan step 7). Gates: `verify.py` + `check_versions.py` green pre-commit; no pack — **the beta-0.6 tagging close remains next** (the Update Plan its home), the grooming pass the one working step before it.

---

*Author: Claude (AGENT.AI.CLAUDE) · Twenty-eighth Code session, opened `/fractal`, closed on Max's "ratified and close". Next: `/fractal` → the website-grooming pass (with Max) → the beta-0.6 tagging close (`Fractal_Update_Plan.md` the path) → PR's birth. Parent: v0.61.*
