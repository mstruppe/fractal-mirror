# Fractal Governance Protocol — Claude Series v0.43

**Topic:** Third postscript to the thirteenth Code session (2026-08-16, same conversation as v0.40–v0.42) — **the first `/fieldnote` wave, and the determinism ruling.** The new capture channel was used five times in one afternoon (entries 37–41) and improved itself once under real failure: after entry 41 arrived without a target word, Max ruled — *"I forgot the target 'knet' which triggers an urgent need to make the target deterministic. the command should have not been triggered and come back as error no target"* — and the cure landed within the hour: **`Site/fieldnote.py`**, the machine half of the command (target resolution a fact; missing/unknown target → hard error, nothing written; formatting stays the AI surface's judgment — the C-073 division). Ruling recorded **un-numbered inside C-094's scope** (the C-090 license-posture precedent); verified live at the tail: an empty `/fieldnote` bounced with `no target given`, capturing nothing.
**Status:** Ratified (2026-08-16, in-conversation per C-033) · **Date:** 2026-08-16 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.42

---

## 1. The ruling (un-numbered, C-094 scope)

**`/fieldnote` routing is deterministic.** The roster's authority is machine-readable inside `Site/fieldnote.py`; the command file's table is a projection of it. A capture without a resolvable target **does not happen** — no fallback interpretation, ever. The tool appends the verbatim, timestamped raw block; the AI surface expands it into the ledger format afterward. Graduation trigger recorded in the tool itself: it moves beside the store tools by ordinary decision when the friends-beta adds the intake/parse half. (Command Index → v0.2 in the same pass; the graceful-degradation alternative was explicitly **rejected by Max** after it silently absorbed entry 41's missing target.)

## 2. The field wave (executed — captures, no numbers; the ledger is the record)

Entries 37–41, all via `/fieldnote`, all pre-canon (C-062): **37** — the preface idea (a FRACTAL standard for analysing papers; per-user marginal value). **38** — the **outsourcing pivot** (*"the governance is fractals business"* — C-092 generalized to the whole tool layer; the vehicle decision reframed) + the **founder-bias observation** (*"fractal is built after my mind"*) with its fade-curve caveat (interview loop → suggestive moves → nothing). **39** — the **friends-beta design**: kernel `/fieldnote` in every newborn (GENESIS parameter 7 finally gets its door), machine-parsable entry format, collection by **sent file in expected form** (pull/consent-clean; no phone-home). **40** — **Zotero adopted in the child (P-003)**: the pivot's first execution; the preface matured into the **paper passport** (immutable original · sidecar record · reading copy) — the child independently rediscovering content-hash provenance; the standards library's first concrete member forming in the field. **41** — **the USP, felt**: *"usually you're vendor bound to get memory within projects"* — the second-client trial's result as a pitch sentence; move-2 lead positioning candidate.

## 3. Ripple

Register → **v0.46** (C-094 row annotated: the deterministic split + the friends-beta intake as its recorded future) · Local Context → **v0.62** · board restamped · this protocol DOC-minted at the close · push carries all (C-064). **Unchanged with reasons:** Rule Overview v0.35 (no rule added — a C-094-scoped tooling ruling) · Settings v0.8 + adapters (no conduct touched — the C-071 question asked, answered *no*) · Architecture State v0.20 (`fieldnote.py` is mother-side working tooling in `Site/`, pre-graduation — not a store tool; the six-tool claim stands) · Index v0.32 (no routing change) · BOOTSTRAP v0.20 · Schema/Template/Contract. **No separate Return Package** — the postscript pattern; this protocol and the Local Context carry the record.

## Document identity

| Field | Value |
|---|---|
| Document Title | Fractal Governance Protocol — Claude Series v0.43 |
| Document Type | Governance Protocol (historical artifact, dated identity — C-012) |
| Version | v0.43 |
| Status | Ratified (2026-08-16, in-conversation per C-033) |
| Domain | GOV |
| Prepared By | Claude |
| Reviewed By | Max (2026-08-16, this conversation — the ruling his, verbatim) |
| Date | 2026-08-16 |
| Parent Protocol | Fractal Governance Protocol — Claude Series v0.42 |
| Related Documents | Site/fieldnote.py (working tool — the machine half); Site/Fieldnotes_2026-08-15_First-Shipping-Run.md (entries 37–41 + dispositions); Fractal_Command_Index (stamp inside — v0.2); Decision Register (stamp inside); KNet (external — P-003, the paper passport, its own jurisdiction) |
| Document ID | DOC-01M0562E79PN1FVVEF5KPXGVZX (minted 2026-08-16 via `close.py --create`; stamped by the post-mint revise) |
