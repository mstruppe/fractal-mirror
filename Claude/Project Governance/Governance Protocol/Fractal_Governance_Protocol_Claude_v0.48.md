# Fractal Governance Protocol — Claude Series v0.48

**Topic:** The seventeenth Code session (2026-08-17) — **the refinement-pivot session.** Max opened it by re-deciding the queue's shape: *"the flip will no more be anticipated at an artificial point (beta 0.4) we publish when it's necessary"* — **C-107**, the flip de-triggered to an ordinary open item and **refinement-through-use** declared the operating mode (C-094 promoted from loop to program), with KNet's **three-channel role stated in full** on his addendum (first-child governance evidence · research whose subject is FRACTAL's big picture · method standards forged in use), the **controlled network** named (many governed instances under one custody *are* the network), **OQ-33** opened (instance migration / kernel upgrade path), and the **registry vision** banked into the Knowledge Network Foundation §6 (v0.3 — the mirror as the network's service surface; the flip's necessity trigger given a face: the first off-machine registry consumer). Two ultracode flights flew: the **refinement flight** (re-based v0.47 commission — three persona simulations on the beta-0.4 tree, the capture v1.2 work-over at Max's 70–80% ruling, the site stage) and the **distillation flight**, born of Max's mid-flight friction (*"you need to tell me the phases of the flights and which effort"*) and his design — *"ultracode flight can be fractal language to a multi set of flights … both procedures need their standardised conduct procedures and reportings etc. … already provided by history it only needs to be implemented as such"* — **C-108**: the flight tier's two-level taxonomy (ultracode flight = container; Scan, capture-review, simulation = pluggable procedures), the fixed launch sequence (game plan → agreement → declared go with effort set before launch, effort immutable mid-flight), **beta-0.5 declared the registry release**, and the two standards distilled from history, adversarially checked (11 findings cured), walked with Max section-by-section and ratified **"full slate"** — `Fractal_Ultracode_Flight_Protocol_v0.1` + `Fractal_Scan_Procedure_v0.1`, DOC-minted, the Scan-#5 checker gate armed. A `/look knet` on Max's steer surfaced the child's **context-fidelity experiment** (Q07, accepted design v0.2, its H6 reserved as the first direct FRACTAL experiment); his **"go on all"** fired **C-109** — the frozen-tree grant (a clone of the mother at the anchored beta-0.4 tag, the C-096 asymmetry extended to data: frozen copy, never the living repo), the context-fidelity probe queued as the registry's first foreign-forged candidate, fieldnotes FN-0004/0005 (`knet`), and the OQ-32 multi-writer datum (two AI writer identities live in the child). The refinement flight landed with the session's sharpest external result: **all three personas returned "not yet — but the substrate is right," and every wishlist is registry-entry-shaped** — the registry validated as the adoption gate. Max's calls at landing: capture v1.2 **held** (v1.0 stays the backbone of record), the **full site slate applied** (tagline to the proven memory claim, the model section, the birth-path proof, honest-limits expanded, one-public-home, the registry as one labeled vision passage, the FN-0005 command library), sim findings **collected** into the flight record (`Flight_2026-08-17_Refinement.md`, `RF1-…` ids) as the next session's head. Three more fieldnotes protocolled in-lane (the command-library section, `publish` FN-0005; the drone-as-triggered-suggestion, `general` FN-0003 — the fifth conduct candidate banked; model-capacity benchmarking, `general` FN-0004 — the KNet transfer anchored). No release packed — under C-107 a pack wants a reason, and beta-0.5 (the registry release) is the named next one.
**Status:** Ratified (2026-08-17, in-conversation per C-033 — the verdicts quoted per decision below; the close Max's explicit command: *"/close"* with his three landing calls) · **Date:** 2026-08-17 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.47

---

## 1 · The decisions

### C-107 — Refinement-through-use (the flip de-triggered; the operating mode)

Max's verdict, verbatim: *"the flip will no more be anticipated at an artificial point (beta 0.4) we publish when it's necessary. the being public doesn't change anything and our identity isn't finished, a marketing scheme hasn't even been named once, therefore the going public part is not important and can stay on the agenda as simply open status. what I do want, is to refine fractal."* His addendum of record: *"KNet is very rich in knowledge extraction for many reasons, firstly being the first child, secondly it's research revolves around the big picture of fractal, and within working with KNet we do what we do in fractal we improve fractal itself — I want that to be noted."* The registry extension: *"this could go in a direction where the mirror ships a guide for a registry full of standards and skills … the start up only needs to tell where to find and navigate the register."* Registered on his go (*"is this all being registered"*). Full decision text: Register C-107. With it: **OQ-33** opened; Knowledge Network Foundation → **v0.3**.

### C-108 — The flight tier and the registry release

Max's design across two exchanges, verbatim anchors: *"the ultracode flight needs a protocol of conduct as does the scan procedure, these btw are belonging to the standards and skill registry we need to implement into beta 0.5"* and *"ultracode flight can be fractal language to a multi set of flights covering a thorough check of parts or the entire system while scan is a specific procedure to find loose ends, which can be implemented into the ultracode flight … the good news is, already provided by history it only needs to be implemented as such."* His clarifying notes pinned same session: the Scan standalone-or-plugged; the fixed launch sequence with the pre-go effort reminder (effort immutable mid-flight). **Executed:** the distillation flight (3 agents, declared contract) extracted both standards from the record; the verifier's High — the flight draft missing C-108's own launch clause — plus 10 further issues cured; Max walked both drafts and ratified **"full slate"**; landed, DOC-minted, routed (Index v0.36+). The Scan-#5 checker gate stands as written.

### C-109 — The frozen-tree grant (first cross-instance data grant)

On the `/look knet` report (Max's steer: *"take a look at the experimental track, I believe KNet can deliver an experimental method to really test fractal"* — confirmed: Q07's H6 is FRACTAL's structural claim made falsifiable), Max's **"go on all"** ratified the slate: the frozen clone at `~/Desktop/fractal-frozen-beta-0.4` (detached HEAD — a C-091 reference surface by construction; the C-096 asymmetry extended to data grants: a child receives a frozen copy of an anchored release state, never a window into the living mother); the probe queued as registry candidate; the metrology offer's literature half noted delivering in the child; fieldnotes FN-0004/0005; the OQ-32 annotation. The KNet-side hand-off (the path) travels through Max, per C-096.

## 2 · Also of this session (recorded, no numbers)

- **The two flights as first conformance data:** the refinement flight launched *undeclared* (the friction that fired C-108) and later miscounted its own agent roster in the declaration (8 declared, 7 roles — the flight record's honest deviation); the distillation flight declared its contract but preceded the go-declaration rule it was itself distilling. The standard's first fully conformant launch is the next commissioned flight.
- **Capture v1.2 held:** the work-over draft (6,300 words, 21 deltas, verification issues cured) stands in the session record as a draft; **v1.0 remains the approved backbone**; the site slate drew only on approved v1.0 content and record facts.
- **The sim slate (`RF1-…`):** collected in `Flight_2026-08-17_Refinement.md` (canonical, frozen); grouped rows enter the Register's findings ledger Open — the next session's commissioned work by Max's call (*"collect all the sim findings, we work on it next session"*). Headline: the registry is the adoption gate; plus the empty-`Site/` release defect, the checker-vocabulary extension gap, PERSON-vs-AGENT, and the micro-close need.
- **Five conduct candidates now banked** for the next conduct-touching Settings reissue: suggestion hygiene · board offer · always-flag · one-open-loop · **the drone offered actively at outsourcing need** (`general` FN-0003, the C-102 pattern extended to C-066).
- **Benchmarking anchored** (`general` FN-0004): model-capacity floors per use intensity — the issue transfers to KNet by Max's own hand; mother-side siblings named (OQ-28, Q07's instrument, OQ-32's model dimension, the effort-tier gap).

## 3 · Ripple

Register → **v0.63** (C-107–C-109 protocol-referenced; RF1 legend + grouped rows) · Rule Overview → **v0.39** · Local Context → **v0.67** · Context Index → **v0.37** · Architecture State → **v0.24** · Agenda Board regenerated · Knowledge Network Foundation v0.3 (in-session) · GENESIS/BOOTSTRAP/Schema/Template/Settings/adapters unchanged with reasons (checklist walk, Local Context §Standing refresh items). New canonicals this close: this protocol · `Flight_2026-08-17_Refinement.md` · (in-session: the two ratified standards, DOC-minted). No release packed (C-107; beta-0.5 = the registry release, next).

## Document identity

**Identity:** `Fractal_Governance_Protocol_Claude_v0.48` · **Domain:** GOV · **Author:** Claude · **Reviewed By:** Max (2026-08-17, in-conversation per C-033) · **Document ID:** DOC-01M07MME41BW1TK5F6SYYXX0HQ (minted at this close via `close.py --create`; stamped by the post-mint revise)
