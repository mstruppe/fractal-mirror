# Fractal Governance Protocol — Claude Series v0.21

**Topic:** Client adapters become canonical artifacts (C-065): `CLAUDE.md` is promoted from disposable stamped projection to canonical document — identity `Fractal_Claude_Code_Adapter`, version stamped inside, DOC-minted, and machine-guarded in `check_versions.py`'s STRICT scope. Rationale of record (Max): toward shippable FRACTAL, the adapter file is *the identity any agent takes shape from* — the untouchable box, not disposable glue. Conversation Settings remains the sole normative source of conduct; canonization covers the artifact, never a second rule source.
**Status:** Ratified (2026-08-14 — ratified in the conversation it records, per C-033) · **Date:** 2026-08-14 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.20

---

## 1. Context

Same conversation as v0.20 and its adapter-landing postscript. The postscript had landed `CLAUDE.md` under the then-standing classification: a derived projection (C-003/C-035), deliberately outside `check_versions.py`'s scan scope — the canon/projection boundary argued for leaving it out, and the C-059 checklist row covered its stamp by hand. When that exclusion was surfaced, Max overruled the classification with a forward-looking rationale: *as FRACTAL is built toward a shippable tool, most files become an untouchable box — the identity any agent takes shape from.* An adapter that defines what an agent **is** on a surface is not disposable glue; it is part of the product's identity and deserves the canon's full guardrails.

## 2. Questions investigated

1. Does canonizing the adapter create a second source of behavioural truth? *(→ No — deliberately guarded against: Settings stays the sole normative source (C-035 compression discipline); C-065 canonizes the artifact — its identity, existence, version, and stamp — not its content's authority.)*
2. What does canonical treatment concretely mean here? *(→ The BOOTSTRAP pattern: stable root filename, `Fractal_` identity, version inside (C-012), DOC mint with route alias (C-041), STRICT scan scope (C-060).)*
3. What of the Cowork sibling adapter? *(→ Stays a projection: it lives in the vendor app, unscannable and unmintable — excluded by impossibility, not by choice. The C-059 row still walks both stamps.)*

## 3. Decision of record

- **C-065 — Client adapters are canonical artifacts (executed for the Code surface).** `CLAUDE.md` is canonized: identity **`Fractal_Claude_Code_Adapter`**, living document with stable root filename (C-012), **v0.1** stamped inside, **DOC-minted** with a route alias (C-041), and added to `check_versions.py`'s scan scope in the **STRICT** class (C-060) plus the LIVING registry, so its stamp and claims are machine-checked at every close. **Boundary held:** Conversation Settings remains the sole normative source of conduct — the adapter's content is and stays a governed C-035 compression, re-projected (never hand-forked) at each Settings reissue; its refresh triggers say exactly that. **Rationale of record (Max, this conversation):** toward shippable FRACTAL, adapter files are the identity any agent takes shape from — the untouchable box; treat them as canon while the product is being built. The Cowork Custom-Instructions field remains a projection (vendor-held; excluded by impossibility). The C-059 client-adapter-stamps row continues to walk both. *(Working Decision.)*

## 4. Executed this close (one `[GOV]` commit, C-037; push = Claude, C-064)

- **`CLAUDE.md` → v0.1 (canonical):** identity header, version stamp, dual-nature clause (canonical artifact / compressed content), refresh triggers, revision history; DOC-minted with route alias `Fractal_Claude_Code_Adapter`.
- **`check_versions.py`:** scope + STRICT + LIVING registry gain the adapter (docstring updated) — the previous postscript's known gap closed.
- **BOOTSTRAP → v0.9** — §2 Code-adapter paragraph upgraded to the canonical classification (C-065).
- **Context Index → v0.14** — adapter row added to the canonical-documents table; protocol-series range → v0.21; Register citation made version-agnostic (C-012 stamp-inside, continuing the churn-class cure).
- **Decision Register → v0.19** — C-065 entered; Sources and revision history.
- **Rule Overview → v0.17** — document-identity row extended (client adapters canonical, C-065); ledger caveat → Register v0.19; Register citation made version-agnostic.
- **Store** — two mints (this protocol + the adapter DOC: create + route alias + `topic:GOV` + `agent` each) and five `revise` events (Register, Rule Overview, Index, BOOTSTRAP, Architecture State — the last a sources cure from the checker's ripple pass); **63 nodes / 278 events**; both checkers green before commit.
- **Local Context → v0.27** — decision recorded; checklist walked; board republished (stamp → v0.27); Return Package second postscript.

## 5. Calls recorded (Max, 2026-08-14, this conversation)

1. **The classification:** "treat the CLAUDE.md file as any other canonical document" — overruling the projection-class exclusion after its arguments were laid out on request.
2. **The rationale, verbatim in substance:** once FRACTAL is a shippable tool, most files are an untouchable box — the identity any agent takes shape from. Recorded as the forward pointer for the shipping tier (OQ-16 trajectory: the adapter class is product identity, not vendor glue).

## 6. Ratification record (2026-08-14, in-conversation per C-033)

Max asked for the arguments for the standing exclusion, weighed them, and overruled with the canonical-treatment call and its rationale. Claude executed the full treatment in the same pass — identity, mint, scan scope, and the ledger/pointer ripple — committed as attributed author (C-037) and pushed per C-064. Ratified in the conversation it records; no caveats.

## 11. Open Questions (TBD)

*(Per C-034 these are entered in the Register's cumulative ledger.)*

- None opened; none resolved. Standing items carry: OQ-4, OQ-6, OQ-9, OQ-13, OQ-16 (C-065's rationale is its new forward pointer), OQ-17, OQ-18, OQ-20, OQ-23, OQ-25, OQ-26, OQ-27. The review ledger stays fully green.

---

| Field | Value |
|---|---|
| Document Title | Fractal Governance Protocol — Claude Series v0.21 |
| Document Type | Governance Protocol (historical artifact, dated identity — C-012) |
| Version | v0.21 |
| Status | Ratified (2026-08-14, in-conversation per C-033) |
| Domain | GOV |
| Prepared By | Claude |
| Reviewed By | Max (2026-08-14, this conversation) |
| Date | 2026-08-14 |
| Parent Protocol | Fractal Governance Protocol — Claude Series v0.20 |
| Related Documents | CLAUDE.md v0.1 (`Fractal_Claude_Code_Adapter`); Conversation Settings v0.4 (normative source); Decision Register v0.19; Rule Overview v0.17; Context Index v0.14; BOOTSTRAP.md v0.9; Local Context v0.27; Return_Package_2026-08-14_First-Code-Session.md (postscripts) |
| Document ID | DOC-01M00B43C0THT16GFBHK9RMDRD (minted 2026-08-14, C-041, per this protocol) |
