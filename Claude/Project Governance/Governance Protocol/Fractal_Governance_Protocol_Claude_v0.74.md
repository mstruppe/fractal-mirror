# Fractal Governance Protocol — Claude Series v0.74

**Topic:** The fortieth Code session (2026-08-23) — **C-140 minted: THE UPGRADE-INSTALL PROGRAM — the wave planned, the advisories posted and machine-gated, the native-form interview, the seam-pair windows; BETA-0.8 PACKED on "ratified, let's pack it up."** Opened `/fractal` onto the post-39 queue (buffer 8/10 relayed; the postman's first opener poll — CN-0002 clean, CN-0001's placeless WARN as expected). **(1) The upgrade-procedure discussion on his direction** (*"Now that we have 0.8 ready, let's talk about the upgrade procedure for both children"*): the KNet re-foundation assessment refreshed from the record (Protocol v0.71 item 6 — fix-in-place never re-pours the birth layer · full rebirth pays with the provenance chain · **the re-foundation in place** recommended and adopted: rebirth the state, keep the story), his branch-freeze memory resolved onto the doctrine (git history is the story; a **signed boundary tag** the compatible refinement); KNet's ground state read honestly (dirty tree — 11 items, the Q07 drafts; Gate 0 blocked until its own close); **his PR question answered decisively — update, never re-birth** (his conditional: *"re-birthing PR would probably result in about the same work as updating. If re-birthing would be a better choice I'd opt for it"*; the assessment: the P-001/P-002 citation chain severs · KMP §10.2 rules it — *a young instance's migration is thin, never a re-birth* · the light lane's named first specimen would be lost); the one-act fold-in laid out gate-by-gate. **(2) The commission** (*"safe this plan as a document, from this we will create a new doc series: 'upgrade install procedure'. this will be the v0 … we use this doc now to have a game plan which we protocol. We adjust, we execute, we test and learn"*): **`Fractal_Upgrade_Install_Procedure_v0.0`** authored (Draft, GOV — the campaign runbook instantiating the KMP, never duplicating it; §0 the series law, §7 the execution log, §6 the distillation contract), then **expanded to full completeness on his rider** (*"use all steps of what is now necessary, so nothing gets lost … the elimination of information is the job later to form v0.1"*) — the pack ritual spelled, the advisory rows instantiated, both crossings step-spelled including the IPF §2.2 ceremonies, the thirteen-row KNet census intake, the mother-side landing steps. **(3) P3 verified on his "KNet is now closed":** the read confirmed `447cd97` ("Close beta-0.7 semantic smoothing"), tree clean, `main` even — the campaign log's first row. **(4) The Agent Teams review on his link** (agentteams.live): an open-source desktop orchestration shell over agent runtimes — **independently converging on the interface place's file-based-mail architecture** (durable inbox files, read between turns, pull-never-push) while carrying the opposite trust model (one app's bubble, no identity/consent layer) and an `info_for_agent` hidden-instruction pattern that is **the exact inverse of the C-096 floor**; captured as **FN-0008** on his "yes record that please" (the ratified reading beneath the block; the watch shelf beside FN-0003's pair). **(5) "okay let's start the procedure: what is step one?"** — P1 pulled before P0 per the plan's own ritual: **both advisories authored and POSTED — IF-0016 (KNet) + IF-0017 (PR)** via `postman.py post` (the advisory lane's first exercise; index conformant, 16 rows, 2 connections), the KMP v0.2 §8 nine-row template's first instantiations — pin identity (fingerprint + principals + the signers-bind verbatim; SHAs structurally absent), transport, the hand-derived C-134→C-139 delta inventory with per-row stances, the receipt hand-off, the feedback ask, per-child retrofit sets, **§9 the release class declared MINOR** with the audit instruction; **each §7 hygiene row a deliberate open slot stamping at the pack** (hand-off before the stamp refused by the advisory's own text). **(6) His two questions → the panel ruling** (the native question panel's **first governance exercise — deliberately itself the demo** of the capability question b asked about): **(a)** the advisory duty dissected — obligation governed (KMP §8), execution ritual, **HABIT-class by C-136's own test**, and the §7.5 generator's build trigger ("the second advisory authored") found **already fired at beta-0.7's advisory, unnoticed**; his ruling *gate now, generator at 0.9* → the **advisory-occurrence pack gate built into `close.py --pack`** (the forming release derived from the tag chain; no tag without the posted advisory, no hand-off without the §7 stamp; **fail-closed red-verified live** — the gate correctly flagged both unstamped envelopes and surfaced the standing pack duties early), the generator seated at the 0.9 shelf with the fired trigger recorded honestly (the trigger-watch gap itself a v0.1 finding); **(b)** the birth questionnaire — fixed in substance since ever (the protocol script + defaults-on-silence + the day-one receipt), chat in form; his ruling *build into 0.8* (a ratified slate amendment) → **Onboarding Protocol v0.8**: the §1 **native-form provision** — choice-shaped stations (§1.2b place · §1.4 territories · §1.5 durability · §1.6 credentials) render through native structured input where the client offers it, **one station at a time, never a bulk form** (the one-question law refined, not repealed), chat the floor everywhere, the free-text stations conversational; **vendor-generic on his mid-turn Codex note** (*"codex can use UI too, but maybe a bit differently"*) — per-client mechanics live in the **Client Library** (C-097), never in the protocol; `/begin` re-projected. **(7) His consolidation question** (*"what is the fractal onboarding protocol? … if we could consolidate them … less docs is better"*): the two documents the **same genus on opposite seams** (each the human procedure wrapping a seam's machine act — Onboarding : genesis.py :: Upgrade-Install : KMP), the seams themselves **never merging** (the two-seam doctrine; the F1 fracture class is what conflation looks like); the consolidation that serves the preference recorded as **§6.9**: at distillation, v0.1 weighed into the **KMP as its execution layer** — one procedure document per seam, zero net new standing documents; his call at distillation. **(8) The scope call** (*"The mining tool is pushed to beta 0.9"*): row 1 closed as MOVED, the whole charter re-seated on the §5 shelf (Update Plan v0.18); no advisory amendment needed — nothing of it ships in 0.8. **(9) The pair direction** (*"the user should then have a user document for both … this is fractal custom, we improve ourselves as we go"*): the two **seam-pair windows** adopted per Pair Procedure §3 (five steps, one sitting) — **`Fractal_Birth_Guide.html`** (half of the Onboarding Protocol; durable) + **`Fractal_Upgrade_Guide.html`** (half of the campaign plan; **RAM at campaign scale**, re-points or retires at distillation); the two guides deliberately mirroring the two-seam doctrine, each linking its sibling; all six user-document bars wired; the Upgrade-Install original's banner carries the pair line; **the recorded deviation** — a kernel-shipped original's banner never names an instance half (the seed-body discipline outranks the §3.2 both-banners letter; a kernel-original clause noted for the Pair Procedure's next reissue); both pairs C-127 custom, neither in the release — his scope reading confirmed. **(10) THE RATIFICATION AND THE TRIGGER — "ratified, let's pack it up":** **C-140 minted** (the block carrying his verbatims — the commission, the completeness rider, the conditional, the panel rulings, the pair direction, the scope call, the trigger); the pack cures executed — **GENESIS** clause + §0 to **C-140** with C-138 joining the instance-class exclusions · **AST §6 seats C-134–C-140** (C-134/135/136/137/139 + C-140 kernel, C-138 instance; counts **~107 / 9 / 24**, 116/140 substrate ≈83%) · **FN-0007 dissolved at this pack by its own law** — the ascent framing live in the **blog spine** (*the final ascent to fractal 1.0 … when the friction register runs dry, the summit gets its version number*) and the **Roadmap horizon**, the editorial law held (written at the pack, not before); the Context Index to v0.56 (the Upgrade-Install row NEW · **the KMP row's v0.1 lag caught at this walk and cured to v0.2** · the pair-series row current); Rule Overview v0.59 (the two C-140 rows). **(11) THE PACK:** the gates green — verify · check_versions · doctor-in-gates · anchor-occurrence · the clause-span/§0/partition trio post-cure · **the manifest gate's first governing run** · **the advisory-occurrence gate's first green after the §7 stamps landed** (the designed red→stamp→green sequence executed as written); **`beta-0.8` tagged signed + OTS-anchored** (the receipt pair into `Provenance/`), **the mirror rider ridden** (curated sync → rebuild commit preserving `.github/` + `gh-pages` → `--pack --mirror-only` green → the mirror's own signed `beta-0.8` under the public identity → its OTS pair → push `main` + tag), **`gh-pages` refreshed** (pull-before-push; the ascent re-frame + the wave's touched pages served); the **Update Plan's staged v0.18 committed before the reset** (S6-1.1), the reset to **beta-0.9 — the completeness release** (the seed artifact · the §7.5 generator · the Mining Tool · FN-0006's naming call) as the post-tag forward commit. **(12) This close:** Settings **unchanged with recorded reason** — C-140 is program/kernel-side (the gate a tool rule, the native form Onboarding-side interview conduct; no conversation-conduct rule moved, C-071 not triggered); this issue + the Return Package, **LC v0.93** (the pointer re-pointed — the F2 standing duty; session row 40; the wave's queue), the board's fortieth chip (the 30th rolled to the archive; the bar grown by the two seam pills), the Gas rows (a flight-free session — estimates marked honest), mint-before-stamp held (this issue + the campaign plan DOC-minted at this close), the gates, the commit (committer Max — his command), the push per C-064. Drag gauge: **LC + Context Index YELLOW** (standing; the diet offer rides); buffer **8/10** (FN-0007 out, FN-0008 in); the corpus audit lag now spans **C-134–C-140** (the next classification audit's intake). **Next: PR's crossing — the wave's P2, its own session through Max; the campaign plan §3 the script.**
**Status:** Ratified (2026-08-23, in-conversation per C-033 — the slate whole and the pack trigger on *"ratified, let's pack it up"*; the panel rulings, the scope call, and the pair direction each his own word in-session) · **Date:** 2026-08-23 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.73 · **Document ID:** DOC-01M0QDZ073GS2G68S1C971SA55 (minted at this close via `close.py --create`)

---

## 1 · The session's acts (each on Max's word, in order)

1. **The upgrade-procedure layout** — KNet's re-foundation refreshed, PR's update-not-rebirth ruled, the one-act crossing laid out.
2. **The series commissioned** — `Fractal_Upgrade_Install_Procedure_v0.0`, the game plan we protocol; over-complete by his rider.
3. **P3 verified** — KNet's close landed (`447cd97`), the campaign log's first row.
4. **Agent Teams reviewed; FN-0008 captured** — the convergence + the C-096 inversion, the watch shelf.
5. **P1 executed** — IF-0016/IF-0017 authored and posted; the §7 stamps deliberately open until the pack.
6. **The panel rulings built** — the advisory-occurrence gate (red-verified) + Onboarding v0.8's native-form provision + `/begin`; the generator's fired trigger recorded, seated at 0.9.
7. **The consolidation answer** — one procedure per seam; §6.9 the distillation question, his preference on record.
8. **The Mining Tool to beta-0.9** — Update Plan v0.18.
9. **The seam-pair windows adopted** — Birth Guide + Upgrade Guide; the kernel-original banner deviation recorded.
10. **C-140 minted; the pack cures** — GENESIS/AST §6 to C-140; FN-0007 dissolved into the blog spine + Roadmap horizon.
11. **THE PACK** — gates green (the two new gates' first governing greens), the tag + anchor, the mirror rider, `gh-pages`, the plan's staged edition then the beta-0.9 reset.
12. **This close** — the ripple, the gates, the commit on his command, the push.

## 2 · References

Register v0.91 (the C-140 block) · `Fractal_Upgrade_Install_Procedure_v0.0.md` (the campaign plan + its §7 log — DOC-minted this close) · IF-0016/IF-0017 (`Interface/Advisory_2026-08-23_Beta-0.8-Migration-{KNet,PR}.md`) + the Interface index stamp · `close.py` (the advisory-occurrence gate) · Onboarding Protocol v0.8 + `.claude/commands/begin.md` · `User Documents/Fractal_Birth_Guide.html` + `Fractal_Upgrade_Guide.html` (+ the six wired bars) · Update Plan v0.17→v0.18 + its half · Rule Overview v0.59 · Context Index v0.56 · GENESIS (clause/§0 to C-140) · Architecture State §6 (the C-134–C-140 seats) · `Site/blog.html` + `User Documents/Fractal_Roadmap.html` (FN-0007's two homes) · `Site/Fieldnotes.md` (FN-0008 in, FN-0007 out) · the Provenance `beta-0.8` pair + the mirror's `-public` pair · `Return_Package_2026-08-23_Beta-0.8-Pack-Upgrade-Wave.md` · KMP v0.2 (the law the campaign instantiates) · C-134–C-139 (the shipped slate this pack carries).
