# Fractal Governance Protocol — Claude Series v0.63

**Topic:** The twenty-ninth Code session (2026-08-19) — **the user-documents folder (C-126), the integration categories (C-127), the board's rolling window (C-128), the Gas Ledger pair (C-129), the Mining Tool's sorting, the beta-0.7 intake, and the grooming pass opened.** Opened `/fractal` onto the queue; Max drove six arcs. **(1) The folder, his direction verbatim** — *"sometimes it would be simply easier to open the agenda board myself instead of asking you. therefore the user docs should have dedicated folder I can go to in the folder system"* — proposed with the full complexity accounting (his check question: *"how much complexity cost adds this to the system?"*), ratified on his *"ratified"*: all four user halves moved to **`User Documents/`** at the repo root, the Pair Procedure reissued **v0.2** (the location law), the Board Format reissued **v0.3** (rule 2's home — forced honestly by its own path law). **(2) The Mining Tool sorted** — his correction of my capture-tool conflation answered (fieldnote.py ≠ the mining tool), then his *"yes"* + jurisdiction sentence: *"that tool build is actually KNet territory, here we only implement and update new kernels and develop governance standards"* — OQ-35 annotated: named **the Mining Tool**, spec seat the mother, build seat KNet, **decoupled from beta-0.6** (*"so we can release it faster and start the network augmentations"*). **(3) The beta-0.7 intake, his parallel-plan direction** — *"already start the update plan -beta-0.7, parallel to the beta 0.6"* — executed as an **intake shelf inside the one plan** (the RAM law intact: the shelf promotes to the new body at the pack; the design call reported, a split file offered and not needed). **(4) The integration categories, his separation directive** — *"I want a clear separation of concepts and tool, each defined by their integration category… this is thinking ahead of need. for now we have the kernel concepts, custom concepts, and tool i already in the making so we can already create a placeholder mark for it"* — C-127: five categories, two banked, the custom-class mirror exclusion, the Registry the home, the site's stranger section + the Mining Tool's placeholder. **(5) Both standing proposals on his "go"** — C-128 the rolling done bar + chip archive (his words: *"nothing is lost but doesn't need to stay on the surface"*; the mother's board conformant 54→10+44; the Local Context deliberately untouched — his TBD resolved no-touch) and C-129 the Gas Ledger pair (his commission: *"the user should have a better picture of what different procedure and operations take toll in 'gas'… the agents form on their native behaviours behalf an info system"* — built, seeded, natively fed, the Mining Tool's executing spec seed). **(6) The grooming pass opened** (the queue's head, with him): the guide held good (*"we leave it at that"* — the growing-section expansion banked post-network), the concepts page's guide section removed as redundancy on his first grooming directive. Closed on his *"let's close this session here and continue grooming in the next session"*.
**Status:** Ratified (2026-08-19, in-conversation per C-033 — C-126 on "ratified"; C-127 within his separation directive; C-128/C-129 on "go"; the OQ-35 sorting in his own sentences) · **Date:** 2026-08-19 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.62 · **Document ID:** DOC-01M0DJTCGF94N33S67NX9FRB62 (minted at this close via `close.py --create`)

---

## 1 · The session's acts (each on Max's word, in order)

1. **A check question answered first (no act):** old Return Packages cost zero context tokens unless deliberately opened — the orientation reads exactly one; the frozen records are recoverable-but-unloaded (the gas lane's ≈1,350-per-1,000-words number cited as the price of the opposite).
2. **C-126 — the `User Documents/` folder:** the four user halves `git mv`'d to the repo root's ninth folder; originals stay home (the pair split visible in the file system); **Pair Procedure v0.2** minted (the §2 location law — halves together, folder born on first use, never pre-scaffolded; the "three laws" count corrected to four) and **Board Format v0.3** minted (rule 2's home; the C-124-banked **extension-pill provision** landed; §4 adoption move mechanics); `close.py` `board_paths()` + `check_versions.py` STRICT + `genesis.py` shipped-standards re-pointed; GENESIS **v0.14**; Registry **v0.9**; Global **v0.8** (pointers); Index + Rule Overview + LC currency; the four halves' one link depth re-based. **The checker then caught 7 errors** — stale paths in the frozen Format editions and the C-048 row, stale GENESIS/Global claims in the STRICT Local Context — all cured with the house *path-current-since* pattern and honest rewording (historical facts kept true, never falsified): the takeover gate's discipline exercised on the move's own debris. **Scratch birth green both checkers** — a newborn receives v0.3 + v0.2 and its Interface place; the migration-visible tool changes flagged into the Update Plan for the §8 advisory.
3. **The Mining Tool sorted (OQ-35 annotated, no new number):** the capture-tool conflation corrected (three "captures" distinguished: `fieldnote.py`'s door · OQ-35's instrument · the C-103 review class); his naming + jurisdiction + decoupling sentences recorded verbatim on the row; the Update Plan's armed list released it; the LC carries it as **the Mining Tool lane** beside the release.
4. **The beta-0.7 intake (Update Plan §5):** the plan's banner widened from "the forming release" to "the release lane"; the reset law extended in place — **the shelf promotes to the new body at the pack**; seeded with the Mining Tool + the spec-reissue pair (S3-2.2/S5-2.4); rows are pointers, leaving by sorting call or promotion.
5. **C-127 — the integration categories:** kernel concept · kernel tool (his "maybe" resolved yes — the store's copied set exists as a class from birth) · custom concept (**never released** — the C-116 curated sync excludes custom-concept artifacts; the stranger meets the class, never an instance's copy; kernel pair halves keep shipping as the living demo) · installable tool (**banked** — the Mining Tool forging first, Registry §5 row + the site's placeholder mark) · plug-in (**banked**, named only). Canonical home the Registry's §1 block (**v0.10**); the site's concepts page gains the dedicated categories section + bib currency (Board Format v0.3 · Pair Procedure v0.2).
6. **C-128 — the rolling window (his "go"):** Board Format v0.3 §2 row 3 — the newest **ten** chips on the surface, older ones falling **verbatim** to `<Instance>_Agenda_Board_Archive.html` (append-only, newest-first, one standing bar link); replaces v0.2's era-compaction on the board surface; **the LC deliberately untouched** (its era table keeps its own diet-governed overflow — two reader-matched mechanisms, uncoupled). **The mother's board brought conformant in the same act:** 54 chips → 10 + the archive born with 44, script-executed and structure-verified; the board's stale v0.2 format claim cured in passing.
7. **C-129 — the Gas Ledger pair (the same "go"):** the pair series' second extension, §3 walked whole — `Fractal_Gas_Ledger.md` (CTX original: append-only durable data; process classes open · close · scan · flight · genesis · session; **methods honest per row** — budget-delta / tool-reported / estimate) + `Fractal_Gas_Gauge.html` (the owner's price list; **the sixth pill on every bar**). **The feeding law is native:** agents read their own budget every turn; the close appends (a new C-059 walk row — `close.py --walk` picks it up from the LC table by construction). **Seeded non-empty:** the Scan-5 lane's rows + ratios + the first live native reading (this session's own orientation ≈85k). **The Mining Tool connection recorded everywhere it matters:** the row grammar proven manually on the mother is the vocabulary KNet builds the instrument against (C-008 manual-first across the sorted jurisdiction split; the Registry row reads *executing since C-129*). Category: custom concept — mirror-excluded with the Update Plan pair (step-6's widened exclusion; extension pills drop in the synced copy so the bar tells the truth in every tree).
8. **A design question answered (weighed and declined):** a background register indexing all chips — **redundant by the system's own rules**: the protocol series is the canonical list as a *class reference* (filename-enumerable, never drifts), the LC table and board+archive are its reader-matched faces, and a fourth projection would add a maintained enumeration (the Scan-5 recurrence class) with no new reader; any future need is a checker that *computes* board+archive and compares (the Board Format's banked trigger). Max's "great" closed it.
9. **The grooming pass opened (the queue's head, with him):** the guide ruled good as-is — its **growing-section expansion ("beyond one fractal") banked for after the network is established**; first directive executed: the concepts page's guide section removed as clustering redundancy (the top nav's Guide link the one path), the subnav's dangling entry with it, and the categories section's missing subnav entry caught and added in the same touch. **Grooming continues next session** — his closing word.

## 2 · Notes, banked, and open

- **Banked:** the gas pair's kernel candidacy (a C-122-class act if ever felt) · the guide's growing-section expansion (post-network) · the C-128 conformance checker (computes board+archive, the Format's existing trigger) · the check-register posture (weighed, declined — recorded in act 8) · the Settings bench holds at **seven** (no conduct touch: C-126–C-129 are structure/format/data).
- **Drag gauge at this close:** Index YELLOW (2,263) + LC YELLOW (3,690) — both diets already scheduled as Update Plan step 8 at the tagging close, the phase boundary. Buffer 4/10 — silent.
- **Pending on Max (delta):** the standing list unchanged, **minus** nothing and **plus** nothing new — the grooming pass simply continues as the next session's head (its material: buffer FN-0003/FN-0008 + his live directives).
- **The release picture stands reviewed in-session** (his request): one working step (grooming) before the eight-step tagging ritual; the manifest C-116–C-129; PR's birth + KNet's offer armed on the tag; the beta-0.7 intake parallel.

## 3 · The ripple (this close)

Register → **v0.83** (the C-126 · C-127 · C-128+C-129 rows; the OQ-35 annotation; the C-048 path-currency touch) · Rule Overview → **v0.52** (the pair-series/board rows carry C-126/C-128; the categories row) · Global Context → **v0.8** (pointers; §3 untouched) · Local Context → **v0.80** (session row 29; the Mining Tool lane; the Gas Ledger walk row; the walk table finalized) · Context Index → **v0.49** (the pair-series row's folder+extensions; the two standard rows; GENESIS v0.14) · Agenda Board restamped (the twenty-ninth chip; **the rolling window's first live regeneration** — the oldest surface chip falls, the archive at 45) · **New canonicals:** `Fractal_Agenda_Board_Format_v0.3` + `Fractal_User_Document_Pair_Procedure_v0.2` + this protocol (DOC-minted at this close; v0.2/v0.1 predecessors frozen history) · **New instance organs (custom class, unminted per C-042):** `User Documents/` (the four moved halves + `Fractal_Gas_Gauge.html` + `Fractal_Agenda_Board_Archive.html`) · `Fractal_Gas_Ledger.md` (+ its first close-fed row at this close) · **Reissues:** GENESIS **v0.14** · Registry **v0.9 + v0.10** · Update Plan **v0.2–v0.5** (the intake · the manifest · the exclusions) · **Tools:** `close.py` (board_paths) · `check_versions.py` (STRICT path) · `genesis.py` (ships v0.3 + v0.2; scratch birth green) · **Site (source-side; the `gh-pages` refresh rides the tagging close):** the integration-categories section + subnav · the guide-section cut · bib currency · the user-docs folder line. Gates: `verify.py` + `check_versions.py` green pre-commit; no pack — **the grooming pass continues next session, then the beta-0.6 tagging close** (`Fractal_Update_Plan.md` the path), then PR's birth.
