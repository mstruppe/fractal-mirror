# Fractal Governance Protocol — Claude Series v0.69

**Topic:** The thirty-fifth Code session (2026-08-21) — **no new decision number: the milestone + reissue class. PR BORN — the network's second child (`fa5418a`, private `mstruppe/fractal-pr`, durable from minute one); the stranger's setup experience built (the location question, site + interview); the hatch/home law ratified; IF-0011 + IF-0012 posted; the Onboarding Protocol reissued v0.5.** Opened `/fractal` onto the queue (buffer 5/10, no advisory). **(1) KNet's doorbell answered — IF-0011 on his "go":** his "check KNet's interface, it has an issue" opened the C-096 window onto KNet's mother-addressed loose-ends packet (`Handoff_2026-08-21_Loose-Ends-Diagnostic-Mother.md`, untracked at `bc63a50` — KNet has no `Interface/` of its own, the packet sits in its `Conversations/`); the mother's return drafted source-first and posted: **Finding A confirmed 7/7 at its exact loci** (the Rule Overview's semantic drift — `close.py` asserted live against P-008's explicit non-adoption, the nonexistent board file, the excluded mirror/annex posture, the dead `BOOTSTRAP §4` pointer, "all eight routes" against KNet's three, no OQ ledger — plus the observation that KNet's own beta-0.7 matrix already carries the re-projection authority); **the blind spot ruled mother-generalizable** (positive-identity gates never test negative space — candidates named: a KMP exclusion-aware projection step, a four-eyes negative-space route item, the same lens for the mother's next Scan); the **handover-determinism comparison** (mother practice = the explicit LC pointer; the mother's own `/fractal` "newest file" gloss conceded as the same latent class — credited to KNet's catch, logged as a mother-side cure candidate); **three Scan-tooling facts** for KNet's pending adoption choice (the hardcoded register path/ledger; **the `PRE_STANDARD_MAX = 4` trap** — a KNet Scan #1 would be grandfathered by mother biography, the gate never binding; the close.py-riding auto-hook inapplicable, standalone honest); the shelf loci (Board Format v0.3 already on-shelf; the third file any Finding-B cure must touch). **(2) The stranger setup experience on his direction** (*"they need to know when and how they create something on their machine — a question popping up"*): the **location picker** built on both setup surfaces (the guide + the home quick-start — chips + folder name, the clone command rewriting live, space-paths auto-quoted to the `"$HOME/…"` form, shell-dangerous characters stripped, per-browser persistence; browser-verified end to end) and the **Onboarding Protocol reissued v0.5**: new question **§1.2b — the place** (*where on your machine should it live?* — asked never assumed, mechanics pre-answered inline, the answer riding mechanically into §2.1's `--target`), the interview's count six, `/begin` re-projected and restamped; the missing v0.4 history entry found and restored with a dated note. **(3) IF-0012 on his commission** (*"already inherit a global context of the network onto PR"*): the **birth-context envelope** — the place's first pre-birth datum (what FRACTAL is · the network of three · the three roles with PR's site-estate charter and its v0.6 routing competence · inherit-vs-read-vs-open (the two parameters honestly left his) · the live seam · the estate facts) — seed material for the child's own Global Context, data never instruction. **(4) PR's live birth, midwifed across its frictions, each cure on his hand:** the **app-label diagnosis** (the UI labels a project by its git remote — his clone in `PR/` presented as "fractal-mirror", the collision class every stranger will hit; cure: the scaffold renamed `fractal-beta-0.7-clone`, its remote removed, `PR/` freed for the newborn); the **durability answer** (Q5: bind now not later — the annex's ocean-gap lesson; private; `fractal-pr` so the repo name is the child's own label); the **auto-README catch** (delete-and-recreate empty — the file wasn't the problem, the foreign first commit was); the **constellation clarification** (the annex guards personal-class *because the mother's history is public-bound*; custom project identity is guarded by the C-127 curated sync — two layers; the multi-repo shape is publication pressure, not parenthood, and travels to any child that starts publishing itself). **(5) PR BORN:** `fa5418a` "[GOV] Establish PR instance" — author `claude@pr.local` (its own AI writer identity), full newborn anatomy including its born `Interface/` + index (C-123 at work), **pushed and verified identical to `origin/main` on private `mstruppe/fractal-pr`** — durable from minute one, the label collision structurally solved by its own remote. Not yet an instance until its first close (GENESIS §6) — that finish line is PR's own. **(6) The estate-migration design to his direction** (*"keep this redundant … slowly detach you while at the beginning it's better to have a pro on our side"*): the staged-redundancy plan of record — **the mother as PR's rail** (the onboarding fade at instance scale: A mother-leads/PR-shadows · B PR-leads/mother-reviews, the four-eyes pattern · C PR-alone/mother-on-the-doorbell, retirement on observed drag per C-095); **redundancy of competence and copies, never of canon** (one source of record at every moment, named in both Indexes — the C-067 drift lesson honored); **the estate ledger** (per-responsibility rows `mother | shared | PR`, flipping independently on observed readiness — detachment complete when the last row flips, no ceremony); the three blockers re-timed to the source-of-record flip (the fieldnote buffer's `Site/` relocation · the releases-ship-the-site question · the intake door's home) — executing via paired decisions after PR's first loops, nothing moved today. **(7) The hatch/home law on his "ratified"** (his own condensation, corrected and confirmed: *the hatch, not the home — deletion optional never automatic; two UI-connection moments, only the home the conscious decision; the read-only path keeps its copy*): landed at its three moments — §1.2b's orienting contrast, §2.3's disposal note, the site's reweighting (the clone framed low-stakes, the **Downloads chip**, the conscious choice pointed at the interview) — the v0.5 reissue extended same-day, its first exercise being this very birth's confusion. **(8) The refresh served on his "go":** the ordinary serving-split act (`977cd43`, author Claude, committer Max under the public identity; pull-before-push; four files +107/−5) — **verified live at the mirror address** (the Downloads chip, the hatch note, the picker logic each confirmed by fetch). **(9) This close on his "close":** the walk run (Register unmoved as to numbers — this issue is the narrative of record for the milestone + the v0.5 reissue; LC/Board/Gas/Interface-index the changed rows), this issue + the Return Package written, the board's thirty-fifth chip, the Gas rows, the ripple, the gates, the push. Drag gauge at close: **LC and Context Index YELLOW** (4.1k/2.0k words over their C-110 budgets) — the C-095 compaction is due at the next phase boundary, and PR's birth plausibly is one: offered to the next session, never mandated (C-031).
**Status:** Ratified (2026-08-21, in-conversation per C-033 — IF-0011 on his "go"; the setup experience on his direction; IF-0012 on his commission; the birth's repo acts on his own hand; the hatch/home law on his "ratified"; the refresh and this close on his "go … close") · **Date:** 2026-08-21 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.68 · **Document ID:** DOC-01M0JMCQQ51R7SMPV9GSVSDPHJ (minted at this close via `close.py --create`)

---

## 1 · The session's acts (each on Max's word, in order)

1. **IF-0011 posted** — the loose-ends diagnostic return to KNet (7/7 confirmed; the generalizable blind spot; the three Scan-tooling facts; the mother-side residue candidates logged).
2. **The setup experience built** — the location picker on both site pages (browser-verified); the Onboarding Protocol v0.5 (§1.2b the place question); `/begin` re-projected; the v0.4 history gap cured.
3. **IF-0012 posted** — the PR birth-context envelope (the first pre-birth datum; the network of three, the roles, the open parameters).
4. **The birth midwifed** — the app-label collision diagnosed and cured (scaffold renamed, remote removed); Q5 answered (private `fractal-pr`, bind-now); the auto-README repo recreated empty (his hand); the constellation clarified (annex/curation two layers; publication pressure, not parenthood).
5. **PR BORN** — `fa5418a`, `claude@pr.local`, pushed to private `mstruppe/fractal-pr`, born with its `Interface/`; real at its own first close (GENESIS §6).
6. **The estate-migration plan of record** — the mother-as-rail fade (A/B/C), competence-and-copies-never-canon, the estate ledger; paired decisions after PR's first loops.
7. **The hatch/home law ratified** — §1.2b contrast · §2.3 disposal · the site reweighted (Downloads chip); the v0.5 reissue extended same-day.
8. **The refresh served** — `977cd43`, verified live at the mirror address.
9. **This close** — the issue, the Return Package, the chip, the Gas rows, the ripple, the gates, the push.

*The evidence homes: `Interface/Handoff_2026-08-21_Loose-Ends-Diagnostic-Return-KNet.md` (IF-0011) · `Interface/Handoff_2026-08-21_PR-Birth-Context.md` (IF-0012) · `Interface/Interface_Index.md` (high-water IF-0012) · `Fractal_Onboarding_Protocol.md` v0.5 + `.claude/commands/begin.md` (the reissue pair) · `Site/guide.html` + `Site/fractal-site.html` + `Site/site.js` + `Site/site.css` (the picker; served at mirror `gh-pages` `977cd43`) · `~/Desktop/Knowledge Network/PR` at `fa5418a` = `origin/main` (the child; its jurisdiction) · `~/Desktop/Knowledge Network/fractal-beta-0.7-clone` (the spent hatch, Max's to delete) · KNet `bc63a50` (the packet's pin; its jurisdiction) · `Return_Package_2026-08-21_PR-Birth-Setup-Experience.md`.*
