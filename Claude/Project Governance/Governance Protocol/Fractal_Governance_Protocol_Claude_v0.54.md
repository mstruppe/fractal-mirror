# Fractal Governance Protocol — Claude Series v0.54

**Topic:** The twentieth session, fourth act (2026-08-17) — **C-118, the Scan conformance checker.** Max asked why the Scan-#5 checker "is blinking up" on the agenda; the answer was the queue's one ratified obligation with no calendar — the Scan Procedure v0.1 shipped §12 *prose-first with the trigger armed* ("Scan #5 does not close without the conformance checker"), so the item stands visible until built, by design (a latent hard gate cannot be quietly forgotten — the standard refusing to become its own blind spot). In the same exchange the **procedures blog gained the missing paragraph** (the prose+checker rule and the armed gate were not told on the site; the scan article now carries them — Max: *"if not it should have a paragraph"*). His *"go"* fired the build; his *"ratified"* landed it.
**Status:** Ratified (2026-08-17, in-conversation per C-033 — Max's *"go"* on the build proposal and *"ratified"* on the gated result) · **Date:** 2026-08-17 · **Parent Protocol:** Fractal Governance Protocol — Claude Series v0.53 · **Document ID:** DOC-01M0B1VV0ET8QMQ121031FD1BB (minted retro 2026-08-18, the C-041 lapse cured per Scan #5 S5-2.1; stamped by the paired revise)

---

## 1 · The decision

### C-118 — `check_scan.py`, the §12 gate discharged (the eighth store tool)

The C-060 lineage as the standard prescribes: stdlib-only, read-only, red blocks. Two halves per §12 — **report conformance** (filename grammar; control header incl. ratification Status, Reviewed By, Document ID; the state-scanned block's HEAD hash, store counts, baseline; the `S<n>-x.y` id grammar with **ordinal correctness** — the S2-2.2→S3-2.2 re-entry precedent enforced; the disposition-seed table with cell sanity) and **ledger integration** (every seed id resolves to a Register ledger row; no ledger S-row anywhere without a disposition — C-058's leave-only-by-recorded-disposition, machine-enforced). Frozen history is honored: Scans #1–#4 predate the standard and can never be revised (C-058), so pre-standard ordinals report WARN, never ERR — **the red block binds Scan #5 onward**. Gated at ratification: **Scans #3 and #4 PASS with zero warnings** (the distillation claim proven mechanically), #1/#2 grandfather at 7/3 warnings, and a synthetic broken Scan #5 **fails with 11 errors, exit 1**, every check class firing. Code artifact (the C-006 exception — not DOC-minted). Full decision text: Register C-118.

## 2 · Ripple

Register → **v0.72** · Registry → **v0.3** (the Scan row's checker half: *prose-first, gate armed* → **shipped**) · store README + Context Index + Local Context tool rosters at **eight tools** · the procedures blog's scan article carries the gate paragraph (rides the close with `Site/`). The agenda's blinking item retires; a Scan #5 can now be commissioned without a latent tooling block. Remaining named build items after this act: the C-110/C-112 close-tool mechanization and the Migration Procedure's §7 triggers as they fire.

---

*Author: Claude (AGENT.AI.CLAUDE) · Reviewed and ratified by Max in-conversation (C-033). Parent: v0.53. The session continues; the close will carry the walk.*
